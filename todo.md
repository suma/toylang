# TODO - Interpreter Improvements

## 完了済み ✅

ALLOCATOR-PHASE5-fixed-buffer-auto-cleanup. **`with allocator = FixedBuffer::new(cap) { body }` の temporary-form auto-cleanup** (2026-05-04): Arena auto-cleanup (`03d6c10`) と同形で FixedBuffer も対応。Design A (scope-bound) を 2 つ目の wrapper struct に拡張、3 backend で対称な semantics。**runtime** (`compiler/runtime/toylang_rt.c`): 新 helper `toy_fixed_buffer_drop(handle)` — fixed_buffer slot 専用、tracked allocations を free + bookkeeping array clear + `used = 0` にリセット。non-fixed_buffer slot は no-op (defensive、既存 `toy_arena_drop` の pattern を踏襲)。**JIT mirror** (`compiler/src/jit.rs`): 同形 `toy_fixed_buffer_drop` extern fn + `register_runtime_symbols` 登録。**IR** (`compiler/src/ir.rs`): 新 variant `InstKind::AllocFixedBufferDrop { handle }` (Display も対応)。**codegen** (`compiler/src/codegen.rs`): `rt_fixed_buffer_drop: FuncId` 宣言 + `RuntimeRefs::fixed_buffer_drop: FuncRef` + `AllocFixedBufferDrop` arm で `call(fixed_buffer_drop, [h])`。**WithScopeCleanup enum 化** (`compiler/src/lower/mod.rs`): 旧 `Vec<Option<ValueId>>` を `Vec<WithScopeCleanup>` (`None / ArenaDrop(h) / FixedBufferDrop(h)`) に refactor、`emit_with_scope_cleanup` が enum variant で適切な drop instruction (`AllocArenaDrop` / `AllocFixedBufferDrop`) を emit。**AOT lowering** (`compiler/src/lower/expr.rs::Expr::With`): 既存の `Arena::new()` 検出を `InlineAlloc { Arena, FixedBuffer(cap_ref) }` enum 化、`FixedBuffer::new(cap)` の場合は `cap_ref` を lower → `InstKind::AllocFixedBuffer { capacity }` で handle 取得 → 既存パスと同じ AllocPush + scope record + linear/early exit ともに `AllocPop` + `AllocFixedBufferDrop` emit。**interpreter** (`interpreter/src/heap.rs::FixedBufferAllocator::reset` + `evaluation/expression.rs::Expr::With`): `FixedBufferAllocator::reset` を `Allocator` trait override で実装 (tracked addrs を free + used=0)、`Expr::With` の lexical sniff を `inline_arena: bool` から `inline_temporary: bool` に rename し `Arena::new()` (0 args) と `FixedBuffer::new(cap)` (1 arg) の両方を検出、scope exit で同じ `allocator_rc.reset()` 経路 (Arena / FixedBuffer の override が実 free を実行、それ以外は default no-op)。**JIT**: silent fallback で interpreter 経路に流れて自動対応。**test** (`compiler/tests/consistency.rs`): `fixed_buffer_temporary_auto_cleanup_round_trip` (16-byte quota で 8+8+1 byte の 3 alloc → quota 超過の最後だけ NULL を確認、scope 終了後に active stack が default に戻る) + `fixed_buffer_temporary_auto_cleanup_early_return_round_trip` (`with` body 内 `return` から関数を抜けた後の active stack 復帰)。両方 3-way `assert_consistent`。1166 → **1168 tests pass**。**残**: 汎用 Drop trait + RAII (lifetime / 例外時 drop 順序の議論)、AllocatorBinding::Static/Local の lower 配線 (perf 最適化のみ)。

ALLOCATOR-PHASE5-arena-auto-cleanup. **`with allocator = Arena::new() { body }` の temporary-form auto-cleanup** (2026-05-04): ALLOCATOR_PLAN.md で確定した Design A (scope-bound) の最小 viable サブセット — temporary form の inline arena だけ syntactic sniff で auto-cleanup wiring。`val a = Arena::new()` 経由の named binding は user 管理 (`a.drop()`) を維持、FixedBuffer / Global は対象外、汎用 Drop trait は別 phase。**AOT** (`compiler/src/lower/expr.rs::Expr::With` + `mod.rs`): 新フィールド `FunctionLower.with_scope_arena_drops: Vec<Option<ValueId>>` を追加 — 1 entry / active with scope。`Expr::With` の lowering で allocator_expr が `Expr::AssociatedFunctionCall("Arena", "new", [])` の形なら、`InstKind::AllocArena` で handle を直接 emit (Arena::new method dispatch を bypass)、`AllocPush` + body lowering + (linear exit) `AllocPop` + `AllocArenaDrop`。早期 exit は既存 `emit_with_scope_cleanup` 拡張で `with_scope_arena_drops[depth-1]` から handle を取り出して `AllocPop` の直後に `AllocArenaDrop` も emit — `return` / `break` / `continue` 全 path で arena slot 解放。**interpreter** (`interpreter/src/evaluation/expression.rs::Expr::With`): 同形の lexical sniff で `inline_arena: bool` を計算、`allocator_stack.pop()` 後に `allocator_rc.reset()` (Allocator trait の default は no-op、`ArenaAllocator::reset` のみ実 free — `__builtin_arena_drop` と同じ semantics)。body が error で終わっても reset は走る。**JIT**: silent fallback で interpreter 経路に流れて自動対応。**test** (`compiler/tests/consistency.rs`): `arena_temporary_auto_cleanup_round_trip` (2 連続 inline arena + 各 with 終了後に `__builtin_current_allocator() == __builtin_default_allocator()` を pin) + `arena_temporary_auto_cleanup_early_return_round_trip` (`with` body 内 `return` から関数を抜けた後の active stack 復帰)。両方 3-way `assert_consistent`。1164 → **1166 tests pass**。**残**: FixedBuffer auto-cleanup (drop builtin 追加が前提)、汎用 Drop trait + RAII (lifetime / 例外時 drop 順序など設計議論)、AllocatorBinding::Static/Local の lower 配線 (perf 最適化のみ、const-prop 入る前は無意味)。

REF-Stage-2-compound-mut. **REF-Stage-2 (ii) — compound `&mut T` parameter mutation 伝播 (struct)** (2026-05-04): array-index (`25e548d`) の続き。`fn shift(p: &mut Point, ...)` のような compound `&mut T` parameter で caller binding mutation が AOT でも propagate するように。**問題**: AOT では従来 compound `&mut T` parameter は leaf-flatten erasure (Type::Ref を peel して leaves を per-field local 化) で渡していたため、callee body 内の `p.x = ...` は callee 側 leaf local だけ書き換え caller 側に伝播しなかった (interpreter / JIT は struct の `Rc<RefCell<Object::Struct>>` 共有のため自動的に propagate)。**設計**: MUT-SELF Stage 1 (`&mut self`) の Self-out-parameter writeback convention を generalize — 各 `&mut <compound>` param が leaf scalar 型を `Function::self_writeback_types` に寄与し、callee は exit 時に modified leaves を additional return として返す、caller が CallWithSelfWriteback で受け取って自身の binding leaves に store。**変更**:**(1) declaration phase pre-populate** (`lower_program` 内、関数 declaration loop): 新 helper `flatten_compound_leaf_types(module, ty, out)` を IR `Type` 上で再帰的に walk (struct fields / tuple elements / enum tag+payloads)、各 `&mut <compound>` param の leaf types を `self_writeback_types` に append。これにより main → shift のような forward-call orderingでも caller 側が正しい writeback shape を見られる (codegen も declaration-time の値で signature 構築)。**(2) body lowering** (`lower_body`): 既存 `pending_self_writeback_param` (Stage 1 self) に加えて、`func.parameter` を walk して `&mut <compound>` の binding leaves を collect (`flatten_struct_locals` / `flatten_tuple_element_locals`)、`Binding::RefScalar` (scalar Ref) と self は skip。`writeback_leaves` から types を再構築して `self_writeback_types` を上書き (declaration-time の値と一致 — body 側が source of truth)。**(3) call site** (`call.rs::lower_call` + 新 `collect_compound_writeback_dests`): callee の `self_writeback_types.is_empty()` でなければ args を walk し `&mut <compound-var>` arg を検出 → caller 側 binding leaves を flatten して `self_dests` に追加。`InstKind::CallWithSelfWriteback` を emit (既存 method 用 InstKind を再利用)、ret_dest 経由で user-return 値を取得。**method calls / let-rhs calls はまだ未対応** — free function の expression-position call のみ wire 済み (実用に十分、let-binding やメソッド経由はフォローアップ)。**test** (`compiler/tests/consistency.rs::ref_stage2_compound_mut_ref_propagates_round_trip`): `shift_x(&mut p, 30u64)` を 2 回累積 (= 10 + 30 + 2 = 42)、`p.y` 不変を pin。3-way `assert_consistent` で全 backend pass。1163 → **1164 tests pass**。**残**: tuple compound `&mut T` (`fn shift(t: &mut (u64, u64))` — interpreter で `t.0 = ...` の tuple element 代入が `bad assignment due to lhs is not identifier or array access` で reject される既存 limitation)、enum compound `&mut T` (writeback 経路を enum tag/payload まで拡張)、let-rhs / method-call で同 wireback 経路を使えるように。

REF-Stage-2-array-index-borrow. **REF-Stage-2 (iii-index) — array element mutable borrow `&mut arr[i]`** (2026-05-04): nested-chain (`1ab7ff7`) の続き。配列要素を target にした borrow を 3 backend で対応。**type checker** (`frontend/src/type_checker/expression.rs::find_borrow_lvalue_root`): `Expr::SliceAccess(obj, info)` arm を追加 — `SliceType::SingleElement` のみ受理 (range slice は reject)、obj に対し再帰。**IR** (`compiler/src/ir.rs`): 新 variant `InstKind::ArrayElemAddr { slot: ArraySlotId, index: ValueId, elem_ty: Type }` (`Type::U64` 返す)。Display も追加。**codegen** (`compiler/src/codegen.rs`): `ArrayElemAddr` arm — `stack_addr(I64, slot, 0)` でベース取得 → `imul_imm(idx, stride)` でオフセット → `iadd(base, off)` で要素アドレス。`elem_stride_bytes` は `ArraySlotInfo` から取得 (NUM-W-AOT-pack で per-element stride に対応済み)。**AOT lowering** (`compiler/src/lower/expr.rs::lower_call_args`): `Unary(BorrowMut, SliceAccess(arr, info))` を検出、`info.slice_type == SingleElement` で arr が `Binding::Array { element_ty, slot, .. }` かつ scalar element_ty なら、`info.start` の index 式を lower し `ArrayElemAddr` を emit (本物の pointer)。**interpreter writeback** (`interpreter/src/evaluation/call.rs`): 新 `WritebackTarget::ArrayElement { obj: RcObject, index: usize }` variant。`classify_writeback_target` の `Expr::SliceAccess` arm — info から index 式と obj を取り出し、両方 call 前に評価 (副作用は call site で 1 回だけ実行)、index 値を `usize` に変換 (8 narrow + 64bit int variants 全部 cover)。`apply_writeback` の `ArrayElement` arm — `borrow_mut + Object::Array(elements) + bounds check + elements[index] = new_value` で in-place 上書き、`StructField` / `TupleElement` と完全に対称。**JIT**: silent fallback で interpreter 経路に流れて自動対応。**test** (`compiler/tests/consistency.rs::ref_stage2_array_index_mut_borrow_round_trip`): `add_in_place(&mut arr[1u64], delta)` を 2 回累積 (= 20 + 20 + 2 = 42)、index 0/2 不変を pin。3-way `assert_consistent` で全 backend pass。1162 → **1163 tests pass**。**残**: (ii) compound 型 `&mut T` true pointer-passing (現在 compound `&mut T` parameter は leaf-flatten erasure なので caller binding への mutation 伝播は本物の pointer 経由ではなく、interp の Rc cell 共有経由のみ)。

REF-Stage-2-nested-chain-borrow. **REF-Stage-2 (iii-deep) — nested field / tuple chain `&mut a.b.c` 等** (2026-05-04): tuple writeback (`28ccd97`) の続き。`&mut <chain>` の operand に任意の depth の field-/tuple-access chain (例: `&mut o.inner.value`、`&mut p.a.0`、`&mut t.0.x`) を allow。type checker の `find_borrow_lvalue_root` は既に再帰的に walk していたので変更不要。interpreter writeback も `evaluate(<chain>.0)` / `evaluate(<chain>.field)` が parent の Rc cell を返すので既存の field/tuple writeback arm で自動対応していた。**修正対象は AOT のみ**。**問題**: `lower_call_args` は `Unary(BorrowMut, FieldAccess)` と `Unary(BorrowMut, TupleAccess)` を別々に handle し、TupleAccess 経路は `resolve_tuple_element_local` を呼んでいたが同 helper は obj が bare identifier のみ受理 — `&mut p.a.0` (= TupleAccess(FieldAccess(p, a), 0)) で fallback して address-of を発行できず、struct field 上の tuple element borrow が segfault する状態だった。**修正** (`compiler/src/lower/expr.rs::lower_call_args`): FieldAccess / TupleAccess の 2 個別 arm を 1 つに統合し、`resolve_field_chain(inner)` (struct + tuple chain を再帰 walk) を呼んで `FieldChainResult::Scalar { local, ty }` が返れば address-taken 化 + AddressOf 発行。これで mixed chain (struct→tuple→scalar / tuple→struct→scalar / 任意 depth) すべて対応。**test** (`compiler/tests/consistency.rs::ref_stage2_nested_chain_mut_borrow_round_trip`): 1 program 内で 3 種の chain を exercise — `o.inner.value` (struct→struct→scalar)、`p.a.0` (struct→tuple→scalar)、`t.0.x` (tuple→struct→scalar)、各々で sentinel field 不変 + leaf mutation を検証。3-way `assert_consistent` で全 backend pass。1161 → **1162 tests pass**。**残**: index borrow `&mut arr[i]`、(ii) compound 型 `&mut T` true pointer-passing。

REF-Stage-2-tuple-borrow. **REF-Stage-2 (iii-tuple) — interpreter `&mut t.0` writeback** (2026-05-04): field-borrow (`1cad540`) の続き。tuple element borrow の AOT 経路は struct と同形 (`resolve_tuple_element_local` 経由 + AddressOf) で既に動いていたが、interpreter 側だけ writeback 未実装だった。**`WritebackTarget` 拡張** (`interpreter/src/evaluation/call.rs`): 新 variant `TupleElement { obj: RcObject, index: usize }`。**`classify_writeback_target`**: `Expr::TupleAccess(obj, index)` arm を追加 — parent tuple を call 前に評価して Rc を capture (`Object::Tuple` cell)。**`apply_writeback`**: `TupleElement` arm で `borrow_mut` → `Object::Tuple(elements)` を取り出し → `elements[index] = new_value.clone_to_rc()` で in-place 上書き。bounds check 付き (`IndexOutOfBounds`)。struct field 経路と semantically symmetric — 両方とも parent の Rc cell を借りてその中の slot を書き換える。**test** (`compiler/tests/consistency.rs::ref_stage2_tuple_mut_borrow_propagates_round_trip`): `add_in_place(&mut t.0, 30u64)` を 2 回累積 (= 10 + 30 + 2 = 42)、`t.1` unchanged を pin。3-way `assert_consistent` で全 backend pass。1160 → **1161 tests pass**。**残**: index borrow `&mut arr[i]`、deeper field chain (`&mut a.b.c` — 現在 1 level)、(ii) compound 型 `&mut T` true pointer-passing。

REF-Stage-2-field-borrow. **REF-Stage-2 (iii) — field-level mutable borrow `&mut s.field`** (2026-05-04): scalar minimal + interp writeback (`906f597`) の続き。`&mut p.x` 形式で struct field をターゲットにした borrow を 3 backend 全部で動かす。**type checker** (`frontend/src/type_checker/expression.rs`): 新 helper `find_borrow_lvalue_root(operand) -> Result<DefaultSymbol>` を追加 — `Identifier` / `FieldAccess(obj, _)` / `TupleAccess(obj, _)` を再帰的に walk して root binding name を返す。`visit_unary` の BorrowMut 分岐で root を取り出して `is_var_mutable` で mutable check (チェイン全体は root の mutability に従う)。**AOT lowering** (`compiler/src/lower/expr.rs::lower_call_args`): `Unary(BorrowMut, FieldAccess(obj, field))` を検出した場合 `resolve_field_chain` 経由で leaf scalar local を取り出し、`address_taken_locals` に登録 + `AddressOf` を emit。`TupleAccess` も同様 (`resolve_tuple_element_local`)。**interpreter writeback** (`interpreter/src/evaluation/call.rs`): writeback target を `Vec<Option<DefaultSymbol>>` から enum `WritebackTarget { None, Name(sym), StructField { obj: RcObject, field } }` に拡張。call site で `&mut <Identifier>` 以外に `&mut <FieldAccess>` も検出して、parent struct を pre-call で評価し `Rc<RefCell<Object::Struct>>` を capture (call 中の mutation で Rc が同じ cell を指すので safe)。post-call で `apply_writeback` が target enum に応じて `set_var` (Identifier 経路) または `borrow_mut + fields.insert` (StructField 経路) を実行 — 後者は user-code `obj.field = x` と同じ Object 操作 path を踏む。**新 helper**: `classify_writeback_target(operand) -> Result<WritebackTarget>` と `apply_writeback(&target, value) -> Result<()>` の 2 関数。**JIT**: `&mut T` parameter 関数は eligibility が現状 silent fallback で interpreter 経路に流れるので自動的に新 writeback パスを通過。**test** (`compiler/tests/consistency.rs::ref_stage2_field_mut_borrow_propagates_round_trip`): `add_in_place(&mut p.x, 30u64)` を 2 回累積で呼び (= 10 + 30 + 2 = 42)、unrelated field y が unchanged であることも 1 行で pin。3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend pass。1159 → **1160 tests pass**。**残**: tuple element の writeback (`&mut t.0` の interpreter 側 — 現状 AOT は動くが interpreter が tuple parent を struct cell として扱えないため writeback target 化していない)、index borrow (`&mut arr[i]`)、deeper field chain (`&mut a.b.c` — 現状 1 level)、(ii) compound 型 `&mut T` の真の pointer-passing。

REF-Stage-2-interp-writeback. **REF-Stage-2 (i) — interpreter / JIT で `&mut T` mutation 伝播 + 3-way consistency 化** (2026-05-04): scalar minimal AOT 完了 (`f95a48c`) の続き。interpreter は by-value semantics なので AOT のような pointer-passing は使えない代わりに、**post-call writeback** convention を導入。**設計**: (1) call site で `Unary(BorrowMut, Identifier(name))` arg を pre-scan し、param 位置と caller side の identifier symbol を pair で記録 (`writeback_targets: Vec<Option<DefaultSymbol>>`)、(2) function body 実行後、`exit_block` で関数 scope を破棄する**前**に各 `&mut T` param の post-body 値を snapshot (`writebacks: Vec<Option<Value>>`)、(3) call site が writeback_targets と writebacks を zip し、各 (caller_name, modified_value) を `set_var(..., Overwrite)` で caller binding に書き戻し。**implementation**: `interpreter/src/evaluation/call.rs` に新 `evaluate_function_with_values_writeback(function, args) -> (Value, Vec<Option<Value>>)` を追加 (param position ごとに `&mut T` なら post-body value、それ以外は `None`)。既存 `evaluate_function_with_values` は新 form を呼び writebacks を捨てる薄い wrapper に。`evaluate_function_call` の args ループで `Expr::Unary(BorrowMut, Identifier(sym))` を `expr_pool` から peek し `writeback_targets` を構築、call 完了後に zip で writeback 適用 (`environment.set_var` w/ `VariableSetType::Overwrite`)。**JIT**: `INTERPRETER_JIT=1` 経由の path は `&mut T` param を含む関数に対する eligibility が現状 silent fallback になり、interpreter 経路に流れ込むので自動的に writeback パスを通過 — 別途修正不要。**test 移動 + 3-way 化**: `compiler/tests/e2e.rs::ref_stage2_scalar_mut_ref_propagates_mutation` (AOT-only) を削除し、`compiler/tests/consistency.rs::ref_stage2_scalar_mut_ref_propagates_mutation_round_trip` (3-way `assert_consistent`) として移植。test source も 2 回連続 `inc(&mut n)` + `n - 1u64` で「同じ binding に対して複数回 mutation が累積する」ことまで pin (= 41 + 1 + 1 - 1 = 42)。1159 tests pass (test 1 件入れ替えで count 不変)。**残**: (ii) compound 型 `&mut T` の真の pointer-passing (struct field 単位 access + アドレス演算)、(iii) lvalue 拡張 (`&mut s.field` / `&mut arr[i]`)、(iv) `&T` (immut) も RefScalar 経路活用 (現状は読み取り auto-deref のまま、struct 等の copy 削減未活用)。

REF-Stage-2-scalar-mut-pointer. **REF-Stage-2 (b)+(c)+(g) scalar minimal — AOT で真の mutation 伝播** (2026-05-04): スカラ `&mut T` parameter を pointer-passing で実装し、AOT で caller binding への mutation 伝播を実現 (大物 Stage 2 残り phase の最小 viable サブセット)。詳細は上の "REF-Stage-2" entry を参照。3 つの IR variant (AddressOf / LoadRef / StoreRef) + `Function.address_taken_locals` + codegen の stack slot dispatch + `Binding::RefScalar` + frontend 側の auto-dereference + `&mut T` param を mutable binding 化、を全 layer に landing。test `ref_stage2_scalar_mut_ref_propagates_mutation` は AOT-only (interpreter / JIT mutation propagation は未実装)。1158 → **1159 tests pass**。**残作業の方向性**: (i) interpreter / JIT mutation propagation で 3-way consistency を取る (Rc<RefCell<Value>> 経由 shared cell が clean、または関数 exit writeback が小さい)、(ii) compound 型 `&mut T` の真の pointer-passing (struct field 単位 access + アドレス演算)、(iii) lvalue 拡張 (`&mut s.field` / `&mut arr[i]`)、(iv) `&T` (immutable) も RefScalar 経路を活用 (現状は読み取り auto-deref + Borrow による pass-through 経路のまま、struct 等の copy 削減は未活用)。

TEST-PERF-lazy-core. **テストスイート高速化: dependencies opt-level=3 + lazy core auto-load + discover cache** (2026-05-04): `cargo nextest run --workspace` の wall-clock を **42s → 29s (~30% 短縮)**、内部の重い batched test は **35s → 3.3s (~10x)**。3 つの独立した最適化を適用。**(1) workspace dev/test profile で deps を最適化** (`Cargo.toml`): `[profile.dev.package."*"] opt-level = 3` + `[profile.test.package."*"] opt-level = 3`。Cranelift など外部 crate を debug build でも release 相当で動かす — 最初のビルドは長い (~1.5 min) が以降は cached。workspace-local crates (frontend / interpreter / compiler / compiler_core) は opt-level=0 のままなので iteration 速度・debugger ergonomics は無変化。**(2) `discover_core_modules` のキャッシュ** (`interpreter/src/module_integration.rs`): `OnceLock<Mutex<HashMap<canonical_path, Vec<DiscoveredCoreModule>>>>` で per-process cache。最初の filesystem walk + 11 ファイル read 後は HashMap 引きだけで返る。1 回あたり ~5ms 削減 × 数百テスト。**(3) lazy core auto-load (本命)**: 簡単な user-only テストでは `core_modules_dir = None` で先に試し、失敗時のみ full auto-load にフォールバック。core 自動 load は debug build で 1 テストあたり ~150ms (parse + integrate + type-check 11 stdlib モジュール) のコスト。**`compiler/tests/e2e_batched.rs::compile_to_jit_lazy_core`**: 3 batched テスト (`batched_e2e_extracted_from_file` / `batched_stdout_extracted_from_file` / もう 1 つ) 全部に適用 — 110 + 49 sub-test の大半が pure user code で、no-core fast path を hit。**`compiler/tests/consistency.rs::assert_consistent`**: in-process interpreter + AOT compile を no-core で先に試し、両方 OK なら JIT spawn も `TOYLANG_CORE_MODULES=""` 付きで spawn。3 path 全部一致したら fast-path で完了。一致しない / どこかで失敗したら canonical full path にフォールバック (visible semantics 不変)。**fast-path 安全網**: AOT compile が no-core で fail した場合 (例: stdlib `Length` trait 経由の `s.len()` を AOT が使うが interpreter は builtin で受ける場合) のために `try_compiler_exit_code -> Option<i32>` を追加し panic させない。**`module_integration` を pub mod に変更** (`interpreter/src/lib.rs`): example / 外部からの `discover_core_modules` 直接呼び出しを許可。**新 example** (`compiler/examples/profile_jit.rs`): per-phase wall-clock breakdown ツール (parse / check_typing+core / lower+JIT) — lazy-core fast-path のベースラインも合わせて表示。debug build で full = 316ms、no-core = 0.7ms と判明 (= 99.8% を core load + integration + type-check が占めていた)。**結果**: 1158 tests pass, 31 skipped、全 backend で regression なし。test suite wall-clock: **42s → 29s** (-31%)。`batched_e2e_extracted_from_file` の compile 時間: **20.6s → 3.2s** (-84%)。`batched_stdout_extracted_from_file` の compile 時間: **12s → 0.06s** (-99%)。

REF-Stage-2-escape-reject. **REF-Stage-2 (e) — 構文的 escape reject (minimal borrow checker)** (2026-05-04): REF-Stage-2 (a)+(d)+(f) の続き。ユーザ確定の制約「lifetime なし / aliasing ルールなし / field-level borrow なし / iterator invalidation なし / escape 解析は簡単なら入れる」に基づき、references が referent の frame を脱出する 3 つの構文パターンだけを type checker レベルで reject。**helper** (`frontend/src/type_decl.rs`): `TypeDecl::contains_ref(&self) -> bool` を追加 — Ref / Array / Dict / Tuple / Struct / Enum / Range を再帰的に walk し、leaf に `Ref { .. }` があれば true。**check site 1: 関数戻り型** (`type_checker/visitor.rs::type_check`): `func.return_type.as_ref()` が `contains_ref()` true なら "function `<name>` declares a reference type in its return position; references cannot escape their referent's frame (REF-Stage-2 (e))" でエラー。extern fn の早期 return / body lowering より先に check するので extern fn にも適用される。**check site 2: val / var binding** (`type_checker/statement.rs::visit_val_impl` + `visitor.rs::process_val_type_with_mut`): annotation の `contains_ref()` チェックを expression 評価前に、inferred final_type の `contains_ref()` チェックを後に — 注釈無し / 注釈あり両方をカバー。"binding `<name>` annotates / is inferred to a reference type; references cannot be stored in val / var bindings"。**check site 3: struct field** (`type_checker/struct_literal.rs::visit_struct_decl_impl`): 各 field の `type_decl.contains_ref()` を field type allowlist match の前に check し、true なら "struct field `<name>` declares a reference type; references cannot be stored in struct fields" でエラー。generic_scope の cleanup を忘れないように early-return も match arm と同様に対応。**method 引数 `&T` と receiver `&self` / `&mut self` は引き続き許可** — function parameter リスト / method receiver position は escape の対象外 (caller frame 由来で死なない)。**test** (`interpreter/tests/trait_tests.rs::errors`): `test_function_returning_ref_is_rejected` (`fn dangling(x: &u64) -> &u64`)、`test_val_binding_of_ref_is_rejected` (`val r: &u64 = &a`)、`test_struct_field_of_ref_is_rejected` (`struct Bad { r: &u64 }`) の 3 件で 3 escape パスを pin。1155 → **1158 tests pass**。**残**: (b)/(c)/(g) IR 真の参照渡し + 真の mutation 伝播 (大物、複数 commit に分かれる予定)。

REF-Stage-2-mut-binding-check. **REF-Stage-2 (f) — `val` からの `&mut` borrow + `T → &mut T` auto-borrow を reject** (2026-05-04): REF-Stage-2 (a)+(d) の続き。`&mut T` 引数型を導入したことで「どの binding が mutable か」を追跡する必要が出てきたので、`val` / `var` の区別を type checker に通す。**設計指針** (Rust に倣う): (1) `&mut <name>` は `name` が `var` 宣言された場合のみ受理、(2) `T → &mut T` の auto-borrow は許可せず call site で必ず `&mut <var>` を明示。**型 checker** (`frontend/src/type_checker/context.rs`): `VarState` に `is_mut: bool` を追加、`set_var` を immutable (`is_mut: false`) のままにし、既存 `set_mutable_var` を mutable (`is_mut: true`) に切替。新 helper `TypeCheckContext::is_var_mutable(name) -> Option<bool>` (None = scope に無い、`Some(false)` = val、`Some(true)` = var)。**process_val_type 拡張** (`frontend/src/type_checker/visitor.rs`): 新 `process_val_type_with_mut(name, type_decl, expr, is_mut)` を追加し、is_mut で `set_mutable_var` / `set_var` を切替。既存 `process_val_type` は backwards-compat で `is_mut=false` を渡す (for-loop iterator は immutable)。`visit_var_impl` (`statement.rs`) を `process_val_type_with_mut(..., true)` に変更 — `var` 宣言が mutable binding を作る経路。**`visit_unary`** (`expression.rs`): Borrow / BorrowMut を Number coercion logic より前に short-circuit する既存パスに、BorrowMut のときだけ追加チェック — operand が `Expr::Identifier(sym)` でなければ "cannot take a mutable borrow of a non-place expression" でエラー、bare identifier でも `is_var_mutable(sym)` が `Some(false)` なら "cannot borrow `<name>` as mutable: binding is not declared `var`"、`None` なら "not a mutable local binding" を返す。**`is_arg_compatible`** (`type_decl.rs`): 旧 `_, Ref { inner, .. }` arm を `_, Ref { is_mut: false, inner }` に narrow。これで `T → &mut T` の auto-borrow が型システムレベルで reject される。**interpreter runtime arg-check** (`evaluation/call.rs`): references 既に erase されているので expected を `deref_ref` で peel してから `is_equivalent` 比較。これで `&mut s` で渡された runtime value (= bare struct) が `&mut T` parameter にちゃんと一致する。**既存 test 更新**: `ref_stage2_explicit_borrow_and_mut_ref_round_trip` から auto-borrow into `&mut T` の line を削除 (新仕様で reject されるため)。**新 test** (`interpreter/tests/trait_tests.rs::errors`): `test_mut_borrow_of_val_binding_is_rejected` (`val a` を `&mut a` で借りる → "cannot borrow `a` as mutable") + `test_auto_borrow_into_mut_ref_is_rejected` (`var a` を bare で `&mut T` 引数に渡す → arg type mismatch) の 2 件で両 reject パスを pin。1153 → **1155 tests pass**。**残**: (b)/(c)/(g) IR 真の参照渡し + 真の mutation 伝播、(e) borrow checker / lifetime 議論、`&mut <expr>` の lvalue 拡張 (現状 bare identifier のみ; field access / index access も allow するか検討)。

REF-Stage-2-explicit+mut. **REF-Stage-2 (a) `TypeDecl::Ref { is_mut, inner }` + (d) explicit `&value` / `&mut value` + `&mut T` 引数型** (2026-05-04): REF-Stage-2-min (`&T` 引数型 + auto-borrow) の続き。Stage 2 大物の中で「IR 真の参照渡し」「borrow checker」「`&mut` mutation 伝播」を意図的に scope 外に置き、(a) 型システムへの mut 拡張 + (d) caller-side explicit borrow 構文を一気に landing。**型システム** (`frontend/src/type_decl.rs`): `Ref(Box<TypeDecl>)` を `Ref { is_mut: bool, inner: Box<TypeDecl> }` に変更 (9 call site 全部更新)。`is_arg_compatible` 拡張 — `&mut T` actual を `&T` expected にダウングレード可、`&T → &mut T` は reject。同じ mutability の `Ref → Ref` は inner 同士の equivalence。auto-borrow (`T → &T` / `T → &mut T`) は維持。`format_type_for_error` も `&mut T` を出力。**parser** (`frontend/src/parser/types.rs`): `& [mut]? T` を type 位置で parse。`Some(Kind::And)` arm で `Kind::Mut` を peek consume → `Ref { is_mut, inner }` 構築。**parser expression** (`frontend/src/parser/expr.rs::parse_unary`): prefix `&` / `&mut` を新たに追加。`&` の binary (bitwise AND) は `parse_bitwise_and` 経由で primary の後に reach されるので、parse_unary 冒頭での `Kind::And` arm との曖昧性なし。**AST** (`frontend/src/ast/expr.rs`): `UnaryOp::Borrow` / `UnaryOp::BorrowMut` を追加。新 variant のため visitor / pool 拡張不要 (既存 Unary パスを再利用)。**type checker** (`frontend/src/type_checker/expression.rs::visit_unary`): Borrow/BorrowMut は Number coercion logic より前に short-circuit、operand_ty を `Ref { is_mut, inner }` で wrap して return。operand 自身が既に `Ref` ならその inner を取り出し double-borrow を回避。**interpreter** (`evaluation/operators.rs`): borrow ops は erasure (operand_v をそのまま return)。`evaluation/call.rs` の runtime arg 型チェックを `is_equivalent` から `TypeDecl::is_arg_compatible` に変更 — `T` の runtime value を `&T` parameter に渡す auto-borrow が runtime check を通るように。**JIT** (`jit/eligibility.rs` + `jit/codegen.rs`): borrow ops は pass-through (operand 型をそのまま forward、operand value をそのまま使う)。**AOT** (`compiler/src/lower/expr_ops.rs::lower_unary`): borrow ops を最初に detect して `lower_expr(operand)` に委譲 (erasure)。`compiler/src/lower/expr.rs::lower_call_args`: borrow 式を peel して inner を取り出し、既存の struct identifier expansion パス (`Binding::Struct/Tuple/Enum` の per-leaf load) に合流させる。これで `len_of(&s)` / `first_byte_mut(&mut s)` のように compound 値の borrow を arg として渡せる。**erasure 設計の含意**: `&mut T` 引数型は型システムレベルで認識されるが、AOT では IR レベルで erase されるため caller の binding に対する mutation 伝播は発生しない (Stage 1 の `&mut self` は別途 Self-out-parameter convention で writeback する)。これは将来 phase で IR `Type::Ref` + `AddressOf` を入れたときに自動的に解消する。**test** (`compiler/tests/consistency.rs::ref_stage2_explicit_borrow_and_mut_ref_round_trip`): `len_of(s)` (auto-borrow) / `len_of(&s)` (explicit) / `len_of(&mut s)` (downgrade) / `first_byte_mut(&mut s)` (`&mut T` arg + explicit) / `first_byte_mut(s)` (auto-borrow into `&mut T`) を 3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend pass。1152 → **1153 tests pass**。**残**: (b)/(c) IR 真の参照渡し、(e) borrow checker、(f) `val` からの `&mut` borrow を reject、(g) `&mut T` 引数型からの真の mutation 伝播。

STDLIB-alloc-trait. **`core/std/allocator.t::trait Alloc` + Wrapper Structs (Global/Arena/FixedBuffer)** (2026-05-04): allocator 群を `Vec` / `Dict` / `String` と同じく stdlib (`core/std/allocator.t`) のユーザ空間 toylang コードとして整理。**設計指針**: (1) `trait Alloc` は `alloc(size) -> ptr` / `free(p)` / `realloc(p, new_size) -> ptr` の malloc 系インターフェース、(2) `TypeDecl::Allocator` (parser 段階の primitive 型) は `with allocator = expr` 用の runtime u64 handle 表現として現状維持、(3) trait 名は `Alloc` で primitive `Allocator` と命名衝突回避、(4) ユーザは `arena.alloc(8u64)` (trait method dispatch) と `with allocator = arena { __builtin_heap_alloc(8u64) }` (active stack 経由 + struct 直接受理) の両方を選べる。**Phase A** (`core/std/allocator.t` 新設): `pub trait Alloc { fn alloc / free / realloc }` + `pub struct Global / Arena / FixedBuffer` (各々 `h: Allocator` field、`new()` constructor、`Alloc` impl)。Arena は `drop(&mut self)` を持ち `__builtin_arena_drop(self.h)` 委譲。各 trait method body は `with allocator = self.h { __builtin_heap_alloc(size) }` で active stack 経由 routing。**Phase B/C — `with allocator = <struct>` の auto-extract**: 型 checker `visit_with` (`frontend/src/type_checker/visitor_impl.rs`) に新 accept パスを追加 — expr の型が struct で trait `Alloc` を impl + `TypeDecl::Allocator` 型 field がちょうど 1 つあるなら受理。lowering `Expr::With` (`compiler/src/lower/expr.rs`) は `resolve_field_chain` で struct binding を引き、frontend の `StructTemplate.fields` から `TypeDecl::Allocator` field 名を特定 (IR 上は U64 で他の u64 field と区別不能なため frontend type 経由)、対応する LocalId の LoadLocal を emit して handle 取得。interpreter (`evaluation/expression.rs::Expr::With`) も同形 auto-extract — `Object::Struct.fields` から `Object::Allocator` value を 1 つ拾って push。**test** (`compiler/tests/consistency.rs`): (1) `stdlib_alloc_with_struct` で `with allocator = arena {}` / `with allocator = fb {}` の auto-extract path を 3-way pin、(2) `stdlib_alloc_trait_methods` で `arena.alloc(...)` / `fb.alloc(...)` の trait dispatch を 3-way pin。両方 interpreter / JIT silent fallback / AOT 全 backend で exit 42 一致。1150 → **1152 tests pass**。**残**: stdlib 内部 (Vec / Dict / String) を新 wrapper API 経由に書き換え、`<T: Alloc>` trait bound の正式サポート、inline arena auto-drop。priority 低。

121-Phase-B-rest-arena-drop. **explicit `__builtin_arena_drop(handle)` builtin** (2026-05-04): Phase B-rest Item 2 cleanup の続き。arena slot の bulk-free を user-callable builtin として追加。3 backend で動作。**frontend** (`frontend/src/ast/expr.rs`): `BuiltinFunction::ArenaDrop` variant + symbol mapping (`__builtin_arena_drop`)。**runtime** (`compiler/runtime/toylang_rt.c::toy_arena_drop`): handle の slot を lookup、kind が arena なら全 tracked addr を free、addrs/sizes/cap_array を zero clear。default 0 / fixed_buffer は no-op (caller-held pointers の silent invalidation を避ける defensive 設計)。**Allocator trait** (`interpreter/src/heap.rs`): `fn reset(&self) {}` を default no-op で trait に追加、`ArenaAllocator` が override して既存 `ArenaAllocator::reset()` (前から存在) に委譲。**interpreter dispatch** (`interpreter/src/evaluation/builtin.rs`): `BuiltinFunction::ArenaDrop` arm — arg を Allocator に downcast → `rc.reset()` 呼出。**AOT codegen** (`compiler/src/codegen.rs`): 新 `toy_arena_drop` helper signature 宣言 + `RuntimeRefs::arena_drop` FuncRef + `InstKind::AllocArenaDrop { handle }` arm で `call(arena_drop, [h])`。**JIT mirror** (`compiler/src/jit.rs`): `JIT_ALLOC_REGISTRY` 上の同形動作、`jit_builder.symbol("toy_arena_drop", ...)` 登録。**JIT eligibility** (`interpreter/src/jit/eligibility.rs`): JIT は ArenaDrop を precise diagnostic で reject (interpreter fallback)。codegen 側にも未実行 arm を defensive エラーで追加。**lower** (`compiler/src/lower/expr.rs`): 新 dispatch arm `AllocArenaDrop { handle }` を emit。**test** (`compiler/tests/consistency.rs::aot_arena_drop_releases_and_reuses`): arena alloc → scope exit → explicit drop → 同 handle で再 alloc → drop の lifecycle を 3-way `assert_consistent` で pin。1149 → **1150 tests pass**。**残**: `with allocator = __builtin_arena_allocator() { ... }` 形の inline arena に対する自動 drop (現状は user が明示 drop 必要)、AllocatorBinding::Generic/Local/Ambient の細かい配線 (perf 最適化のみで観察可能な振る舞い変化なし、IR 上の placeholder)、`__builtin_default_allocator()` の戻り型 API 整理。priority 低。

121-Phase-B-rest-Item-2-cleanup. **`with allocator = ...` body の早期 exit cleanup** (2026-05-04): Phase B-rest Item 1+3 の続き。`with` body 内で `return` / `break` / `continue` した場合に runtime allocator stack が pop されず leak していた問題を解消。**FunctionLower 拡張** (`compiler/src/lower/mod.rs`): (1) 新フィールド `with_scope_depth: usize` で現在開いている `with` scope 数をトラック、(2) `loop_stack` を `Vec<(BlockId, BlockId)>` から `Vec<(BlockId, BlockId, usize)>` に拡張 — 第 3 要素は loop entry 時の `with_scope_depth` snapshot、(3) 新ヘルパ `emit_with_scope_cleanup(target_depth)` が `current_block` 生存中なら `(current depth - target_depth)` 個の `AllocPop` を emit。**`Expr::With` lowering** (`lower/expr.rs`): push 後 `with_scope_depth += 1`、body lowering、`is_unreachable()` でない場合のみ pop emit (early-exit 経路では `terminate_return` / `break` / `continue` 側で先に pop 済みなので duplicate を避ける)、最後に `with_scope_depth -= 1`。**`terminate_return`** (`lower/program.rs`): self-writeback の前に `emit_with_scope_cleanup(0)` で全 active scope を unwind してから `Terminator::Return` 発行。**`Stmt::Break` / `Stmt::Continue`** (`lower/stmt.rs`): `loop_stack` の第 3 要素 (loop entry depth) を取り出し、`emit_with_scope_cleanup(loop_depth)` でループ内で開いた scope だけ pop (外側の `with` は loop の後も生きているので残す)。**新 test** (`compiler/tests/consistency.rs::aot_with_allocator_early_return_pops_stack`): `helper()` 内で `with arena { return 7u64 }`、main で `helper()` 後に `current_allocator() == default_allocator()` を検証。3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend pass。bug fix なし時 → AOT で `cur != def` で exit 1。1148 → **1149 tests pass**。**残**: arena slot の自動 drop (現状 process exit まで registry に残る)、`AllocatorBinding::Generic/Local/Ambient` のより細かい配線。

121-Phase-B-rest-1+3. **arena / fixed_buffer allocator native runtime + heap_alloc dispatch via active stack** (2026-05-04): Phase A (heap/pointer builtins、libc 直接) と Phase B-min (allocator stack push/pop + default/current) の続き。AOT で `__builtin_arena_allocator()` / `__builtin_fixed_buffer_allocator(capacity)` を実装し、`__builtin_heap_alloc / _realloc / _free` が active allocator stack 経由で dispatch するように変更。ユーザ空間で `with allocator = arena { ... }` / `with allocator = fixed_buffer { ... }` が AOT で end-to-end 動作。

**runtime** (`compiler/runtime/toylang_rt.c`): 新規 64-cap fixed registry (`toy_alloc_registry`) + 5 関数: (1) `toy_arena_new() -> u64` (slot index + 1 が handle、0 は default 予約)、(2) `toy_fixed_buffer_new(capacity: u64) -> u64`、(3) `toy_dispatched_alloc(handle, size)` (handle==0 で libc malloc 直接、arena は無制限 + 追跡、fixed_buffer は quota チェック → null return on overflow)、(4) `toy_dispatched_free(handle, p)` (arena は no-op、fixed_buffer は quota 戻し + libc free)、(5) `toy_dispatched_realloc(handle, p, new_size)` (fixed_buffer は projected quota チェック)。

**JIT mirror** (`compiler/src/jit.rs`): `Mutex<Vec<JitAllocSlot>>` 形の registry + 同形 5 関数 (Rust 側で libc malloc/realloc/free を直接呼ぶ)。`jit_builder.symbol("toy_arena_new", ...)` 等 5 個を register。

**IR** (`compiler/src/ir.rs::InstKind`): 3 variant 追加 — `AllocArena` (no args、u64 return)、`AllocFixedBuffer { capacity: ValueId }` (u64 return)、`PtrIsNull { ptr: ValueId }` (bool return、`__builtin_ptr_is_null` の AOT 対応)。Display impl 拡張。

**lower** (`compiler/src/lower/expr.rs::lower_builtin_call`): 旧 reject arm を撤去 → `BuiltinFunction::ArenaAllocator` / `FixedBufferAllocator` / `PtrIsNull` の 3 dispatch arm 追加。`type_inference.rs::value_scalar` も同 builtin の戻り型を peek できるように拡張。

**codegen** (`compiler/src/codegen.rs`): 5 helper signatures + `RuntimeRefs` の FuncRef 5 個追加。**HeapAlloc / HeapRealloc / HeapFree の lowering を変更** — 旧 `call(libc_malloc)` 直接ではなく `let h = call(toy_alloc_current); call(toy_dispatched_alloc, [h, size])` の 2 段呼び出し (`alloc_current_call → dispatched_alloc_call`)。これが Item 3 の本体。`AllocArena` / `AllocFixedBuffer` arm で対応 helper を呼び handle を返す。`PtrIsNull` arm は `icmp_imm(IntCC::Equal, p, 0)`。

**test 置換** (`compiler/tests/consistency.rs`): 旧 `aot_allocator_context_builtin_still_emits_precise_diagnostic` (Phase B-min 時点での diagnostic を pin) を撤去 → 新規 `aot_arena_and_fixed_buffer_allocators_round_trip` (3-way `assert_consistent`) で arena 連続 alloc / fixed_buffer の quota exceeded → null return / `__builtin_ptr_is_null` 経由の null 検査をすべて pin。

**残 (Phase B-rest Item 2 + 残作業)**: (1) `with` body の早期 exit (`return` / `break` / `continue`) で stack を確実に pop する panic-style cleanup (現状 linear exit 限定で reject されている)、(2) `AllocatorBinding::Generic/Local/Ambient` の lower 配線 (現状 active stack 経由の dispatch は heap builtin 命令単位で、より細かい binding 情報は使われていない)、(3) arena の真の `drop` (現状 arena slot は process exit まで生存し registry から消えない、`with` exit 時に自動 reset を入れる選択肢あり)。1147 → **1148 tests pass**。

STRING-eq-clear-push_char. **`String` API 拡張: `eq` / `clear` / `push_char` (+ Vec::clear)** (2026-05-04): REF-Stage-2-min の `&T` 引数型を活かして 3 つの基本 method を追加。**`Vec<T>::clear(&mut self)`** (`core/std/collections/vec.t::impl<T> Vec<T>`): logical clear (`self.len = 0u64`)、cap / data buffer は保持して再利用。**`String::eq(&self, other: &String) -> bool`**: byte-wise 完全一致 — length チェックで早期 false return → `as_ptr` 経由で両 buffer を `__builtin_ptr_read` で 1 byte ずつ比較する while loop。`&self` + `&String` の auto-deref / auto-borrow を一度に exercise。call site は `s.eq(other)` (other: String) で auto-borrow が走る。**`String::clear(&mut self)`**: `self.vec.clear()` 委譲。**`String::push_char(&mut self, c: u8)`**: `self.vec.push(c)` 委譲 (`Vec<u8>::push` の T=u8 経路)。**設計原則** (commit `ea0c0cd` 確立) を踏襲: String 内 method は `self.vec` の field 直接読まずメソッド経由のみ、struct literal 直接構築禁止 (constructor は from_str のみ)。**test** (`compiler/tests/consistency.rs::string_eq_clear_push_char_round_trip`): "hi" + push_char(33u8) → eq("hi!") true / eq("hi?") false / clear → is_empty true → push_char(120u8) → eq("x") true。3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend pass。1147 → **1148 tests pass**。**残**: `String::concat(&self, other: &String) -> String` (compound return + `&self`)、`String` value の println 専用 path (現状 generic struct print 経由で `String { vec: Vec<u8> { ... } }` 形になる)。

REF-stdlib-cleanup. **`core/std/string.t` / `core/std/collections/vec.t` の read-only method を `&self` 化** (2026-05-03): REF-Stage-2-min (上記) で `&` 参照渡しの基盤が landing したのを受けて、stdlib の read-only method の receiver を `self: Self` (by-value、コピー) から `&self` (immutable reference、コピーなし) に置換。**変更**: (1) `string.t::String` の `len` / `is_empty` / `as_ptr` の 3 method、(2) `vec.t::Vec<T>` の `get` / `size` / `capacity` / `is_empty` / `as_ptr` の 5 method — 計 8 method。**効果**: 既存 caller (`s.len()` / `v.size()` 等) は auto-borrow で同じ syntax を維持するが、レシーバの構造体コピーを発生させなくなる (今 phase の AOT lowering ではまだ leaf-flatten のままなので IR 上の差は無いが、将来 phase で IR レベル真の参照渡しに昇格すれば自動的に最適化対象になる foundation)。**mutating method** (`push` / `pop` / `set` / `extend_bytes` / `push_str`) は元々 `&mut self` なのでそのまま。**associated function** (`Vec::new` / `String::from_str`) は self を取らないので無関係。1147 tests pass、regression なし (既存 3-way `assert_consistent` が `s.len()` / `v.size()` 経由で `&self` dispatch を pin)。

REF-Stage-2-min. **`&T` 引数型サポート + `String::push_str(&mut self, other: &String)` demo** (2026-05-03): core modules のメソッド宣言で「引数の型」に `&` を付けて参照渡しを表現できるようにした (REF-Stage-2 最小サブセット)。`&T` と `T` は型システム上**別の型**として扱い (`is_equivalent` は通さない)、call site では auto-borrow で `T` 値を `&T` パラメータに渡せる。runtime / IR は erasure (今 phase では真の pointer 渡しは未実装、leaf-flatten のまま)。`&mut T` / explicit `&value` 式 / borrow checker は意図的に scope 外。

**AST + parser** (`frontend/src/type_decl.rs`、`frontend/src/parser/types.rs`): `TypeDecl::Ref(Box<TypeDecl>)` 追加。`parse_type_declaration_with_generic_context` の冒頭に `Some(Kind::And)` arm を追加 (`&` consume + 再帰的に inner 型 parse → `Ref(Box::new(inner))`)。`&self` / `&mut self` の receiver path (`parser/stmt.rs:636-654`) は別 context なので衝突しない。`substitute_generics` も `Ref(inner)` を再帰対応。`is_arg_compatible(actual, expected) -> bool` ヘルパを `TypeDecl` に追加 (auto-borrow: actual が `T`、expected が `Ref(T)` のとき OK)。`deref_ref(&self) -> &TypeDecl` ヘルパ (method dispatch 経路で `Ref(T) → T` を peel)。

**型 checker**:
- `expression.rs:674`、`method_call.rs:435,511,607` の arg-passing check で `is_equivalent` を `is_arg_compatible` に置換 — auto-borrow が arg-only に閉じる (assignment / 型注釈の equivalence は触らない)。
- `method_call.rs::visit_method_call_impl` で receiver type を `deref_ref` で peel してから既存 enum/struct refinement パスへ。`val s: &String = ...; s.len()` のような receiver も `String` の method に dispatch 可能。
- `method.rs::process_impl_method_validation` の許可型リストに `TypeDecl::Ref(_)` 追加 (param / return 両方)。
- `error_handling.rs::format_type_for_error` に `Ref(inner) → "&{inner}"` フォーマット追加。

**parser** (`program_parser.rs:382-447`): inherent impl path も `parse_type_args_after_lt` を使うように変更 (旧版は `skip_until_matching_gt` で discard)。これで `impl Vec<u8> { ... }` のような concrete-args inherent impl が正しく `target_type_args = [UInt8]` で登録され、CONCRETE-IMPL Phase 2 dispatch にヒットする。

**module integration** (`interpreter/src/module_integration.rs::remap_type_decl`): `TypeDecl::Ref(inner)` arm を追加して inner symbol を再帰 remap (stdlib 内の `&String` が main interner の `String` に正しく解決)。

**AOT compiler erasure**:
- `compiler/src/lower/types.rs::lower_scalar`: 冒頭で `if let TypeDecl::Ref(inner) = ty { return lower_scalar(inner); }` で peel + 再帰。
- `compiler/src/lower/templates.rs::lower_param_or_return_type`: 同形 peel + 再帰。これで `&String` parameter は IR レベルで `String` と同じ leaf-flatten の calling convention になる。

**stdlib**:
- `core/std/collections/vec.t`: 新規 `impl Vec<u8> { fn extend_bytes(&mut self, src: ptr, count: u64) }` (concrete-args impl)。`__builtin_ptr_offset` が無いため byte 単位 loop で `__builtin_ptr_read(src, i) + self.push(b)` 委譲 (geometric grow も既存 `push` 経路に乗る)。`Vec<i64>` 等への error-by-construction は CONCRETE-IMPL Phase 2 で解決済み (registry が target_type_args で disambiguate)。
- `core/std/string.t`: 新規 `String::push_str(&mut self, other: &String)`。body は `self.vec.extend_bytes(other.as_ptr(), other.len())` 一行 (前回コミット `ea0c0cd` で確立した「String 内 method は Vec の field 直接読まずメソッド経由」原則を踏襲)。

**test** (`compiler/tests/consistency.rs::string_push_str_round_trip`): "hello" + " " + "world" を構築 → len=11、first/mid/last byte の 3 点 spot check、3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend 一致。auto-borrow (`s.push_str(sp)` で `sp: String` を `&String` 引数に渡す) を pin。1146 → **1147 tests pass**。

**残 (将来 phase)**: explicit `&value` 式構文、`&mut T` 引数型、IR レベル真の参照渡し (`Type::Ref(StructId)` で leaf-flatten 回避)、borrow checker / lifetime。

STRING-stdlib. **`core/std/string.t::String` heap-managed byte buffer + nested-field method dispatch** (2026-05-03): static `str` を heap 上で操作する `pub struct String { vec: Vec<u8> }` ラッパーを stdlib に追加し、`from_str` を `Vec` の extension trait から `String` のメソッドへ移植。**設計**: (1) `String` は `Vec<u8>` のフィールドのみを持つ private wrapper、(2) `String::from_str(s: str) -> Self` が唯一の constructor (`String::new()` は意図的に省略 — 直接 struct literal 構築不可、from_str 必須)、(3) String 内の method は Vec のフィールドを直接読まず method 経由 (`self.vec.size()` / `self.vec.is_empty()` / `self.vec.as_ptr()`)。**vec.t**: `FromStr` trait + `impl FromStr for Vec<u8>` を撤去 (`String::from_str` に移行)、新規 `Vec::as_ptr(self) -> ptr` getter を追加 (String が data フィールドにアクセスする経路として)。**string.t**: `from_str` 内では Vec literal を inline (`Vec { data, len, cap, elem_size }`) で構築 — 現状 compiler MVP は struct field の rhs に struct literal を要求するため、 `var v: Vec<u8> = Vec::new()` 経由ではなく一発で組み立てる。`String { vec: Vec { ... } }` の trailing local binding (`val result: String = ...; result`) も MVP の bare-identifier return 制約への対応。**compiler MVP 拡張 — nested-field method dispatch** (`compiler/src/lower/method_call.rs`、`bindings.rs`、`field_access.rs`): `lower_method_call` で receiver が `Expr::FieldAccess(...)` の場合、新たに `resolve_field_chain` 経由で synthetic `Binding::Struct { struct_id, fields }` を構築するパスを追加。`FieldChainResult::Struct` に `struct_id: StructId` フィールドを追加 (旧版は `fields: Vec<FieldBinding>` のみ; field_access の 3 親 site と 1 type_inference site を全て更新)。これで `self.vec.size()` のような nested-field method call が compile 可能に。**type checker** (`frontend/src/type_checker/method_call.rs`): `TypeDecl::Identifier(name)` を struct registry が知っている場合 `TypeDecl::Struct(name, vec![])` に refine する arm を追加 (Enum と対称、`val s: String` 注釈経由の method dispatch が動くように)。**test**: 旧 `vec_from_str_round_trip` を `string_from_str_round_trip` に rename + body を `String::from_str("hello")` + `s.len()` + `__builtin_ptr_read(s.as_ptr(), i)` で書き換え。3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend で 'hello' の各 byte と `len()` を確認。1146 tests pass (test 1 件置換で count 不変)。

CONCRETE-IMPL-Phase-2b. **AOT compiler dispatch on concrete type args + Self resolution** (2026-05-03): Phase 2 (interpreter) を AOT compiler にも展開。**method_registry refactor** (`compiler/src/lower/method_registry.rs`): `MethodRegistry` を `HashMap<(target, method), Vec<MethodTemplateSpec>>` に、新規 `MethodFuncIds = HashMap<(target, method), Vec<MethodFuncSpec>>` を追加 (`MethodFuncSpec { target_type_args: Vec<Type>, func_id }`)。新ヘルパ `lookup_method_func` / `lookup_method_template` が 3-tier fallback (exact → empty-args → lone-spec) を提供。`collect_method_decls` の Phase 1 loud-error を撤去、複数 spec を Vec で蓄積。**program.rs declaration loop**: 各 spec ごとに `target_type_args` を `Vec<Type>` に lower、mangled export name を `toy_<target><args_suffix>__<method>` に拡張 (e.g., `toy_Container_Struct(...)__marker_name`)、`MethodFuncSpec` で `Vec` に push。**Self resolution**: `target_type_args_decl` が non-empty なら `TypeDecl::Struct(target_sym, target_type_args_decl)` を Self_decl にする (旧版は `TypeDecl::Identifier(target_sym)` で型引数なし、ジェネリック struct の `self: Self` 引数で "cannot lower" していた問題を解消)。**lookup sites 更新**: `method_call.rs::resolve_method_target` / `lower_method_call`、`let_lowering.rs::lower_let::AssociatedFunctionCall`、`type_inference.rs::value_scalar::MethodCall`、`print.rs::lower_print::MethodCall` の各サイトで `Binding::Struct/Enum` から receiver の `Type::Struct/Enum`.type_args を抽出し `lookup_method_func` 経由で dispatch。primitive receiver (i64.abs() 等) は empty type_args で lookup。`template_self_is_mut` チェックも `lookup_method_template` 経由に統一。**body lowering 順序保持**: 各 (target, method) pair の non-generic specs を `func_specs` (declared FuncId 列) と insertion order で zip して body を lower するように変更 (Phase 1 ではmap.iter() の単一 entry を引いていた)。**generic_methods loop**: pending_method_work の lookup も Vec から first spec を picks する形に (生成 generic は Phase 2c で更に refine)。**test 昇格**: `concrete_impl_dispatch_by_receiver_type_args` を interpreter-only から **3-way `assert_consistent`** に格上げ (interpreter / JIT silent fallback / AOT 全 backend で `Container<u8>::marker_name() + Container<i64>::marker_name() = 8 + 64 = 72`)。1146 tests pass。**残 (Phase 2c)**: (1) annotation hint (`var v: Vec<u8> = Vec::from_str(...)`) を `call_associated_function` の lookup まで thread して lone-spec fallback を狭める、(2) 型 checker `struct_methods` registry を Vec-shape に refactor (現状 same `(struct, method)` で異 args の impl は last-wins、signature が同一なら type-check は通るが厳密化が必要)、(3) generic methods queue で type_args を carry してより精密な template 選択を可能に。

CONCRETE-IMPL-Phase-2-interp. **interpreter dispatch on concrete type args** (2026-05-03): Phase 1 (parser + AST + loud overwrite) の続き。`(struct, method) → Vec<MethodSpec>` 形に registry を refactor し、receiver の `Object::Struct.type_args` / `Object::EnumVariant.type_args` をキーに `impl Foo for Container<u8>` と `impl Foo for Container<i64>` の正しい dispatch を実現。**新型 `MethodSpec`** (`interpreter/src/evaluation/mod.rs`): `target_type_args: Vec<TypeDecl>` + `method: Rc<MethodFunction>`。registry shape は `HashMap<struct_sym, HashMap<method_sym, Vec<MethodSpec>>>`。**新 `register_method`** sig: `(struct, method, target_type_args, method_func)` で同 args の重複は replace、別 args は push。**新 `get_method`** sig: `(struct, method, receiver_type_args)` で 3 段階 fallback — (1) exact match on target_type_args、(2) generic-parameterised impl (empty args)、(3) lone-spec fallback (1 つしかなければ args 関係なく返す、`Vec::from_str()` のような annotation hint 未配線な associated function を救う)。**dispatch sites** (`evaluation/call.rs`): primitive (empty args)、Object::Struct (receiver type_args)、Object::EnumVariant (receiver type_args)、`call_struct_method` (receiver borrow から抽出)、`call_associated_function` (empty + lone-spec fallback で救済)。**lib.rs**: `build_method_registry` の Phase 1 loud-error を削除、`Vec<CollectedMethod>` 形で複数 spec を許容。`register_methods` も Vec を unfold して `register_method` を 1 spec ずつ呼ぶ。**`apply_annotation_type_args` 修正** (`evaluation/statement.rs`): 旧版は `type_args.is_empty()` のときだけ patch していたが、generic struct で field が `T` を含まない場合 (`struct Container<T> { value: u64 }`) は `derive_struct_type_args` が `[Unknown]` を返すため patch が掛からず、val annotation `Container<u8>` の type 情報が runtime に届かず CONCRETE-IMPL dispatch が match しなかった。`is_empty() || all(Unknown)` 条件に拡張して annotation を真の source of truth に。**Phase 1 negative test 置換**: `concrete_impl_conflict_emits_loud_error` (interpreter loud-error) を `concrete_impl_dispatch_by_receiver_type_args` (positive 3-way *interpreter-only* dispatch test、`Container<u8>` + `Container<i64>` で 8 + 64 = 72) に置換。compiler 側 `collect_method_decls` は引き続き loud-error なので 3-way `assert_consistent` ではなく `interpreter_value` で interpreter のみ走らせる。**残 (Phase 2b/2c)**: (1) compiler `MethodRegistry` を同形に refactor + AOT lower の dispatch も type_args ベースに、(2) annotation hint を `call_associated_function` まで thread して lone-spec fallback を狭める、(3) `Self` の return type 解決を concrete args で行う、(4) 型 checker `struct_methods` registry も同形に (現状 same name は last-wins、複数 concrete impl が同じ signature なら type-check は通るが将来的に厳密化が必要)。test: `concrete_impl_dispatch_by_receiver_type_args` で 2 impl 並存 + dispatch を pin。1146 tests pass (Phase 1 と同数、test を置換のため)。

CONCRETE-IMPL-Phase-1. **`impl Trait for Generic<T>` の concrete type args capture + loud overwrite** (2026-05-03): VEC-collection (`a2d1495`) で表面化した silent-overwrite を解消する基盤フェーズ。**parser** (`frontend/src/parser/program_parser.rs:406-437`、新ヘルパ `parse_type_args_after_lt` を `frontend/src/parser/types.rs` に追加): 旧 `skip_until_matching_gt()` を `parse_type_declaration_with_generic_context` 経由の type-args parser に置換。`impl FromStr for Vec<u8>` の `<u8>` を `TypeDecl::UInt8` として正しく capture (generic param 経路は `<T>` を `TypeDecl::Generic` 化する既存 logic を再利用)。**AST** (`frontend/src/ast/expr.rs::Stmt::ImplBlock`、`builder.rs::impl_block_stmt_with_trait`、`pool.rs` の 3 箇所 + 新カラム `impl_target_type_args`): `target_type_args: Vec<TypeDecl>` フィールドを追加、空 Vec が generic-param または inherent impl を表す。**module integration** (`interpreter/src/module_integration.rs:645-685`): impl 統合時に各 TypeDecl を `remap_type_decl` 経由で main interner に再 routing (auto-loaded stdlib 内の `impl FromStr for Vec<u8>` も正しく動作)。**type checker** (`frontend/src/visitor.rs::visit_impl_block`、`frontend/src/type_checker/visitor_impl.rs:75,376` + `impl_block.rs:17-29`): trait method `visit_impl_block(target, target_type_args, methods, trait_name)` シグネチャ拡張。type checker 自体は target_type_args を消費しない (Self 解決の concrete-args 化は Phase 2 の課題)。**Loud overwrite**: 旧 `interpreter/src/lib.rs::build_method_registry` は `(target, method)` 同名衝突を silent overwrite していたのを `Result<_, String>` 化し、`(target, method)` が同じだが `target_type_args` が異なる第 2 impl が出現したら `CONCRETE-IMPL: multiple impl blocks ... different concrete type arguments ...` 形式の precise diagnostic を返す。`compiler/src/lower/method_registry.rs::collect_method_decls` も同形に拡張 (旧 generic duplicate メッセージは同一 args 衝突時のみ)。**残 (Phase 2)**: 真のディスパッチ — `(struct, type_args, method)` キー化と receiver type / annotation hint 経由の lookup、`Self` の return type 解決を concrete args で行う。test: `compiler/tests/consistency.rs::concrete_impl_conflict_emits_loud_error` (2 つの `impl MarkerName for Container<{u8,i64}>` で diagnostic 文言を pin)、既存 `vec_from_str_round_trip` などは regression なし。1145 → 1146 tests pass。

VEC-collection. **`core/std/collections/vec.t` — user-space `Vec<T>`** (`b771b13` `a2d1495`、2026-05-03): 動的配列 stdlib コレクション + str → Vec<u8> 変換を 3 backend で動作。
   - **`b771b13`**: 基本 Vec<T> を新設。`new()` / `push(&mut self)` (geometric grow 4→8→16→...) / `pop(&mut self) -> T` / `get(self, i)` / `set(&mut self, i, v)` / `size()` / `capacity()` / `is_empty()`。`__builtin_heap_alloc/realloc` + `__builtin_ptr_read/write` + `__builtin_sizeof(T)` 経由、`&mut self` Stage 1 + DICT-AOT-NEW Phase B/C + #121 Phase A の組み合わせで成立。`vec_user_space_round_trip` 3-way test。auto-load は `core/std/collections/vec.t` → `std.collections.vec` を nested subdir 走査で discover (既存 `walk_core_dir` が再帰)。
   - **`a2d1495`**: `Vec<u8>::from_str(s) -> Vec<u8>` を memcpy 経路で追加。3 つの基盤拡張: (1) **AOT** — `__builtin_mem_copy` を実装 (libc `memcpy(dest, src, n)`、toylang の `(src, dest, size)` 順を call site で swap)、新 `InstKind::MemCopy` + `libc_memcpy` declare、(2) **Interpreter** — `HeapManager::copy_memory` を typed_slots-aware に拡張 (raw byte copy + typed_slot range copy 両方走らせる、`s.as_ptr()` の typed_slots を dest にも propagate)、(3) **AOT lower** — `Expr::AssociatedFunctionCall` arm が `generic_methods` に加えて `method_func_ids` (non-generic impl 経路) も lookup するように拡張 (`impl FromStr for Vec<u8>` のような concrete-type-args impl が registry に bare `Vec` で登録されるため)。`vec_from_str_round_trip` 3-way test。**既知制限**: parser が `impl Foo for Vec<u8>` の `<u8>` を skip するため registry は bare `Vec` 名で登録 — 同一 Vec へ同名 method を持つ別 impl が複数あると registry overwrite。

STR-LEN-O1. **AOT で `__builtin_str_len` を O(1) 化** (`e2add45`、2026-05-03): str runtime value を byte_start から **u64 len フィールドのアドレス** に変更 (.rodata layout `[bytes][NUL][u64 len LE]` は STR-PTR-LEN のまま)。`__builtin_str_len(s)` は `load.i64(s, 0)` 単発で O(1) (libc strlen を撤去)。`__builtin_str_to_ptr(s)` は `s - 1 - load.i64(s, 0)` で逆算 (= byte_start)、3 IR ops O(1)。`InstKind::ConstStr { message, bytes_len }` に bytes_len を追加 (codegen が `iadd_imm(symbol_addr, bytes_len + 1)` で len フィールドへポイント、interner は lowering 層しか持たないため lower 時に計算)。`Print { value_ty: Str }` も同じ計算で byte_start 出してから `toy_print_str` に渡す。`PrintStr` (literal print) は元から symbol_value 直接使用なので変更なし。`compiler/README.md` に `### str runtime layout` 節を追加 (`7201f83`)。

STR-PTR-LEN. **`str` の `as_ptr` / `len` 拡張 + `.rodata` layout** (`580e93c` `3453822` `93892b2`、2026-05-03): str 値から byte ポインタとバイト長を取り出せるようにした。3 commits で段階的に landing。
   - **`580e93c`**: `__builtin_str_to_ptr(s: str) -> ptr` を 3 backend で実装。AOT は identity (str = pointer-sized handle)、interpreter は heap_alloc + 各 byte を `Object::U8` で typed_slots に書込み + NUL terminator、JIT は eligibility が precise diagnostic で reject (interpreter fallback)。`PtrRead` の type-hint allowlist を narrow ints + Float64 + Allocator まで拡張 (`val a: u8 = __builtin_ptr_read(p, 0u64)` の byte-walk pattern が型チェック通るように)。`str_to_ptr_byte_walk_round_trip` 3-way test。
   - **`3453822`**: `core/std/str.t` 新設 — `trait AsPtr { fn as_ptr(self: Self) -> ptr }` + `impl AsPtr for str` で `s.as_ptr()` extension method を提供 (body は `__builtin_str_to_ptr(self)` 委譲)。AOT compiler の `value_scalar` の primitive-receiver lookup に `Type::Str → "str"` + 6 narrow ints (`u8/u16/u32/i8/i16/i32`) を追加 (これがないと `s.as_ptr()` で型推論失敗)。`str_as_ptr_extension_method_round_trip` 3-way test。
   - **`93892b2`**: AOT の `.rodata` per-literal layout を `[bytes][NUL][u64 len LE]` に拡張 (ユーザー指定形式)。`__builtin_str_len(s) -> u64` builtin を追加 — AOT は libc `strlen(s)` を呼ぶ (末尾 NUL でターミネート)、interpreter は `s.bytes().len()`、JIT は silent fallback。`core/std/str.t::Length::len` extension method (`s.len()`) を追加。`libc_strlen` を `CodegenSession` に declare、`InstKind::StrLen` 追加。`str_len_extension_method_round_trip` 3-way test。**末尾 u64 len** は現状 informational (strlen 呼び出しで O(N) 取得)、将来 O(1) 化のため予約。

121-Phase-B-min. **#121 Phase B-min — `__builtin_default_allocator` / `__builtin_current_allocator` + `with allocator = ...` AOT 対応** (`2bb72d4`、2026-05-03): allocator-context builtin の半分 (default / current) と `with allocator = ...` scope を AOT で動かせるようにした。allocator handle 表現は u64 sentinel (0 = default global allocator、libc 経由)。**runtime** (`runtime/toylang_rt.c`): 64-deep fixed-size active-allocator stack `toy_alloc_stack` + `toy_alloc_push/pop/current` (overflow/underflow は `exit(1)`)。**JIT** (`compiler/src/jit.rs`): `Mutex<Vec<u64>>` で同形 64-cap stack を mirror。**IR** (`compiler/src/ir.rs`): `InstKind::AllocPush { handle }` / `AllocPop` / `AllocCurrent` の 3 variant 追加。**lower**: `TypeDecl::Allocator → Type::U64`、`DefaultAllocator → Const::U64(0)`、`CurrentAllocator → AllocCurrent`、`Expr::With(alloc, body)` → push + lower body + pop (linear exit only — early return / break は将来対応)、`value_scalar` で With の型は body から peek。**codegen**: 3 個の helper signatures + `RuntimeRefs` 拡張。`aot_allocator_default_and_current_round_trip` 3-way test で push/pop の semantics を pin。残: `__builtin_arena_allocator` / `__builtin_fixed_buffer_allocator` の native 実装 (それぞれ独自 free strategy + quota)。1139 → 1140 tests pass。

96残-前半. **deep exhaustiveness check (nested enum patterns)** (`273580c`、2026-05-03): match の網羅性解析を nested EnumVariant payload まで再帰的に walk するよう拡張。`match opt: Option<Option<i64>> { Some(Some(v)) => ..., None => ... }` のように `Some(None)` を忘れたケースをコンパイル時に reject。前は top-level seen_variants だけ見ていて runtime panic ("no matching arm") していた。`pattern_match.rs::check_subpatterns_exhaustiveness` (recursive) を追加、各 top variant について refutable subpattern を持つアームがある場合に payload position ごとにサブパターン coverage を再帰チェック。`Struct(name, args)` 形の generic enum 表現も Enum に正規化して受理。`test_nested_pattern_missing_inner_variant_rejected` で pin。Multi-payload variant の Cartesian 解析 (Pair(A, B) 等) と Tuple sub-pattern は未対応 (false negative のみで false positive 無し)。

MUT-SELF-Stage-1. **`&mut self` (impl-block method receiver) — Stage 1 reference type** (`9d687e4` `c03701d` `e4c5e95`、2026-05-03): impl-block method の receiver 種別として `&mut self` を導入。lifetime / borrow check / 一級型 `&T` は意図的に未対応 (= Stage 1 のみ)。これで DICT-AOT-NEW Phase D を閉じ、`dict_user_space_round_trip` を 3-way `assert_consistent` に昇格できた。

   - **Phase 1a (frontend)** (`9d687e4`): lexer に `Kind::Mut` 予約 (`mut` keyword は事前 grep でゼロ使用確認)、parser の `&self` arm を `& [mut] self` に拡張、`MethodFunction` / `TraitMethodSignature` に `self_is_mut: bool` 追加、trait 適合チェックで receiver kind 完全一致を要求。Interpreter / AOT は flag を見ない (semantically 不変) が、tests + module integration 経由で flag が伝播。
   - **Phase 1b (AOT codegen)** (`c03701d`): Self-out-parameter convention で実装。IR `Function` に `self_writeback_types: Vec<Type>` 追加、新 `InstKind::CallWithSelfWriteback { target, args, ret_dest, ret_ty, self_dests }` で receiver leaf locals に書き戻し。`FunctionLower` に `self_writeback_locals` + `pending_self_writeback_param` 追加、`terminate_return` ヘルパで全 Return site (8 箇所) に self leaves を append、`cranelift_signature_with_writeback` で signature 拡張、`lower_method_body` + `instantiate_generic_method_with_self_type` + 非-generic 登録の 3 箇所で implicit `self` parameter を prepend。frontend type checker (`setup_method_parameter_context`) と interpreter (`call_method` / `call_associated_method`) も implicit `self` の binding を追加。`aot_mut_self_propagates_field_mutation` test (3-way `assert_consistent`) で pin。
   - **Phase 1c (stdlib migration)** (`e4c5e95`): `core/std/dict.t::insert` と `remove` を `&mut self` に migration、read-only method (`get` / `get_or` / `contains_key` / `size`) は `self: Self` のまま。`dict_user_space_round_trip` を hand-rolled interp+JIT 比較から 3-way `assert_consistent` に昇格 — DICT-AOT-NEW Phase D 完全クローズ。

   **未対応 (将来)**: 一般 `&T` (shared 参照含む) を一級型として導入、call-site の `var` / `val` 区別による `&mut self` 拒否、enum receiver の writeback、compound nested field の自動 writeback、`&self` の aliasing 意味付け。

121-Phase-A. **#121 Allocator native codegen Phase A — heap / pointer builtins (default global allocator)** (`9c65757`、2026-05-03): AOT で `__builtin_heap_alloc` / `__builtin_heap_realloc` / `__builtin_heap_free` / `__builtin_ptr_read` / `__builtin_ptr_write` の 5 builtin が動くようになった。**IR**: `InstKind` に `HeapAlloc / HeapRealloc / HeapFree / PtrRead / PtrWrite` の 5 variant 追加 (各 element/value type を抱えて codegen が `load.<cl_ty>` / `store.<cl_ty>` を選択)。**lower**: `lower_scalar` に `TypeDecl::Ptr → Type::U64` 追加、`lower_builtin_call` に 5 builtin の dispatch、`lower_let` で `val name: T = __builtin_ptr_read(p, off)` 形を intercept して annotation の T を elem_ty に注入、`value_scalar` に `HeapAlloc / HeapRealloc → U64` を追加。**codegen**: libc `malloc / realloc / free` 宣言を `CodegenSession::new` で行い `RuntimeRefs` に取り込み、`InstKind::Heap*` arm で `call(self.runtime.malloc, ...)` 等を emit、`InstKind::PtrRead/PtrWrite` arm で `iadd(ptr, offset) + load/store.<elem_ty>` を emit。`aot_heap_alloc_round_trip` (alloc → write × 2 → read × 2 → free、exit 42) と `aot_heap_realloc_grows_buffer` (8 → 24 byte 拡張、pre/post-grow 内容保持確認、exit 600) の 2 つを 3-way `assert_consistent` で pin。残: allocator-context builtin (`arena_allocator` / `fixed_buffer_allocator` / `current_allocator` / `default_allocator` + `with allocator = ...` scope の active-allocator stack push/pop)。1131 → 1133 tests pass。

DICT-AOT-NEW-Phase-C. **per-monomorph generic substitution + `__builtin_sizeof` AOT** (`f4ea5da`、2026-05-03): generic method body 内で `val: K` annotation や `__builtin_sizeof(generic_param)` が monomorphize 後に正しく解決されるようになった。**Subst plumbing**: `PendingMethodInstance` に `subst: Vec<(DefaultSymbol, Type)>` 追加 (Self → `Type::Struct(<recv id>)` も含む)、`instantiate_generic_method_with_self_type` で subst を queue に push、`FunctionLower` に `active_subst` フィールド + `set_active_subst` を追加、`program.rs` で queued body lowering 直前に install。`type_resolution::lower_scalar_with_subst` ヘルパで `Generic(P)` / `Identifier(P)` を `active_subst` から resolve、フォールバック `lower_scalar`。`let_lowering` の PtrRead intercept がこれを利用。**SizeOf AOT**: `lower_builtin_call::SizeOf` arm を追加、`value_scalar` で arg 型 → 1/2/4/8 (interpreter `object_byte_size` と一致) の `Const::U64` を emit。compound 型は precise diagnostic で reject。`aot_dict_contains_key_empty_uses_per_monomorph_subst` test (read-only method 経路で K subst をフルに踏む) を追加。1134 → 1135 tests pass。

DICT-AOT-NEW-Phase-B. **`Dict::new()` AssociatedFunctionCall AOT 対応** (`0665fb6`、2026-05-03): `var d: Dict<i64, u64> = Dict::new()` が AOT で end-to-end コンパイル可能に。**lower**: `lower_let` に `Expr::AssociatedFunctionCall(struct_name, fn_name, args)` の arm を追加。struct_name が generic struct template にある場合、`resolve_struct_instance(struct_name, annotation)` で StructId 解決 → `instantiate_generic_method_with_self_type` で monomorphize (`self_type = Type::Struct(<id>)`、`recv_type_args = [i64, u64]`、`arg_refs = []`) → struct returning なら `allocate_struct_fields` + `CallStruct` で多値受け、scalar returning なら `Call` + `StoreLocal`。dict.t::new の本体は `__builtin_heap_alloc(0u64) × 2` + zero-init field × 4 のみで全部 #121 Phase A の path を踏むので両 commit が end-to-end で噛み合う。`aot_dict_new_associated_function` consistency test を追加。1133 → 1134 tests pass。

NUM-W-AOT-pack. **狭い数値型 AOT の packing + 専用 print helper** (`747146c` `9a85000`、2026-05-03): 2 phase で完了。
   - **Phase 1** (`747146c`): homogeneous scalar element 配列 (`[u8; N]` 等) を実際のスカラサイズで pack。`array_layout::elem_stride_bytes` がスカラ型に応じて 1/2/4/8 を返すよう変更し、コンパウンド要素 (struct/tuple) は従来通り 8-byte per leaf を維持 (`[PackedRgba; N]` の tighter packing は phase 2 として別途検討)。`let_lowering.rs` の ArrayLiteral 要素型 allowlist に narrow int 6 width を追加。`narrow_int_array_packing_round_trip` 3-way consistency test (interpreter / JIT / AOT で全 6 width の `[T; 4]` を読み書き + loop sum) で pin。`[u8; 1024]` で 8KB → 1KB の 8× 削減。
   - **Phase 2** (`9a85000`): 専用 narrow-int print helper を 12 個追加 — `toy_print_{i,u}{8,16,32}` + `toy_println_*`。`compiler/runtime/toylang_rt.c` (printf("%d"/"%u")), `compiler/src/jit.rs` (Rust extern "C" wrappers + `register_runtime_symbols`), `compiler/src/codegen.rs` (12 個の FuncId / FuncRef、`AbiParam::sext()` / `uext()` で C ABI extension contract を honor、`InstKind::Print` arm を per-width call に簡素化)。`stdout_narrow_int_dedicated_helpers` test で `-5i8` / `127i8` / `255u8` / `-1000i16` 等の境界値を 3-way 比較。デバッグ過程で sext/uext flag を初期実装し忘れて `-5` が `251` で出力された問題を発見・修正 (cranelift の AbiParam デフォルトは `ArgumentExtension::None`)。1129 → 1131 tests pass。

DICT-CROSS-MODULE-OPTION. **stdlib 内部から他 stdlib type を参照する際の cross-contamination 修正** (`c711e67`、2026-05-03): user code が `struct Option<T>` を declare した場合に dict.t の `get(key) -> Option<V>` が "Associated function 'Some' not found" でエラーになる問題を解消。**修正方針**: 「shadow → drop」から「shadow → alias」へ転換。`integrate_modules` (lib.rs) が auto-load 時に全 core module の top-level enum/struct decl 名を pre-scan し、user-declared 名と intersect して `shadowed_stdlib_types: HashSet<String>` を作る。`AstIntegrationContext` に新 helper `remap_type_symbol` を追加し、shadow set 内の symbol だけ `__std_<name>` で main interner に再登録する。aliasing は型名が必ず top-level type を指す位置 (`TypeDecl::Identifier/Struct/Enum`、`Pattern::EnumVariant.0`、`StructDecl/EnumDecl/ImplBlock` の name/target、`StructLiteral.0`、`AssociatedFunctionCall.0`、`QualifiedIdentifier` 各 segment) のみに適用し、generic param decl / 関数名 / フィールド名等は plain `remap_symbol` のまま。旧 `existing_*_names` skip path 一式を削除。これで user の `struct Option<T>` と stdlib の `__std_Option<T>` が共存し、dict.t の `Option::Some(v)` は __std_Option に解決、user 側は bare `Option` で自分の struct にアクセス可能。method 呼び出し (`r.is_some()`) は値の actual type 経由で正しい impl に dispatch されるので user は alias 名を意識する必要なし。dict.t header NOTE も更新、`get(key) -> Option<V>` が canonical lookup として復活 (get_or は便利関数として残置)。`dict_get_with_user_option_shadow` regression test (interp + JIT) で pin。1128 → 1129 tests pass。

DICT-RETURN-WHILE. **`return` inside `while` の伝播修正** (`4dfa478`、2026-05-04): 2 箇所の interpreter 修正。(1) `evaluation/statement.rs::evaluate_block` の While arm を For arm と同形に書き直し — handle_while_loop の Return / Break / Continue を `last` に格納するのではなく enclosing block に return。(2) `evaluation/call.rs::call_method` / `call_struct_method` の Return arm が verbatim で `Ok(Return(v))` を返していたが function boundary で Value(v) に変換するように修正 — function 境界を超えて control flow signal を漏らさない。`evaluate_function_with_values` (free function path) は line 897 で既に正しく変換されていた。dict.t の 4 method (insert / get_or / contains_key / remove) を `found` flag + manual `i = self.count` workaround から素直な early-return 形に書き戻し。`return_inside_while_propagates` test (3-way `assert_consistent`) で free-fn + method 両方 pin。1127 → 1128 tests pass。

DICT-TYPED-SLOT-REALLOC. **HeapManager.realloc が typed_slots を migrate する regression test** (`88d9af6`、2026-05-04): 調査の結果、typed_slots migration は `6f60fb0` (2026-04-22) で既に実装済み (`heap.rs::realloc` 行 327-340)。dict.t Phase 2 (`86bb6c2`) は最初の `realloc(null, ...)` = `alloc()` path しか踏まないため、post-grow migration は test カバーされていなかった。`dict_typed_slot_survives_geometric_growth` test (33 entries で cap 4 → 8 → 16 → 32 → 64 を全通過) を追加して migration の regression を pin。

NUM-W-AOT. **狭い数値型 AOT cranelift codegen** (`7b53915`、2026-05-04): NUM-W Phase 5 (`bd2d06c`) で precise diagnostic を pin した状態から、AOT に narrow int の実際の codegen を投入。**IR**: `Type` enum に 6 variant (`I8/U8/I16/U16/I32/U32`) + `Const` enum に同 6 variant + `is_signed()` / `Display` impl 拡張。**lower**: `lower_scalar` / `value_scalar` の TypeDecl→Type マッピング、literal 経路 (`Expr::Int8(n) → Const::I8(n)` 等)、`lower/stmt.rs` の var zero-init、`lower/array_layout.rs` の `leaf_scalar_count` (1 leaf slot ずつ、stride 共用)、`lower/program.rs` の parameter binding (scalar 拡張)、Phase 6 の narrow impl skip 撤去。**codegen**: `ir_to_cranelift_ty` (I8/I16/I32 mapping)、`Const::*` lowering (`iconst` per-width)、`lower_cast` を 13×13 マトリクスから「sign-aware widen + ireduce narrow + fcvt_*_sat」の generic 2-phase 化、`Print` lowering で narrow → wide helper 経由 (sextend/uextend)、`lower/print.rs::format_type_for_display` で narrow type 名を出力。**テスト**: `narrow_int_aot_emits_precise_diagnostic` を `narrow_int_aot_round_trip` に置換 (interpreter / JIT-fallback / AOT 全部 exit 18 = (250+4294966296)&0xff)。1124 → 1124 tests pass。

NUM-W-signed-hash. **`Hash for i8/i16/i32` を byte-level に置換** (`3d40161`、2026-05-04): `self as u64` の sign-extension で `(-5_i8).hash() = 0xFF…FB` (~1.8e19) になる問題を `(self as <unsigned>) as u64` で回避。signed 値の bit pattern が保持される (i8 -5 → 251、i16 -100 → 65436、i32 -1000 → 4294966296)。`narrow_int_hash_dispatch_interpreter` を全 6 width で sum 4295132090 を期待するよう拡張。

NUM-W-1〜6. **狭い数値型 (u8/u16/u32/i8/i16/i32) インタプリタ完全対応** (`208fbdd` `393df3b` `a395cbe` `9c6edbb` `bd2d06c` `03f5243`、2026-05-04): 6 phase を順に実装。
   - **Phase 1** (`208fbdd`、frontend): lexer に 6 keyword + 12 リテラル接尾辞 (decimal + hex 各 6)、`Kind::U8/16/32/I8/16/32` + value-carrying token、`TypeDecl::UInt8/16/32/Int8/16/32`、`parse_type_declaration` の 6 アーム、`Expr::UInt8/16/32/Int8/16/32` + builder、`ExprPool` の `int64_val/uint64_val` を共有しつつ `ExprType` 6 discriminant で width 識別、`AstVisitor::visit_intN_literal` (default impl で TypeDecl を返す)、`visitor_impl::Acceptable for Expr` の 6 dispatch。
   - **Phase 2** (`393df3b`、interpreter): `Object::U8/16/32/I8/16/32` + `Value::U8/16/32/I8/16/32` (inline 高速パス)、PartialEq / Ord / Hash / get_type / to_display_string / `is_primitive_variant` / `From<Object>` / `from_rc` / `into_rc` 全箇所を更新。`evaluate_arithmetic_op_v` / `evaluate_comparison_op_v` に同 width 6 ペア追加、`ArithmeticOp::apply_iN/uN` + `ComparisonOp::apply_iN/uN` 6 width 分実装 (wrapping 算術)。type checker は `expression.rs` の `IAdd/Sub/Mul/Div/Mod` と比較演算に同 width 6 ペア追加 (no implicit widening)。`object_byte_size` で 1/2/4 バイト返却。`evaluate_literal` パターンに narrow variant 追加、`convert_object` arm 追加。
   - **Phase 3** (`a395cbe`、cast + sizeof + テスト): `evaluate_cast` を 13×13 マトリクスから `NumForm{Signed(i128)/Unsigned(u128)/Float(f64)}` への lift + 13 target への narrow に書き換え (Rust `as` semantics: int-int truncation + sign extension、float-int saturation + NaN→0、int-float round-to-nearest)。type checker `visit_cast_impl` を `is_numeric` 単一分類器化。`narrow_int_arithmetic_and_cast_interpreter` consistency test 追加 (interpreter only — JIT/AOT は Phase 4/5 の fallback path)。
   - **Phase 4** (`9c6edbb`、JIT silent fallback): `ScalarTy::from_type_decl` が narrow 型に None を返すので JIT eligibility が automatic に reject、interpreter 経路に切替。`example/narrow_int_jit_fallback.t` + `narrow_int_jit_falls_back_cleanly` test で interpreter / JIT-mode が両方 142 を返すこと pin。実 cranelift codegen は `NUM-W-JIT` (下記) で別途。
   - **Phase 5** (`bd2d06c`、AOT precise diagnostic): `compiler/src/lower/expr.rs` で 6 narrow `Expr::*` literal variant に precise error message (`AOT compiler does not yet model narrow integer types ... NUM-W Phase 5 not yet implemented`) を出すように差し替え (#204 の precise-fallback policy 同形)。`narrow_int_aot_emits_precise_diagnostic` test で文言を pin。実 cranelift codegen は `NUM-W-AOT` (下記) で別途。
   - **Phase 6** (`03f5243`、stdlib + dispatch 配線): `core/std/hash.t` に `impl Hash for {u8/u16/u32/i8/i16/i32}` 追加 (body は `self as u64`)。parser の `primitive_type_canonical_name` / type checker の `primitive_target_symbol_from_type` + `primitive_type_decl_from_symbol` / `process_impl_method_validation` の許可 type list / interpreter の `primitive_target_symbol` を全 6 width 対応。AOT 側 `primitive_type_decl_for_target_sym` も narrow 対応した上で、method registration loop で narrow target は silent skip (Phase 5 の literal-level reject と組み合わせて、narrow 型を使わない program は AOT 通過、使う program は precise diagnostic) — `narrow_int_hash_dispatch_interpreter` test で u8/u16/u32 hash dispatch が 100107 を返すこと pin。
   **ワークスペース**: 1118 → 1124 tests pass, 31 skipped, 0 失敗。各 phase が独立ビルド可能 / テスト pass の状態で landing。

206. **ドキュメント更新 (Option / Result + str enum payload + per-module namespacing)** (`a3c805c` `e05d048`): `docs/language.md` に "Option and Result (via the auto-loaded option / result modules)" 節を追加 (enum 形・全メソッド・no Allocator 設計理由・eager default の理由・shadowing semantics・3 backend coverage)、Known limitations に「Enum support in JIT」エントリ追加、TOC 更新。`compiler/README.md` に str payload bullet と stdlib Option / Result bullet 追加。`interpreter/README.md` に stdlib ファイル一覧テーブル (math.t / i64.t / f64.t / option.t / result.t)。先行コミット `e05d048` で `#193` / `#193b` / `#194` / `#195` を docs 全体に反映。

205. **AOT で str enum payload を許可** (`#96` follow-up、`2cf90ba`): `Result<u64, str>::Err("boom")` 等が AOT で動くように、`is_supported_enum_payload` allow-list に `Type::Str` を追加。Phase T の opaque-pointer 表現 (`Type::Str = i64-sized`) を再利用するので codegen / boundary 変更は不要 (`flatten_struct_to_cranelift_tys` の enum アームと `allocate_payload_slot` の default scalar branch がすでに対応)。`stdlib_result.t` を natural な `Result<u64, str>` に戻す。`compiler/tests/consistency.rs::interpreter_value` を `check_typing_with_core_modules` に切り替え (Result / Option を使う test に必須)、`enum_str_payload_round_trip` 3-backend test を追加 (interpreter / JIT-fallback / AOT 全て exit 99 一致)。

204. **JIT enum constructor / method skip 理由を具体化** (`fe3e2e9`): `Expr::AssociatedFunctionCall` の reject 経路で qualifier が enum 名のときは `JIT does not yet model enum values (constructors / match / methods)` という precise reason を log するようにした。`enum_decl_lookup_by_name` helper を追加。`tests/jit_integration.rs::jit_skip_reason_for_enum_constructor` で fallback exit code (152) と diagnostic 文言の両方を assert。フル enum support (`ParamTy::Enum` + 値モデル + match/tag dispatch + multi-result return) は AOT compiler の Phase A1〜O 相当の規模で別タスク。

203. **stdlib Option / Result (`#96`)** (`ad6d836`): `core/std/option.t` (`enum Option<T> { None, Some(T) }` + `impl<T> Option<T>` の is_some / is_none / unwrap_or / expect)、`core/std/result.t` (`enum Result<T, E> { Ok(T), Err(E) }` + 同等メソッド) を追加。auto-load 経由で import 行なしで利用可能。**module integration の改修**: (1) `EnumDecl` remap が enum 名 / generic params / 各 variant 名 / payload TypeDecl を main interner に reroute (これがないと auto-loaded `Option<T>` が unmappable symbol で struct 扱いされる)、(2) `MethodFunction` remap が generic_params / generic_bounds / parameter TypeDecl / return TypeDecl も remap、(3) 新 helper `remap_type_decl` (Identifier/Generic/Struct/Enum/Tuple/Array/Dict/Range の symbol を再帰 remap) と `remap_pattern` (EnumVariant/Literal/Name/Tuple)、(4) `Expr::Match` arm を `remap_expression` に追加。**衝突回避**: `AstIntegrationContext` が construction 時に `main_program.statement` から user-declared enum / struct 名を snapshot し、`EnumDecl` / `ImplBlock` の remap で同名 user 宣言があれば silent skip (placeholder `Stmt::Break` のまま)。これで既存テストの inline `enum Option<T>` / `struct Option<T>` がそのまま動作。**interpreter dispatch**: `evaluation/call.rs` に `Object::EnumVariant` arm を追加し、enum 名 symbol で method_registry を引く (struct path と同形)。**type-checker**: `visit_method_call_impl` が `Struct(name, args)` annotation を `enum_definitions` に登録された名前なら `Enum(name, args)` に refine、`visit_method_call_on_type` に Enum branch を追加 (T を enum_generic_params から bind、method-only generic を arg 型から推論、Self を receiver enum に解決)。`visit_field_access_impl` が module-qualified resolution の前に local var lookup を実行 (これがないと auto-loaded `result` module alias が DbC `result` キーワードと衝突)。**AOT compiler**: `lower_method_call` の generic-method branch が enum receiver を分岐し、`instantiate_generic_method_with_self_type` (新規) / `peek_method_return_type_with_self` (新規) で `Type::Enum(enum_id)` を Self type に渡す。compiler 側で動作確認。**注意**: JIT は enum 値モデルが無いので silent fallback (interpreter で正しく動作)。

202. **per-module function namespacing — 型チェッカ + interpreter 実行時** (`#193b`、`ecee60e`): `#193` で IR `function_index` を `(qualifier, name)` キー化したのに対して、型チェッカ層と interpreter 実行時の関数テーブルは依然 flat だったため、同名 `pub fn` を複数モジュールが持つ場合に last-wins で誤った関数が呼ばれていた。3 層全てを揃える。**frontend 型チェッカ**: `context.functions` を `HashMap<(Option<DefaultSymbol>, DefaultSymbol), Rc<Function>>` に re-key、`set_fn_with_module` / `lookup_fn(qualifier, name)` 追加 (bare 優先 + 一意な (Some(_), name) fallback)、`add_function_with_module` を utility に追加。`with_program` と `setup_type_checker` の両登録経路で `program.function_module_paths` を引いて qualifier を pass。`dispatch_module_function_call_with_qualifier` を追加。**interpreter 実行時 (本丸)**: `EvaluationContext.function_qualified` フィールド追加、`new_with_qualified` constructor、`lookup_function_qualified(qualifier, name)` helper。`evaluate_function_call` (bare) は qualified table の (None, name) を優先、`call_associated_function` (qualified) は (Some(struct_name), function_name) を優先。`build_function_qualified_map` を `lib.rs` に追加し `execute_program` で渡す。**compiler AOT**: `lower_program` の関数 declare で `module_qualifier` を export name に mangle (`toy_<qualifier>__<name>`)、cranelift declare 衝突を解消。**stdlib 注記**: `core/std/math.t` に `pub fn add` / `pub fn multiply` を export しない方針を明記 (user が独自型で頻繁に書く名前のため)。

201. **AOT compiler enum receiver method dispatch** (`#96` の前提条件、`ad6d836` 内): `lower_method_call` の generic-method branch を enum receiver に対応 (`#96` 完了の根拠)。詳細は #203 参照。

200. **コンパイラ allocator builtin に specific error message** (`#121` 部分対応): AOT compiler が `__builtin_arena_allocator` / `fixed_buffer_allocator` / `current_allocator` / `default_allocator` / `heap_alloc` / `realloc` / `free` / `ptr_read` / `ptr_write` を encounter すると、汎用の "compiler MVP cannot lower builtin yet" ではなく `compiler MVP cannot lower allocator builtin <X> yet (todo #121: needs IR-level AllocatorBinding + native codegen)` を出すようにした。`AllocatorBinding` IR enum 自体は既に定義済み (`compiler/src/ir.rs`)、ヒープ系 builtin 命令への配線と native codegen 側 (with-scope の active-allocator stack push/pop と heap helper) が次の本実装ステップ。

197. **JIT 一般化フォールバック regression test 追加** (`#160` `#159` 部分対応): JIT eligibility が拒否する高度な機能 (nested tuple `((i64,i64), i64)` parameter, generic `struct Cell<T>` + method) について、interpreter が正しく実行し JIT が静かに / clean error で fallback することを assert する fixture + test を追加 (`example/jit_nested_tuple_fallback.t`, `example/jit_generic_struct_fallback.t`, `tests/jit_integration.rs::jit_nested_tuple_falls_back_cleanly` `jit_generic_struct_falls_back_cleanly`)。実装の本体 (nested tuple は `ParamTy::Tuple(Vec<ScalarTy>)` を tree 構造に拡張する 100+ 箇所の refactor、generic struct は `struct_layouts` を type-args 別に持つ refactor) は別タスク。

196. **`extern fn` の generic params** (`#195`、`c014d94`): parser が `extern fn name<T: Trait>(x: T) -> T` を accept、ジェネリック context を param/return type に thread。interpreter は extern_registry を literal name で keyed (型消去された dispatch) しているため、ジェネリック extern も追加コードなしで動く。JIT/AOT は当該 extern 名が dispatch table にないので clean fallback。`extern fn __extern_test_identity<T>(x: T) -> T` を test 用 registry に追加し、3 種の T (u64 / i64 / bool) で同じ extern を呼ぶ fixture (`example/extern_generic_identity.t`) + test (`extern_generic_identity_runs_via_interpreter_registry`) を追加。

195. **JIT primitive method dispatch の chained call** (`#194`、`b0e3238`): `eligibility::check_method_call` の receiver Identifier 限定を解除。Identifier 受信は従来通り `locals` から型を引き、それ以外 (`MethodCall` / `Call` / `BinaryOp` 等) は `check_expr` を受信側に再帰呼び出しして scalar 型を取得、primitive method 経路で dispatch。struct path は引き続き bare Identifier 必須 (struct 値が `struct_locals` の per-field SSA に分解されているため)。`example/extension_trait_chained.t` (`a.neg().neg()`) + 2 件の integration test (interpreter/JIT 両方が exit 7 を返し、JIT compile log に `i64__neg` が含まれることを assert) を追加。codegen は `gen_call_with_targets` が `[receiver, ...args]` を組み立てて `gen_expr` が再帰的に MethodCall を扱うため変更不要。

194. **per-module function namespacing — 抜本対応の IR 部分** (`#193`、`6dc1731`): AST `Program` に `function_module_paths: Vec<Option<Vec<DefaultSymbol>>>` 並列 vector を追加 (各 `function[i]` が来た module path)。`module_integration::integrate_module_into_program_with_options_full` がそれを populate (auto-load / user-import 両経路)。compiler IR の `function_index` を `HashMap<DefaultSymbol, FuncId>` から `HashMap<(Option<DefaultSymbol>, DefaultSymbol), FuncId>` に re-key (qualifier = module path の **last segment**)。bare call (`(None, name)` → 一意な `(Some(_), name)` の fallback) と qualified call (`(Some(m), name)` 直接) を `Module::lookup_function` 経由に統一。`declare_function_with_module` は collision を panic で表面化 (silent overwrite を不可能に)。lowering の call.rs / expr.rs / let_lowering.rs / type_inference.rs / print.rs を全て新 lookup に切り替え。**注意**: 型チェッカーの `context.functions` は依然 flat — 同名 `pub fn` を複数モジュールが持つ E2E までを完全に許可するには別途 fix 必要。

193. **コア・モジュール auto-load** (`<repo>/core/` を起動時に再帰的にロード): 解決順は (1) CLI フラグ `--core-modules <DIR>` (2) 環境変数 `TOYLANG_CORE_MODULES` (空文字で opt-out) (3) 実行ファイル相対 `<exe>/core/` / `<exe>/../share/toylang/core/` / `<exe>/../../core/`。ファイルシステムの階層から module path を導出し (`core/std/math.t` → `["std", "math"]`)、auto-load された各モジュールに対して `enforce_namespace = false` の synthetic `ImportDecl` を `program.imports` に push、type-checker が module alias (last segment) を登録するので `math::sin(x)` が import 行なしで呼べる。ユーザの explicit `import` は path-dedup で no-op に。`load_and_integrate_module` も core dir を search root として受け取るよう拡張、legacy cwd-relative `modules/...` パスは fallback として残置。`module_integration::remap_expression` に `Expr::AssociatedFunctionCall` arm を追加 (auto-load した stdlib body が `math::abs(self)` を含むため)。stdlib は `core/std/math.t` (math 関数 + extern fn 宣言) / `core/std/i64.t` (Abs trait + i64 impl) / `core/std/f64.t` (Abs/Sqrt impls for f64) に分割、impl body は `math::*` 経由で extern fn を呼ぶ。compiler / interpreter 両方の test helper をデフォルト auto-load にし、collide する fn 名がないことを確認。**1251 tests pass** (commits: `c3fd179` `74db25a` `3825d3c` `10ab918`、2026-05-02)

191. **Extension trait 全 backend 対応 (Step A〜F)**: ユーザが `impl <Trait> for <PrimitiveType> { ... }` を書けるようにし、stdlib の `i64.abs()` / `f64.abs()` / `f64.sqrt()` も同じ仕組みで再実装、最終的に `BuiltinMethod::{I64Abs, F64Abs, F64Sqrt}` を削除。
    - **Step A** (`57e9307`): parser + 型チェッカが `impl Trait for i64/f64/...` を受理。parser で primitive type token を canonical name string として interner 化、`Stmt::ImplBlock { target_type: DefaultSymbol }` の shape を維持。型チェッカの `resolve_self_type` が primitive impl target に対して `TypeDecl::{Int64, Float64, ...}` を直接返す。method/return type のホワイトリストに `Float64` / `Ptr` 追加。
    - **Step B** (`6f1d0a1`): interpreter で primitive receiver の MethodCall を user-method registry 経由で dispatch。`evaluate_method_call` の冒頭に `primitive_target_symbol(obj)` lookup を追加し、`Object::{Bool, Int64, UInt64, Float64, ConstString, String, Pointer}` の各 variant に対して canonical 名 → DefaultSymbol → `method_registry` 引きを実行、ヒット時は user impl body を呼ぶ (legacy hardcoded arms より優先)。
    - **Step C** (`187477f`): JIT 対応。`PRIMITIVE_TARGET_SYMBOLS: HashMap<ScalarTy, DefaultSymbol>` を thread-local で持ち、`callable_signature` が `Self_` を primitive `TypeDecl` に解決、MethodCall arm で primitive receiver を accept し `MonoTarget::Method(prim_target_sym, method_name)` を callees に push。method body の monomorph 経路 (`toy_i64__abs` 等) を介して native cranelift code 化。**制約**: receiver は bare identifier 限定 (chained call は interpreter fallback)。
    - **Step D** (`ec9e737`): AOT compiler 対応。`primitive_type_decl_for_target_sym` ヘルパで Self を primitive `TypeDecl` 化、`lower_method_call` の冒頭に `value_scalar` で receiver の IR Type を peek して primitive method dispatch を試みる arm を追加 (struct path より先に走るので chained call も lower 可能)。`value_scalar` の MethodCall arm に `Binding::Scalar` 受信側のフォールバックを追加。
    - **Step E** (`4152edf`): always-loaded prelude (`interpreter/src/prelude.t`、`include_str!` で binary に embed) に `Abs` / `Sqrt` trait + i64/f64 impl を移行。impl body は `__extern_*` 経由で wrapping_abs / IEEE 754 fabs/sqrt の semantic を保持。`__extern_abs_i64` を 3 backend (interpreter registry / JIT helper or NativeAbsI64 variant / AOT libm `labs` import) に追加。`module_integration::integrate_module_into_program_with_options(... enforce_namespace: bool)` を追加 (prelude は false で bare-callable)。`extern fn` を `imported_function_names` から exempt するロジックも追加。**ExprPool::update バグ修正**: `Stmt::ImplBlock` / `Stmt::TraitDecl` の symbol が module interner のままで main interner に remap されていなかった既存バグを発見・修正 (Step E が最初に triggered)。
    - **Step F** (`eb33e50`): `BuiltinMethod::{I64Abs, F64Abs, F64Sqrt}` を frontend enum と全 backend hardcoded dispatch から削除。`compiler/lower/method_call.rs` の hardcoded `("abs"|"sqrt", recv_ty)` fast path も削除し、Step D の primitive-receiver dispatch path を bare-identifier 制約から外して `value_scalar` driven にした (chained primitive method call が AOT でも動くように)。**-163 行 net** の hardcoded dispatch を削除。
    - **Step E の後で**: prelude を空にして `core/std/{i64,f64}.t` に extension trait 実装を移動 (`74db25a`)。

190. **Math externalisation (Phase 1〜4)**: f64 math intrinsic (sin/cos/tan/log/log2/exp/floor/ceil/pow/sqrt/abs) を frontend の `BuiltinFunction` enum から削除し、stdlib `.t` モジュール経由で `extern fn __extern_*_f64` 経由に切り替えた。
    - **Phase 1** (`decb16f`): `extern fn name(params) -> ret` 宣言構文を frontend parser + 型チェッカに追加。`Function::is_extern: bool` フィールド、placeholder body (`Stmt::Break`)、type checker の short-circuit (declared return type をそのまま採用、body 走査スキップ)。`extern fn` を持つ関数を呼ぶと当面 "not yet implemented" runtime error。
    - **Phase 2a** (`6ce8717`): interpreter dispatch。`evaluation/extern_math.rs` に `f64::{sin,cos,tan,log,log2,exp,floor,ceil,sqrt,abs}` + `pow` の Rust 実装を持つ `HashMap<&'static str, fn(&[Value]) -> Result<Value, _>>` レジストリを構築、`EvaluationContext::extern_registry` で startup 時に populate。`evaluate_function*` で `is_extern` 時に `dispatch_extern_fn` 経由で registry を引く。
    - **Phase 2b/2c** (`12bda7e`): JIT / AOT で extern fn を skip (defensive) — JIT は eligibility で reject、AOT は declare/lower で skip。
    - **Phase 2d** (`64e209e`): JIT name-based extern dispatch。`JIT_EXTERN_DISPATCH` table が extern fn 名 → `Helper(HelperKind)` (sin/cos/tan/log/log2/exp/pow) または `Native*` variant (sqrt/floor/ceil/abs) に振り分ける。`try_gen_extern_call` で codegen 側のディスパッチ。
    - **Phase 2e** (`7481770`): AOT 対応。IR に `Linkage::Import` を追加、`libm_import_name_for(name)` で `__extern_sin_f64 → sin` 等に正規化、`Stmt::ImplBlock` / extern fn を `Linkage::Import` で declare、`build_object_module` / `emit_clif_text` で skip。
    - **Phase 3** (`6aed1f9`): `interpreter/modules/math/math.t` を `extern fn __extern_*_f64` ベースに書き換え。**ExprPool::update bug 修正**: Call args が `rhs` 列に書き込まれていたが `add` / `get` は `operand` 列を読む不整合を発見 (Phase 3 で初めて顕在化)。
    - **Phase 4** (`2842bf2`): `BuiltinFunction::{Sin, Cos, Tan, Log, Log2, Exp, Floor, Ceil, Pow, Sqrt}` を frontend enum と全 backend dispatch から削除。**-395 行 net**。e2e テスト 14 件を `extern fn __extern_*_f64` 宣言ベースに書き換え。

189. **compiler test 高速化 — `toylang_rt.c` をビルド時に prebuild**: `compiler/build.rs` で `cc -c -O2 -fPIC runtime/toylang_rt.c -o $OUT_DIR/toylang_rt.o` を 1 回実行、`driver.rs` は `include_bytes!` で取り込んで link 時に書き出すだけにした。1 テストあたりの compile 時間が 700-800ms → 50ms (debug build) に。並列 wall-clock は macOS の Mach-O 検証コスト (150-300ms/binary) が dominate しているため改善限定的だが、sequential / CI シナリオで効く。残るボトルネック: 各 e2e テストが新規 binary を fork+exec する macOS のコード署名検証。完全解決には cranelift-jit ベースの in-process loader が必要 (要 codegen 大幅 refactor)。コミット: `b295174` (2026-05-02)

188. **`compiler/README.md` にテスト構成セクションを追加** (`2e4d8f6`): `e2e.rs` (191) / `consistency.rs` (23) の役割、`COMPILER_E2E=skip` opt-out、`cargo nextest run -p compiler` 推奨、現状の wall-clock (60-70s on 20-core macOS)、パフォーマンス内訳 (compile ≈50ms / Mach-O 検証 ≈150-300ms)、調査済み dead-end (cp/exec/dlopen/lld/codesign/shared path)、残る改善余地 (cranelift-jit / テスト統合)。Future contributor が同じ調査を繰り返さないように記録。

187. **`ExprPool::update` の Call args 列ミスマッチバグ修正** (Math externalisation Phase 3 副作用): `add` / `get` は `Expr::Call(func, args)` の args を `operand` 列に格納するが、`update` は `rhs` 列に書いていた。Module integration の `update_with_remapped_content` が remap した Call を pool に書き戻すと silent corruption (slot の `expr_types` は Call なのに `get` が None を返す)。Phase 3 で math.t の wrapper body が `__extern_*_f64(x)` を呼ぶようになって初めて顕在化。1-line fix で `operand` 列に統一、コメントで invariant を明示 (`6aed1f9`)。

184. **`trait` 宣言と `impl <Trait> for <Type>`**: 共通インターフェースの仕組みを追加。`trait Name { fn m(self: Self, ...) -> T; ... }` でシグネチャだけを宣言、`impl <Trait> for <Struct> { ... }` で body を提供。型チェッカーが trait のシグネチャと比較して欠落メソッド・型不一致を検出（`missing method` / `parameter type mismatch` / `return type mismatch`）。型パラメータ bound `<T: SomeTrait>` を関数・struct・impl に書け、呼出時に「実型がその trait を実装しているか」を `struct_trait_impls` で検証。`Self` は impl の対象 struct に解決される。impl-trait の method は同 struct の inherent method としても登録されるため、interpreter 側のメソッドディスパッチは無変更で動作。トークン `Kind::Trait`、AST `Stmt::TraitDecl` + `Stmt::ImplBlock.trait_name: Option<DefaultSymbol>`、新規 `frontend/src/type_checker/trait_decl.rs` で conformance check。tests: `interpreter/tests/trait_tests.rs` に 10 件追加（基本宣言・impl-method dispatch・bounded-generic dispatch・複数 struct 実装・missing method / signature mismatch / 未実装 struct の bound 違反 / 重複 trait / 重複 method）。example: `interpreter/example/trait_basic.t`。`docs/language.md` に新章 *Traits*、CLAUDE.md にもキーワード追加。out of scope（後続）: trait ジェネリクス・デフォルトメソッド・複数 bound・trait 継承・`dyn Trait`・associated types (2026-04-30)
182. **Value/Reference 分離 Phase 5 後半 — variable assignment / 演算子 operand の Value 化**: `handle_variable_assignment` を Value-native に書き直し（`val.borrow()` 経由の Object クローンを Value::clone で置換、不要な `rhs_borrow` を排除）、`evaluate_binary` / `evaluate_unary` / 短絡論理演算子の operand 評価を `try_value!` (Rc allocate) → `try_value_v!` (Value 直) に置換、Phase 2 で残っていた `Value::from_rc(&lhs_val)` の中間変換を削除。`handle_val_declaration` / `handle_var_declaration` も同様。**Bench 結果 (Apple Silicon release)**:

```
                          Pre-Phase1   Post-Phase4   Post-Phase5(全)   vs Pre-P1
fibonacci_recursive       130 µs       150 µs        120 µs            -8% 高速
for_loop_sum              314 µs       349 µs        275 µs            -12% 高速
complex_expressions       35 µs        36 µs         34 µs             -3% 高速
type_inference_heavy      27 µs        27 µs         27 µs             parity
variable_scopes           65 µs        69 µs         63 µs             -3% 高速
parsing_only              34 µs        34 µs         36 µs             +6% (noise)
```

最も hot な fibonacci/for_loop で **pre-Phase1 を 8〜12% 上回る** 性能を達成。当初の目論み「primitive 値の Rc 排除で 10〜30% 改善」がようやく現実化。残るは literal eval / array slice / dict 等の cold path だが、計測可能な hot path はほぼカバー済み。tests: 492 件すべて pass (2026-04-30)
181. **Value/Reference 分離 Phase 5 — hot path consumer の Value-native 化（前半）**: `evaluate_function_with_values` のシグネチャを `args: &[RcObject] → &[Value]`、戻り値 `RcObject → Value` に変更。`evaluate_function_call` の引数評価を `try_value_v!` ベースに、`Vec<Value>` で受け取る。`evaluate_if_elif_else` の cond 評価を Value 直 match、`handle_while_loop` / `handle_for_loop` の cond / start / end も Value 経由。`execute_for_loop` 内のイテレータ束縛も Value 直接。`call_struct_method` / `call_associated_function` は legacy `RcObject` 引数を境界で `.into()`。Bench 改善 (Apple Silicon release): fibonacci_recursive 150→136µs (-9%)、for_loop_sum 349→337µs (-3%)、complex_expressions 36→34µs (-5%)、variable_scopes 69→66µs (-4%)。Pre-Phase1 (dd9ff33) 比では fibonacci +5% / for_loop +7% / complex_expressions -3% / type_inference -3% / parsing -3% — 一部 hot path で改善、残るは Phase 5 後半 (literal eval / member access / arithmetic operand 等) で更に migration が必要。tests: 492 件すべて pass (2026-04-30)
180. **Value/Reference 分離 Phase 4 — Environment を Value に**: `VariableValue.value: RcObject` → `Value`、`Environment::set_val` / `set_var` / `get_val` のシグネチャを Value 化。これにより val/var 宣言時の `Rc::new(RefCell::new(...))` 構築が primitive で消える（inline 値そのまま格納）、Identifier 参照時の `Rc::clone` も `Value::clone()` （primitive は cheap copy、Heap は Rc::clone）に置換。call.rs / statement.rs / expression.rs / lib.rs の全 set_val/set_var/get_val callsite に `.into()` 変換を挿入し、From<Object> / From<RcObject> for Value 経由で自動変換。`handle_identifier_expression` から `.borrow().is_null()` を `Value::is_null()` に直接置換、`handle_assignment` の type-check ブランチも Value 直アクセスに。tests: 492 件すべて pass (2026-04-30)
179. **Value/Reference 分離 Phase 3 — `EvaluationResult` を Value に**: `EvaluationResult::Value(Rc<RefCell<Object>>)` → `Value(Value)`、同様に `Return(Option<RcObject>)` → `Return(Option<Value>)`。`try_value!` macro はバックエンド互換性のため `v.into_rc()` で内部変換、つまり既存 consumer サイト（`val.borrow()` パターン）は無変更で動作。新規 hot path 用に `try_value_v!` macro を提供（`Value` を直接返す）。全 ~30 箇所の `EvaluationResult::Value(rc)` 構築サイトに `.into()` 追加（`From<Object>` / `From<RcObject>` for Value 経由）、`Rc::new(RefCell::new(obj))` パターンは `obj.into()` に書き換え。`evaluate_function_with_values` / `evaluate_method` の Variant ↔ RcObject 変換も整理。`Object` enum 自体はまだ primitive variants を持っており、Phase 4 以降で内部表現の最適化を進める基盤。tests: 492 件すべて pass (2026-04-30)
178. **Value/Reference 分離 Phase 2 — operators の内部 Value 化**: `operators.rs` の comparison / arithmetic / bitwise / shift / unary / short-circuit 全dispatch経路を `&Value` ベースに書き換え。`evaluate_arithmetic_op_v` / `evaluate_comparison_op_v` / `evaluate_bitwise_*_v` / `evaluate_*_shift_v` を新設、内部の primitive matching は `Object` ではなく `Value` で行う。`evaluate_binary` / `evaluate_unary` は entry で `Value::from_rc`、exit で `into_rc` する境界変換に統一。短絡論理演算子も同様に Value 経由。legacy `&Object` 受け取り版 (`evaluate_add` 等) は `object_ref_to_value` / `value_to_object` shim で Value path に flatten、API 互換は維持。tests: 492 件すべて pass、Phase 1 と合わせて Phase 3 (evaluate 戻り値を Value 化) のための基盤完成 (2026-04-30)
177. **Value/Reference 分離 Phase 1**: `src/value.rs` に新規 `Value` enum を追加。primitive variants (Bool / Int64 / UInt64 / Float64 / ConstString / Pointer / Null / Unit) は inline で持ち、composite は `Heap(RcObject)` で既存の `Rc<RefCell<Object>>` を再利用。`Value::from_rc` / `Value::into_rc` / `Value::clone_to_rc` の境界 shim を提供し、Phase 2 以降で hot path の関数シグネチャを `RcObject` → `Value` に置換しても interpreter の他部分は無変更で動く。primitive 構築・型取得・accessor (`try_unwrap_int64` 等) を実装、`debug_assert!` で `Value::heap` に primitive variant を渡すミスを検知。tests: `value.rs` に 4 件追加 (round-trip / const_string / heap-sharing / type_lookup_matches_legacy)。既存 488 件すべて green、合計 492 件 (2026-04-30)
176. **`Object::Struct` のフィールド key を `String` から `DefaultSymbol` に変更**: `fields: Box<HashMap<String, RcObject>>` → `Box<HashMap<DefaultSymbol, RcObject>>`。フィールドアクセス毎に発生していた `string_interner.resolve(*sym).to_string()` の allocation を排除し、HashMap lookup を u32 比較に。`evaluate_field_access` / `evaluate_struct_literal` / `handle_field_assignment` から resolve の呼び出しを削除（エラーパスでのみ resolve）。`Object::set` の field clone も `(*k, v.clone())` で軽量化。`to_display_string` は表示時に各 symbol を resolve する形に（フィールド数分の lookup だが一回限りなので OK）、ハッシュは symbol 数値 id でソート（テキスト ASCII 順ではなくなったが Hash 用途なので問題なし）。dead code だった `Object::get_field`/`set_field` と `ObjectError::FieldNotFound` を削除。oop_tests のテスト fixture を `DefaultSymbol::try_from_usize(N)` ベースに移行。tests: 488 件すべて pass、struct 経由の `jit_struct.t` も interpreter / JIT 両モードで exit=20 を継続 (2026-04-30)
175. **`ScalarTy::Never` で expression position の panic を JIT 化**: `BuiltinFunction::Panic` の eligibility 戻り型を `Unit` から新設 `ScalarTy::Never` に変更。`Never` は bottom type として `ScalarTy::unify_branch(a, b)` で他のどの型とも互換となり、`if cond { panic("...") } else { 5i64 }` のような expression position でも if 式全体が i64 として通る。`Expr::IfElifElse` eligibility は等値比較ではなく `unify_branch` で枝の型を統合し、`gen_if` codegen も全枝の型を unify して `cont` block param を決定する。Never 枝は codegen で `trap UserCode(1)` を出して `terminated=true` になるため cont へ jump せず、verifier は predecessor 1 のみで満たされる。`val` / `var` の RHS が Never の場合は eligibility で reject（`val x = panic("...")` は意味のないコードだが silent fallback で interpreter が正常 panic させる）。`ir_type(Never) = None`、main の Never 戻りは jit_panic helper の `process::exit` 経由で reach 不可。example: `jit_panic_expr.t` / `jit_panic_expr_fail.t`、tests: `jit_integration` に 1 件追加（成功・失敗両パスで `divide` が JIT 化されることを確認）。JIT.md 更新、Known limitations から「expression-position panic 不可」を削除 (2026-04-30)
174. **`panic` / `assert` を release でも常時 active と決定（運用方針をドキュメント化）**: `INTERPRETER_PANIC` / `INTERPRETER_ASSERTIONS` 系 env-var の追加は見送り。理由: D の `-release` で assert が消え本番でバグが顕在化する事例の再発を避けるため、type-safe に切れる assert も含めて release で残す方針。`docs/language.md` の Known limitations から「No release-mode gate for panic / assert」を削除し、代わりに「intentionally always-on by design」セクションに置換。`docs/language.md` / `interpreter/README.md` / `README.md` の `INTERPRETER_CONTRACTS` 説明箇所に「`all` を本番でも維持するのが推奨。`pre`/`post`/`off` は計測可能なホットパスのみで使うこと」「panic/assert は build profile 間で挙動を一致させるため意図的に gate しない」運用警告を追記 (2026-04-30)
173. **`assert(cond, msg)` ビルトイン + JIT 対応**: `panic` の隣人として追加。`BuiltinFunction::Assert`、シンボル `assert`（user-facing 名）、type_checker は `(bool, str) -> ()`。interpreter は cond を bool として評価し、true なら Unit、false なら msg を to_display_string で文字列化して `InterpreterError::Panic { message }`。message は false 時にのみ評価される（lazy）。JIT は `brif cond, cont_blk, fail_blk; fail_blk: call jit_panic(msg_sym); trap UserCode(1); cont_blk: …` で lower、success path は1分岐コスト、failure path は `panic("literal")` と同じ `jit_panic` helper に集約。message は `Expr::String(_)` のみ accept（panic と同じ制約）。example: `jit_assert.t`、tests: `language_core_tests` に 3 件 / `jit_integration` に 3 件追加。docs/language.md / CLAUDE.md / JIT.md 更新、Known Limitations の「assert 無し」削除（代わりに「panic/assert の release-mode gate 無し」を残置） (2026-04-30)
172. **`panic("literal")` の JIT 対応（Option B: symbol id 渡し）**: cranelift JIT で `panic("literal")` をサポート。`HelperKind::Panic` を追加し、`extern "C" fn jit_panic(sym_id: u64)` が thread-local `JIT_STRING_INTERNER` 経由で program の `&DefaultStringInterner` を `*const` で borrow して symbol → &str を resolve、`Runtime error occurred:\npanic: <msg>` を stderr に出して `process::exit(1)`。`execute_cached` 内で raw pointer を install/clear（HeapGuard と同じパターン）。eligibility は `Expr::String(sym)` のときのみ accept、それ以外は既存の "unsupported builtin" 経路。codegen は `iconst u64 sym.to_usize()` + `call_helper(Panic)` + `trap UserCode(1)` を emit、`state.terminated = true`。trap は helper が exit するため dead code、CFG terminator として cranelift verifier を満たす目的のみ。expression position（`if cond { panic("...") } else { value }`）は branch 型不一致で silent fallback（将来的には `ScalarTy::Never` で対応予定）。example: `jit_panic.t`、tests: `jit_integration` に 2 件追加（JIT compiled + helper 経由 panic / dynamic 引数の fallback）。JIT.md 更新 (2026-04-29)
171. **`panic("msg")` ビルトイン**: 実行を中断する終了用 builtin。`BuiltinFunction::Panic` を AST に追加、シンボルは `panic`（`__builtin_` prefix 無しの user-facing 名）。type_checker のシグネチャテーブルで 1引数 `str` → `Unknown` として登録、Unknown を「発散する式の型」として if-elif-else の枝統一と関数 body の戻り型一致判定でワイルドカード扱い（`if cond { panic("...") } else { 5i64 }` や `fn foo() -> i64 { panic("not impl") }` がそのまま通る）。interpreter は `InterpreterError::Panic { message }` を返して停止、表示は `panic: <message>`。JIT は既存の catch-all `unsupported builtin` 経路で silent fallback。example: `panic.t`、tests: `language_core_tests` に 3 件追加（基本 / if-then 位置で型ユニファイ / const をメッセージに使う）。docs/language.md / CLAUDE.md 更新、Known Limitations から「panic 無し」を削除し「assert 無し」だけ残置 (2026-04-29)
170. **トップレベル `const` 宣言**: `const NAME: Type = expression` を関数の外側に書けるようにした。`Kind::Const` トークン、`Program.consts: Vec<ConstDecl>`、parser で `pub? const NAME: Type = expr` を読む経路、type_checker でグローバルスコープに `set_var` で登録（前方参照不可）、interpreter で main 呼出前に各 const を順番に評価して `environment.set_val`。型ミスマッチは「Const `X` declared as ... but initializer has type ...」の専用エラー。JIT は const を参照する関数を silent fallback（const 値は eligibility walker からは未知の identifier に見える）。example: `const_decls.t`、tests: `language_core_tests` に 5 件追加（基本利用 / 関数からの参照 / f64 const / 先行 const 参照 / 型エラー）。docs/language.md / CLAUDE.md / JIT.md を更新 (2026-04-29)
169. **言語リファレンス `docs/language.md` を新設**: 構文・型・式・文・関数・struct / impl・enum / match・generics / bounds・modules・allocators・builtins・Design by Contract・runtime model・known limitations を 1 ファイルに集約。CLAUDE.md / README.md からはトップで `docs/language.md` を「正本」として案内し、言語仕様の重複を許容しつつ最新は language.md を参照する形に。隣接ドキュメント（JIT.md, ALLOCATOR_PLAN.md, BUILTIN_ARCHITECTURE.md, interpreter/README.md）は実装者向けとして残しリンク (2026-04-29)
168. **`InterpreterError::PropagateFlow` の除去とフロー伝搬バグ修正**: `extract_value` が制御フロー (Return/Break/Continue) を `Err(PropagateFlow(_))` に詰めて伝搬していたが、誰もキャッチせず関数 / ループ境界をすり抜けて user に「Propagate flow: …」を表示する潜在バグを発見（`val y = if cond { return X } else { Y }` で再現）。`extract_value` を `try_value!` macro と `unwrap_value` に分離。前者は flow を `return Ok(flow)` で関数の caller に正しく伝搬、後者は flow を許さない位置（contract 述語、pattern literal）で flow を InternalError 化。`InterpreterError` から `PropagateFlow` variant 削除（API leak 解消）。`handle_val_declaration` / `handle_var_declaration` の戻り型を `Result<Option<EvaluationResult>, _>` → `Result<EvaluationResult, _>` に統一し、`EvaluationResult::None` で「値を生まない statement」を表現。tests: `language_core_tests` に regression 2 件追加（return が if-then / else 位置から正しく function return 値になる） (2026-04-29)
167. **DbC release mode（`INTERPRETER_CONTRACTS` env var）**: `requires` / `ensures` を独立に切替可能な runtime gate。`ContractMode { check_pre, check_post }` を `evaluation::mod` に追加し、`EvaluationContext::new` で env を 1 回読む。値は `all|pre|post|off`（unset = `all`、case-insensitive、`on/1/true` / `0/false` も受け付け、未知値は stderr 警告 + `all` フォールバック）。`evaluate_function_with_values` の requires/ensures ブロックと、method 経路の `evaluate_method_requires` / `evaluate_method_ensures` ヘルパー先頭で gate。テストは `tests/contract_mode_tests.rs` に 7 件（process spawn ベース、各モード × pre/post 違反プログラム）。CLAUDE.md / README.md にも記載 (2026-04-29)
166. **Design by Contract（`requires` / `ensures`）**: 関数とメソッドの `-> ReturnType` の後、body `{` の前に `requires <bool_expr>` / `ensures <bool_expr>` 節を複数並べられる（案 1 多節形式）。`Function` / `MethodFunction` AST に `requires: Vec<ExprRef>` / `ensures: Vec<ExprRef>` を追加。parser は contract clause 列をパース（`Condition` context で struct literal 抑止）。type_checker は各節を bool で検証し、`ensures` では `result` 識別子を戻り値型で binding。interpreter `evaluate_function_with_values` / `call_method` / `call_associated_method` で entry 時に `requires` を、exit 時に `result` を bind して `ensures` を評価し、違反時に `InterpreterError::ContractViolation { kind, function, clause_index }` を返す。JIT は contract を持つ関数を eligibility で reject（silent fallback）。`old(...)` と名前付き return（案 4）は今回スコープ外。example: `contracts.t`、tests: `language_core_tests` に 6 件追加（pass / requires違反 / ensures違反 / 多節での clause index / メソッド contract / 非 bool 節は型エラー） (2026-04-29)
165. **f64 (浮動小数点数) サポート**: `TypeDecl::Float64`、`Kind::F64` / `Kind::Float64(f64)`、lexer に `1.5f64` / `42f64` パターン（タプルアクセス `t.0.1` との曖昧性回避のため `f64` サフィックス必須）、`Expr::Float64(f64)`、`Object::Float64(f64)`。算術 (`+ - * / %`)、比較 (IEEE 754 ordered)、unary minus、`as` による i64/u64 ↔ f64 変換、`__builtin_sizeof = 8` を実装。Hash/Eq/Ord は `to_bits()` でビット等価ベース（NaN を Dict キーに使えるように total order）、表示は `1.0` のように常に小数点付き。JIT も対応：`ScalarTy::F64` を追加し、`fadd/fsub/fmul/fdiv` と `fcmp` (Ordered)、`fneg`、`fcvt_from_sint/uint` および `fcvt_to_sint/uint_sat`（Rust の `as` と一致）、`jit_print_f64` / `jit_println_f64` ヘルパー、`main` の f64 戻り値を `Object::Float64` に詰め直し。f64 mod は cranelift にネイティブ命令が無いため eligibility で reject（silent fallback）。example: `float64.t` / `jit_float64.t`、tests: `language_core_tests` に 7 件、`jit_integration` に 2 件追加 (2026-04-28)
164. `%` 剰余演算子と複合代入 (`+= -= *= /= %=`): lexer/token/AST に `IMod` および `PlusEqual` 系トークンを追加。parser の `parse_mul` で `%` を *,/ と同じ優先度で扱い、`parse_assign` 入口に複合代入 dispatch を追加。複合代入は `lhs op= rhs` を `lhs = lhs op rhs` に desugar (LHS は identifier / `FieldAccess` 対応、SliceAccess も既存 SliceAssign 経路で動く)。type_checker は既存の `IAdd | ISub | IDiv | IMul` ケースに `IMod` を merge。interpreter は `ArithmeticOp::Mod` を Rust の `%` で実装 (truncated remainder)。JIT は cranelift の `srem`/`urem` で実装。example: `modulo_compound.t`、tests: `language_core_tests` に 5 件追加 (2026-04-28)
163. JIT タプル対応 (flat scalar tuples): `ParamTy::Tuple(Vec<ScalarTy>)` を導入、tuple 型の関数 param / return / val / var / TupleAccess / TupleLiteral RHS / tuple-returning call / tuple alias を JIT eligibility と codegen に追加。tuple param は要素ごとに cranelift param に分解、tuple return は multi-return、TupleAccess は要素 SSA Variable から `use_var`。`val (a, b) = expr` 分解は parser desugar (`val tmp = expr; val a = tmp.0; val b = tmp.1`) 経由で自動的に動く。tuple 引数は名前付き local 必須 (inline literal は不可)。Out of scope: ネストタプル、tuple-of-struct、main の tuple return。example: `jit_tuple.t`、tests: `jit_integration` に 2 件追加 (2026-04-27)
162. ネストした val/var タプル分解: `parse_tuple_destructuring` を `DestructPat { Name | Tuple }` 木で再帰化、`emit_destructure` が深さに応じて `__tuple_tmp_N` を連鎖させる。outer `is_val/is_var` は leaf binding にのみ伝播し、内部 tmp は常に `val`。`val ((a, b), c) = ...` / `val ((a, b), (c, d)) = make()` / `val (((a, b), c), d) = ...` / `var ((a, b), c) = ...` + 再代入が動作。example: `tuple_destructure_nested.t`、tests: `collections_tuple_struct_tests` に 4 件追加 (2026-04-27)
161. match arm guard: `match x { v if v < 0 => …, _ => … }` のように pattern と `=>` の間に `if <bool>` を置ける。AST は `MatchArm { pattern, guard: Option<ExprRef>, body }` 構造体に統一、parser は guard 式を `Condition` context で読む（struct literal 禁止）、type_checker は guard を `Bool` 型でチェックし pattern bindings を可視に保つ。guarded arm は exhaustiveness で wildcard 扱いせず、literal/enum-variant の "fully covered" マークも付けないので網羅性が緩まない。interpreter は pattern 一致後に guard を評価し false なら次の arm にフォールスルー（bindings はスコープごと破棄）。example: `match_guard.t`、tests: `collections_tuple_struct_tests` に 5 件追加。JIT は match を従来どおり silent fallback (2026-04-27)

## 未実装 📋

NUM-W-JIT. **NUM-W 続き — JIT cranelift codegen for narrow ints**: NUM-W Phase 4 (`9c6edbb`) で silent fallback を pin したので機能上は動くが、interpreter 経由なので速度が出ない。本格対応には `ScalarTy::U8/16/32/I8/16/32` を追加し、`ir_type` で `I8 / I16 / I32` cranelift type に lower、`make_signature` の ABI 越境で sign-extension / zero-extension ルール (cranelift は引数を I64 / I32 register 単位で渡すので narrow 型は `uextend` / `sextend` が必要)、算術 / 比較 / cast / print helper の per-width 経路 (cranelift の `iadd` 等は operand width から推論するが、wrap-on-overflow の semantic は trap 命令と区別が必要)、`StructLayout` の field packing。eligibility 周辺と codegen 全体に渡る数百〜千行規模。完了後は `narrow_int_jit_falls_back_cleanly` test を「JIT が compiled する」アサートに昇格 + Phase 3 / 6 の interpreter-only consistency test を 3-way 一致に切替。優先度: 中。

NUM-W-AOT-pack-Phase3. **狭い数値型 AOT の compound element packing 続き**: NUM-W-AOT-pack Phase 1 (`747146c`) で homogeneous scalar 配列の packing は完了。残: compound element 配列 (`[PackedRgba; N]` で `struct PackedRgba { r:u8, g:u8, b:u8, a:u8 }` が現状 32 バイト消費、本来 4 バイト) の tighter layout。実装には `ArraySlotInfo` を per-leaf strides ベースに refactor、もしくは `ArrayLoad/Store` に element_index + leaf_offset_const の 2 値を取らせる IR 拡張が必要。優先度: 低 (機能は動くがメモリ効率の改善のみ)。

195b. **`extern fn` 生 monomorph 化** (`#195` の続き): 現状ジェネリック extern は interpreter の type-erased registry でだけ動く。JIT/AOT で動かすには call site ごとに mangled extern symbol (`__extern_id__u64`, `__extern_id__i64` など) を emit し、対応する monomorph 実装を Rust 側に登録する仕組みが必要。優先度: 低 (現状必要なケースなし)

185残. **3+ part qualified call** (`std::math::abs(x)` 形式) — 現状は `import std.math` (または auto-load) してエイリアス `math` 経由でしか呼べない (parser が 3-part path で last 名のみを採用するため)。優先度: 低 (auto-load のおかげで実用上の不便は限定的)

JIT-enum-1. **JIT 値モデルとしての enum サポート** — `ParamTy::Enum(EnumId)` 追加、enum constructor (`Option::Some(...)`) の cranelift lowering、match の brif chain dispatch、enum-typed function param/return の boundary handling、enum receiver method dispatch、generic enum (Option<T>) のモノモル化対応。AOT compiler の Phase A1〜O 相当の規模 (~1000+ 行)。現状は silent fallback (interpreter で全機能動作)。優先度: 中 (実装すれば Option/Result + ユーザ enum がすべて JIT compile 可能になる)。**diagnostic 状態 (`453c68b`)**: 既存の precise message に `JIT-enum-1` cite を追加 — `JIT: skipped (... JIT does not yet model enum values (constructors / match / methods; see JIT-enum-1))` のように出るので、ユーザは diagnostic から直接この todo entry に飛べる。regression test は既に `jit_skip_reason_for_enum_constructor` (#204) で pin 済み。

160. **タプルの追加 JIT 対応** — フラットなスカラーtupleの param / return / TupleAccess / destructure / tuple-returning call は完了 (`#163`)。残: ネストタプル (`((a,b),c)`) と tuple-of-struct を JIT codegen で扱う (`ParamTy::Tuple(Vec<ScalarTy>)` を tree 構造に拡張する 100+ 箇所の refactor)、inline tuple literal を call argument として渡せるようにする
159. **JIT Phase 2 拡張** — Phase 1 / 2a-2h / 2c-2 / 2d-2/3/4 / 2e (allocator stack) / `__builtin_fixed_buffer_allocator` / `with` 内の早期 exit はすべて完了。残: generic 構造体 / メソッド (`struct_layouts` を type-args 別に持つ refactor)。サポート範囲のまとめは `JIT.md`。**diagnostic 状態 (`98e2249`)**: generic struct を踏むと `JIT: skipped (... struct literal references a generic struct (JIT does not yet model generic struct values; see #159))` が出るので、diagnostic から直接 todo entry に飛べる。`jit_skip_reason_for_generic_struct` test で wording を pin。
121-Phase-B-rest-leftover. **inline arena 自動 drop + AllocatorBinding wiring + default_allocator API** — Phase B-rest Item 1+3 + Item 2 cleanup + arena_drop builtin 完了。残: (1) `with allocator = __builtin_arena_allocator() { ... }` 形での自動 drop (現状は user が `__builtin_arena_drop` を明示呼出する必要、syntactic check で inline arena だけ auto-drop する option)、(2) `AllocatorBinding::Generic/Local/Ambient` の lower 配線 (perf 最適化のみで観察可能な振る舞い変化なし、IR 上の placeholder)、(3) `__builtin_default_allocator()` の戻り型を `Allocator` から `u64` に変えて生比較 (`!= 0u64`) を許可するか — 現状は `!= __builtin_default_allocator()` の方法しかない。priority 低 (cosmetic / perf / API 整理)。
96残-後半. **Enum/match — Option/Result の追加 stdlib メソッド** — `Option::map<U>` / `and_then` / `Result::map_err` 等を追加するには **closures** が必要 (要設計議論)。toylang は closure 型をまだ持たないので、その前段の検討が必要。前半 (深い網羅性解析) は `273580c` で完了。

CONCRETE-IMPL-Phase-2c. **annotation hint threading + 型 checker registry refactor** — Phase 1 / 2-interp / 2b は完了 (パーサ + AST + interpreter dispatch + AOT compiler dispatch、3-way `assert_consistent` で `Container<u8>` / `Container<i64>` 並存 dispatch を pin)。**残 (Phase 2c)**: (1) annotation hint (`var v: Vec<u8> = Vec::from_str(...)`) を `call_associated_function` (interpreter) / `lower_let::AssociatedFunctionCall` (compiler) の lookup まで thread し、現状の lone-spec fallback を狭める。複数 concrete-args 入りの associated function (`impl FromStr for Vec<u8>` + `impl FromStr for Vec<i64>` で両方が `from_str(s) -> Self` を持つケース) を annotation で disambiguate 可能に。(2) 型 checker `struct_methods` registry を Vec<MethodSpec> 形に refactor (現状 same `(struct, method)` で異 args の impl が来ると last-wins、signature が同一なら type-check は通るが、異なる signature を持つ複数 concrete impl を厳密化するために必要)、(3) generic methods queue (`pending_method_work`) で対象の `target_type_args` を carry し、Vec から first spec ではなく対応する spec を選ぶように。priority 中。

REF-Stage-2. **一般 `&T` reference を一級型として導入 (Stage 2)** — Stage 1 (`e4c5e95`) で `&mut self` (impl-block method receiver 限定) は完了。Stage 2-min (REF-Stage-2-min) で `&T` 引数型 + auto-borrow も完了。**REF-Stage-2 (a)+(d) 完了 (2026-05-04)**: `TypeDecl::Ref` を `Ref { is_mut: bool, inner }` に拡張、`& [mut]? T` を type 位置で parse、`UnaryOp::Borrow / BorrowMut` を追加し prefix `& expr` / `&mut expr` を expression 位置で parse、type checker `is_arg_compatible` を `&mut T → &T` ダウングレードまで対応、interpreter / JIT / AOT は borrow を pass-through erasure、AOT `lower_call_args` で borrow 式を peel して struct identifier 展開パスに合流。test `ref_stage2_explicit_borrow_and_mut_ref_round_trip` (3-way) で auto-borrow + explicit `&s` + `&mut T` 引数型 + `&mut s` ダウングレードを pin。**REF-Stage-2 (f) 完了 (2026-05-04)**: `val` からの `&mut` borrow を reject + auto-borrow `T → &mut T` を reject。`VarState` に `is_mut: bool` 追加、`set_var` (immutable, default) と `set_mutable_var` (mutable) を区別。`process_val_type_with_mut` を新設し `visit_var_impl` (= `var` 宣言) は is_mut=true、その他は false。新ヘルパ `TypeCheckContext::is_var_mutable(name) -> Option<bool>`。`visit_unary` で `BorrowMut` のとき operand を peek、`Expr::Identifier` 以外は "not a place" でエラー、bare identifier でも `is_var_mutable` が Some(false) / None なら "cannot borrow as mutable" でエラー。`is_arg_compatible` から `T → &mut T` 経路を削除 — `&mut T` 引数は call site で必ず `&mut <var>` を書く形に統一。interpreter の runtime arg-check は references 既に erase 済みなので expected を `deref_ref` で peel してから `is_equivalent` 比較。test `test_mut_borrow_of_val_binding_is_rejected` + `test_auto_borrow_into_mut_ref_is_rejected` (interpreter trait_tests) で両 reject ケースを pin。1153 → 1155 tests pass。**REF-Stage-2 (e) 完了 (2026-05-04)**: 構文的 escape reject (lifetime / aliasing / field-level borrow / iterator invalidation はすべて scope 外、escape 解析だけ minimal に入れる方針)。3 サイトで type checker reject — (1) 関数戻り型に `Ref` を含む / (2) `val` / `var` binding の annotation 又は inferred 型が `Ref` を含む / (3) struct field 型が `Ref` を含む。helper `TypeDecl::contains_ref()` で再帰判定 (Array/Tuple/Struct/Enum/Dict/Range/Ref を walk)。method 引数 `&T` と receiver `&self` / `&mut self` は引き続き許可。test `test_function_returning_ref_is_rejected` + `test_val_binding_of_ref_is_rejected` + `test_struct_field_of_ref_is_rejected` で 3 escape パスを pin。1155 → 1158 tests pass。**REF-Stage-2 (b)+(c)+(g)+(i) scalar minimal 完了 (2026-05-04)**: スカラ `&mut T` parameter のエンドツーエンド真の mutation 伝播 (3-way: AOT pointer-passing + interpreter / JIT post-call writeback)。**IR**: `InstKind::AddressOf { local }` / `LoadRef { ptr, ty }` / `StoreRef { ptr, value, ty }` の 3 variant + `Function.address_taken_locals: HashSet<LocalId>` を追加。**codegen**: address-taken local ごとに cranelift `StackSlot` (size = pointee bytes) を allocate、`LoadLocal` / `StoreLocal` を `stack_load` / `stack_store` 経由に dispatch、`AddressOf` で `stack_addr`、`LoadRef` で `load.<ty>`、`StoreRef` で `store.<ty>` emit。entry block の param-binding でも address-taken パラメータは `def_var` ではなく `stack_store` で slot に書込む。**lowering**: 新 `Binding::RefScalar { local, pointee_ty, is_mut }` variant を追加 — local は U64 (pointer-sized handle) を保持。`templates::lower_param_or_return_type` を拡張してスカラ `&T` / `&mut T` param を `Type::U64` に lower (compound &T は従来の leaf-flatten erasure を維持)。`lower_body` の param binding ループで `TypeDecl::Ref { inner: <scalar> }` を検出し `Binding::RefScalar` を作る。`Expr::Identifier` 読みは `LoadLocal` + `LoadRef`、`Expr::Assign(Identifier, ...)` は (is_mut チェック後に) `LoadLocal` + `StoreRef` を emit。`lower_call_args` で `Expr::Unary(Borrow|BorrowMut, Identifier(sym))` を検出し scalar `Binding::Scalar` なら local を `address_taken_locals` に登録 + `AddressOf` を emit、`Binding::RefScalar` なら pointer をそのまま forward。`MatchScrutinee` も RefScalar に対応 (auto-deref 後に scalar match path)。**type checker**: `visit_identifier` で Ref binding を auto-dereference (value position だけ — method dispatch / explicit borrow は別 path)。`visit_function` の param 登録で `Ref { is_mut: true, .. }` は `set_mutable_var` を使う (body の `x = ...` 代入を許可)。**interpreter**: `Environment::set_val_mutable` を新設、`evaluate_function_with_values` で `&mut T` param を mutable bind (body は走るが mutation propagation は AOT 限定 — interpreter の by-value semantics 上、Rc<RefCell<>> 経由の shared cell 化は別 phase)。**test** (`compiler/tests/consistency.rs::ref_stage2_scalar_mut_ref_propagates_mutation_round_trip`): `fn inc(x: &mut u64) { x = x + 1u64 }` + `var n = 41u64; inc(&mut n); inc(&mut n); n - 1u64` (= 42) を 3-way `assert_consistent` で interpreter / JIT silent fallback / AOT 全 backend pass。**(i) interpreter writeback**: `evaluate_function_with_values_writeback` で post-body `&mut T` param 値を snapshot、`evaluate_function_call` が pre-eval で `Unary(BorrowMut, Identifier(name))` arg を捕捉し call 完了後に caller binding へ書き戻し。JIT は silent fallback で同じ writeback 経路に流れ込む。**残**: (ii) struct / tuple / enum `&mut T` true pointer (compound 型は現在 leaf-flatten erasure)、(iii) field-level borrow (`&mut s.field`) / index borrow (`&mut arr[i]`) — 別の lvalue 型サポート、(iv) `&T` (immutable) でも RefScalar 経路を活用して copy 削減。優先度: 低〜中。

65. **frontend の改善課題** — docコメント拡充、プロパティベーステスト追加、コード重複削減
26. **ドキュメント整備** — 言語仕様 / API ドキュメント (`docs/language.md` は最新化済み、`compiler/README.md` / `interpreter/README.md` も追従済み。残: API リファレンス、advanced topics)
183. **コンパイラの作成（MVP + IR + panic/assert + print/println + struct + cast/f64 + struct boundary + tuple + const + DbC + --release + nested fields + tuple boundary + 3 経路一致テスト 対応・段階的進行中）** — toylang のソースを実行可能バイナリにコンパイルする独立コンポーネントを新設する。

   **2026-05-01: 3 経路一致テストに JIT を追加** — `compiler/tests/consistency.rs` が interpreter (lib API) / compiler (subprocess) / **JIT (interpreter binary spawn with INTERPRETER_JIT=1)** の 3 経路で同一 exit code を保証するように拡張。`OnceLock` で interpreter binary を一度だけビルドしてキャッシュ、各テストで `Command::new(bin).env("INTERPRETER_JIT", "1").arg(src)` で spawn。3-way assertion は interp vs compiler / interp vs jit を別々に diff 表示するため、どのペアがズレたか明示できる。新機能カバレッジ向上のため struct / tuple / const / DbC のケースも追加（計 14 テスト 全 green）。**発見した既知差分**: u64 underflow 時、interpreter は overflow panic、compiler / JIT は wrap。consistency テストでは underflow を起こさないオペランドを選んで意図しないドリフトのみを catch する設計に。

   **2026-05-01: tuple cross-boundary を追加** — `TupleId(u32)` newtype と `Type::Tuple(TupleId)` を IR に追加、`Module.tuple_defs: Vec<Vec<Type>>` で tuple shape を intern（linear-search dedup）。`InstKind::CallTuple { target, args, dests }` を `CallStruct` と並列で追加。`lower_param_or_return_type` が `TypeDecl::Tuple(elements)` を受理（scalar element 限定）し `intern_tuple` で TupleId を取得。`allocate_tuple_elements` で tuple param を per-element local に展開、`lower_tuple_literal_tail` で tail-position tuple literal を `pending_tuple_value` に貯めて implicit return が消費。`lower_let` の Call 検知が `CallTuple` も emit、explicit `return p` が tuple binding 経由 / tuple literal 経由で expand。codegen の `flatten_struct_to_cranelift_tys` を `Type::Tuple` も再帰展開するよう拡張、`InstKind::CallTuple` を multi-result call として lower。e2e テスト 4 件追加（tuple return / tuple param / round trip / call-into-destructure）、計 49 テスト全 green。

   **2026-05-01: --release ゲート と ネストした struct field を追加** — `CompilerOptions.release: bool` を追加、`--release` 指定で `lower.rs` が requires/ensures 検査を一切 emit しない（interpreter の `INTERPRETER_CONTRACTS=off` と同等）。ネストした struct: `FieldBinding` を `{ name, shape: FieldShape }` に refactor、`FieldShape` enum で `Scalar { local, ty }` と `Struct { struct_name, fields }` を表現。`collect_struct_defs` は struct field type も accept、`allocate_struct_fields` ヘルパーで再帰的に local 展開、`store_struct_literal_fields` ヘルパーで入れ子リテラル `Outer { inner: Inner { x: 1 } }` を再帰 store。`a.b.c` chain access は `resolve_field_chain` で walk、`FieldChainResult::Scalar` / `Struct` を返す。codegen の `flatten_struct_to_cranelift_tys` で nested struct param/return を leaf scalar まで再帰展開。e2e テスト 3 件追加（release skip 検証 / nested field read+write / nested struct param）、計 45 テスト全 green。

   **2026-05-01: tuple / top-level const / DbC を追加** — tuple は struct と同じ「per-element ローカル展開」パターンで `Binding::Tuple { elements }` を新設、`Expr::TupleLiteral` を val rhs として allocate、`Expr::TupleAccess` の読み書きを実装。tuple は局所バインディング限定（関数引数 / 戻り値は未対応）、`val (a, b) = (x, y)` のパーサ desugar が自然に動く。const は `evaluate_consts` で program.consts を compile-time fold（リテラル + 既存 const 参照 + arithmetic）し、Identifier 解決時に local binding が無ければ const テーブルにフォールバック。DbC は `lib.rs::ContractMessages` で "requires violation" / "ensures violation" を pre-intern して `&mut interner` 問題を回避、`emit_contract_checks` で各 clause を bool 評価 → 失敗時 `Terminator::Panic` に分岐、`requires` は entry で / `ensures` は全 Return 直前で発火。`ensures` の `result` は scalar 戻り値（struct は first field）に bind。e2e テスト 9 件追加（tuple 4 + const 2 + DbC 3）、計 42 テスト全 green。

   **2026-05-02: lower.rs 分割 refactor 完了 (Phase Z8 — Z20)** — Phase Z 残り 13 ラウンドを一気に実施し、`compiler/src/lower.rs` 8116 行モノリスを **24 ファイル / mod.rs 265 行** (約 **97% 縮小**) まで分割。`FunctionLower` struct 定義 + IR builder primitive (fresh_value / fresh_block / emit / terminate / switch_to / is_unreachable / value_ir_type_for) のみ mod.rs に残し、残りはすべて topical sub-module へ。各サブモジュールは `impl<'a> super::FunctionLower<'a> { ... }` で同一 struct にメソッドを追加し、`pub(super)` で他モジュールから呼べるようにすることで semantics 不変。
   - **Z8 `compound_storage.rs` (914 行)**: enum / struct / tuple の "pre-allocated storage に書き込む" 系 19 メソッド (`allocate_enum_storage` / `allocate_payload_slot` / `bind_enum` / `write_variant_into_storage` / `load_enum_locals[_into]` / `flatten_enum_dests[_into]` / `detect_enum_result` / `lower_into_enum_storage` / `write_enum_into_target` / `lower_if_chain_into_enum` / `lower_match_into_enum` / `copy_enum_storage` / `copy_struct_fields` / `lower_into_struct_slot` / `copy_tuple_elements` / `store_value_into_tuple_element_shape` / `lower_into_tuple_slot`)。enum payload に struct/tuple がネストする相互再帰なので一括同居。
   - **Z9 `call.rs` (344 行)**: function call lowering 5 メソッド (`resolve_call_target` / `infer_generic_args_from_param` / `instantiate_generic_function` / `lower_type_with_subst` / `lower_call`)。
   - **Z10 `match_lowering.rs` (567 行)**: `match` 式 lowering 6 メソッド (`lower_match` / `arm_body_type` / `dispatch_enum_variant_pattern` / `apply_arm_pattern_bindings_for_inference` / `classify_match_scrutinee` / `emit_literal_eq_branch`)。
   - **Z11 `field_access.rs` (265 行)**: `obj.field` / `obj.0` 読み + chain 解決 4 メソッド (`lower_field_access` / `resolve_field_chain` / `resolve_tuple_element_local` / `resolve_field_local`)。
   - **Z12 `compound_literal.rs` (480 行)**: struct / tuple リテラル allocation + tuple-access 9 メソッド (`store_struct_literal_fields` / `allocate_struct_fields` / `allocate_tuple_elements` / `infer_tuple_element_type` / `allocate_tuple_element_shape` / `resolve_tuple_chain_elements` / `lower_tuple_access` / `lower_struct_literal_tail` / `lower_tuple_literal_tail`)。
   - **Z13 `expr_ops.rs` (265 行)**: 算術 / 比較 / 短絡 / 単項 / if-chain 4 メソッド (`lower_binary` / `lower_short_circuit` / `lower_unary` / `lower_if_chain`)。
   - **Z14 `type_resolution.rs` (355 行)**: instance + 型引数解決 6 メソッド (`resolve_struct_instance` / `extract_struct_type_args` / `resolve_enum_instance` / `resolve_enum_instance_with_args` / `extract_enum_type_args` / `lower_type_arg`)。
   - **Z15 `assign.rs` (168 行)**: `lower_cast` / `expect_string_literal` / `lower_assign` の 3 メソッド。
   - **Z16 `let_lowering.rs` (631 行・最大)**: `lower_let` 単独。binding shape (Scalar / Struct / Tuple / Enum / Array / range slice) を rhs から決定して該当 storage に評価。
   - **Z17 `loops.rs` (141 行)**: `lower_while` / `lower_for`。
   - **Z18 `stmt.rs` (289 行)**: `lower_stmt` per-statement dispatcher。
   - **Z19 `expr.rs` (285 行)**: `lower_call_args` / `lower_expr` / `lower_builtin_call` の expression 系 dispatcher 3 メソッド。
   - **Z20 `program.rs` (734 行・最終)**: top-level driver `pub fn lower_program` + per-function bootstrap (`new` / `lower_method_body` / `lower_body` / `emit_implicit_return` / `emit_contract_checks` / `emit_ensures_checks`) + 関連 type alias (`GenericFuncs` / `GenericInstances` / `PendingGenericInstance`)。`mod.rs` から `pub use program::lower_program` で外部 API は無変更。

   **最終構成**: consts (104) / array_layout (84) / types (35) / method_registry (75) / templates (574) / bindings (221) / type_inference (196) / method_call (421) / print (795) / array_access (442) / compound_storage (914) / call (344) / match_lowering (567) / field_access (265) / compound_literal (480) / expr_ops (265) / type_resolution (355) / assign (168) / let_lowering (631) / loops (141) / stmt (289) / expr (285) / program (734) / mod (265)。各ラウンドで `cargo nextest run` -> **1179 passed, 31 skipped** を維持、semantic 不変。後続作業の足がかりに、各サブモジュール冒頭に責務説明の module doc を付与。

   **2026-05-01: lower.rs 分割 refactor (Phase Z7, array access)** — `compiler/src/lower/array_access.rs` (442 行) に array リテラル / index access / SliceAccess(SingleElement) lowering を独立 impl ブロックで切り出し: `infer_array_element_type` (要素型推論)、`store_array_element` (per-leaf ArrayStore で要素を書き込む)、`lower_slice_access` (SingleElement read; struct/tuple element も対応)、`lower_slice_assign` (SingleElement write)、`try_constant_index` (literal/const index folding)。range slice (`arr[start..end]`) の lowering は `lower_let` 内に残っており、これは後で `lower_let` 自体を別ファイルに切り出す phase で一緒に移動予定。`mod.rs` は ~5669 行 → ~5258 行 (約 410 行削減)。フル e2e 159 件 + consistency 23 件全 green。

   **2026-05-01: lower.rs 分割 refactor (Phase Z6, print)** — `compiler/src/lower/print.rs` (795 行) に print/format 系を独立 impl ブロックで切り出し: `lower_print` (本体)、`emit_print_struct` / `emit_print_tuple` / `emit_print_array` / `emit_print_enum` / `emit_print_enum_variant_body` / `emit_print_raw_text`、ジェネリック型引数表示用の `format_struct_header` / `format_enum_header` / `format_type_for_display`。`mod.rs` は ~6440 行 → ~5669 行 (約 770 行削減)。フル e2e 159 件 + consistency 23 件全 green。

   **2026-05-01: lower.rs 分割 refactor (Phase Z5, method call)** — `compiler/src/lower/method_call.rs` (421 行) に method-call 関連の impl を独立ブロックで切り出し: `peek_method_return_type` (R3 用 &self peek)、`lower_method_param_type` (Self/Generic substitution)、`instantiate_generic_method_with_args` (Phase R3 + X モノモル化)、`bind_method_only_param` (X arg-type inference)、`resolve_method_target` (Phase W 用 peek)、`lower_method_call` (call site lowerer)。`mod.rs` は ~6862 行 → ~6440 行 (約 420 行削減)。フル e2e 159 件 + consistency 23 件全 green。

   **2026-05-01: lower.rs 分割 refactor (Phase Z4, value_scalar)** — `compiler/src/lower/type_inference.rs` (196 行) に `value_scalar` 系の構造的型推論を独立 impl ブロックで切り出し。Rust では `impl<'a> super::FunctionLower<'a> { ... }` を別ファイルに置けるので公開 API を変えずに分割可能。`value_scalar` は `&self` のみで、`Expr::*` の各ケースを peek して IR の `Type` を返す。MethodCall は generic method (impl-level + method-only) の subst も組み立てて `peek_method_return_type` を呼ぶ。`mod.rs` は ~7059 行 → ~6862 行 (約 200 行削減)。フル e2e 159 件 + consistency 23 件全 green。

   **2026-05-01: lower.rs 分割 refactor (Phase Z3, binding shapes)** — `compiler/src/lower/bindings.rs` (221 行) に binding shape 関連を一括移動: `Binding` (Scalar/Struct/Tuple/Enum/Array)、`EnumStorage`、`PayloadSlot` (Scalar/Enum/Struct/Tuple)、`TupleElementBinding` + `TupleElementShape`、`FieldBinding` + `FieldShape`、`FieldChainResult`、`MatchScrutinee`、両 flatten ヘルパー (`flatten_struct_locals`, `flatten_tuple_element_locals`)。flatten 系は元々 `FunctionLower` の inherent fn だったが `&self` を持たないので free function に格下げ、全 call site (10 箇所) を `Self::flatten_struct_locals` → `flatten_struct_locals` に sed 置換。`mod.rs` は ~7265 行 → ~7059 行 (約 200 行削減)。フル e2e 159 件 + consistency 23 件全 green (semantics 不変)。

   **2026-05-01: lower.rs 分割 refactor (Phase Z2, struct/enum templates)** — `compiler/src/lower/templates.rs` (574 行) に struct / enum template まわりを一括移動: `StructDefs` / `EnumDefs` (型 alias)、`StructTemplate` / `EnumTemplate` / `EnumTemplateVariant` (template structs)、`collect_struct_defs` / `collect_enum_defs` (program walker)、`instantiate_struct` / `instantiate_enum` (monomorphisation)、`substitute_field_type` / `substitute_payload_type` / `is_supported_enum_payload` (再帰的 type substitution)、`lower_param_or_return_type` (boundary lowerer)。これらは互いに mutual recursion するので 1 ファイルにまとめた (公開可視性は `pub(super)`)。mod.rs から `use templates::{...}` で必要な item だけを re-import。`mod.rs` は ~7868 行 → ~7265 行 (約 600 行削減)。フル e2e 159 件 + consistency 23 件全 green (semantics 不変)。

   **2026-05-01: lower.rs 分割 refactor (Phase Z, 第1ラウンド)** — 8000+ 行に膨れ上がった `compiler/src/lower.rs` を `compiler/src/lower/` ディレクトリ + 複数モジュールに分割し始めた (semantics 不変、refactor のみ)。第1ラウンドで以下の関心事を切り出し: `consts.rs` (top-level const evaluation: ConstValues, evaluate_consts, eval_const_expr, const_fold_binop)、`array_layout.rs` (ARRAY_LEAF_STRIDE, leaf_scalar_count, elem_stride_bytes, leaf_type_at)、`method_registry.rs` (MethodRegistry, GenericMethods, MethodInstances, PendingMethodInstance, collect_method_decls)、`types.rs` (lower_scalar, intern_tuple)。mod.rs は依然 ~7800 行と巨大だが、関連性ごとの初期切り分けの土台ができた。フル e2e 159 件 + consistency 23 件全 green (no functional change)。**残作業**: struct/enum 全体 (collect/instantiate/substitute 系)、FunctionLower の巨大 impl ブロックを method_call / array / enum / struct 等のサブモジュール impl に分けて行く。

   **2026-05-01: 配列要素に tuple (Phase Y3)** — Phase Y2 (struct 要素 + range slicing) を tuple 要素まで拡張。`[(1, 2), (3, 4)]` リテラルを `store_array_element` の `Type::Tuple(_)` アームで `allocate_tuple_elements` + `store_value_into_tuple_element_shape` 経由で leaf scalar に decompose、`flatten_tuple_element_locals` で順に per-leaf ArrayStore。`lower_slice_access` の compound element パスを `Type::Struct(_) | Type::Tuple(_)` 統合し、tuple なら `pending_tuple_value` に stash。`lower_let` の SliceAccess アームも Tuple element 対応で `Binding::Tuple` を作成。e2e 2 件追加 (tuple element const index / runtime index in for-loop)。フル e2e 159 件 + consistency 23 件全 green。**残課題**: enum 要素 (PayloadSlot ベースのストレージが必要)、variable-bound range slicing (動的長さ配列が必要)。

   **2026-05-01: 配列要素に struct + range slicing (Phase Y2)** — 配列要素に compound 型 (struct) を許可し、`arr[start..end]` の range slicing も const-bound でサポート。**leaf-index addressing**: 1 要素あたり `leaf_scalar_count(elem_ty)` 個の連続する 8 バイト leaf スロットを占有、ArraySlot length は `num_elements * leaf_count`。`elem_stride_bytes` は ARRAY_LEAF_STRIDE (=8) で固定し、stride を超える offset 計算は lowering 側で `i * leaf_count + j` を index として組み立てる。新ヘルパ `leaf_scalar_count` / `leaf_type_at` / `infer_array_element_type` / `store_array_element` を追加。`lower_let` の ArrayLiteral アームが element 型を `infer_tuple_element_type` 経由で受け取り、struct 型なら `store_array_element` 経由で leaf を per-leaf ArrayStore で書き込む。`lower_slice_access` は struct element を検出すると fresh `Binding::Struct` を allocate、各 leaf を ArrayLoad で読んで対応する local に store、結果は `pending_struct_value` 経由で chain access に流れる (`val p: Point = arr[i]; p.x`)。`lower_let` の SliceAccess (SingleElement) アームが pending_struct_value を取り出して `Binding::Struct` として bind。`lower_let` の SliceAccess (RangeSlice) アームが両端 const をフォールド、新 ArraySlot を確保して `for i in 0..new_len for j in 0..leaf_count` で ArrayLoad+ArrayStore 列を emit。defaults は interpreter と同じ (`..end` で start=0, `start..` で end=length, `..` で全範囲)。e2e 3 件追加 (struct 要素 const index / 同 runtime index / range slice 4 形)。フル e2e 157 件 + consistency 23 件全 green。**残課題**: tuple / enum 要素、variable-bound range slicing。

   **2026-05-01: 配列 runtime index (Phase Y)** — Phase S の per-element local 表現を `ArraySlotId` ベースの stack slot に refactor。IR に `ArraySlotId` / `ArraySlotInfo { element_ty, length, elem_stride_bytes }` / `Function.array_slots` / 新 InstKind `ArrayLoad { slot, index, elem_ty }` / `ArrayStore { slot, index, value, elem_ty }` を追加。codegen は per-IR-slot で cranelift `StackSlot` (length × stride バイト) を `create_sized_stack_slot` で確保、`stack_slots: HashMap<u32, StackSlot>` に保持。Load/Store は `iadd(stack_addr(slot, 0), idx * stride)` + `load.<elem_ty>` / `store.<elem_ty>` で実装。stride は scalar 型統一 8 バイト (bool は 1 バイトだが slot で padding)。`Binding::Array` を `{element_ty, length, slot}` に変更、`lower_let` ArrayLiteral アームと `lower_slice_access` / `lower_slice_assign` / `emit_print_array` をすべて新 IR 命令に切替。const index は `try_constant_index` でフォールド + Const::U64 emit、runtime index は `lower_expr` で値生成 — 両者とも同じ ArrayLoad/Store に流れ込む (cranelift の最適化が const offset を畳み込む)。これで for ループ等で `arr[i]` (i は変数) が使える。e2e 2 件追加 (for-loop sum / runtime write)。フル e2e 154 件 + consistency 23 件全 green。**残課題**: 配列要素に compound 型 (struct/tuple/enum)、range slicing は frontend 拡張が必要。

   **2026-05-01: method-only generic params (Phase X)** — `impl Box { fn pick<U>(self, a: U, b: U) -> U }` のように impl の generic params とは独立した method 自身の generic params を実装。**frontend** parser/stmt.rs の impl method 解析が従来 `skip_until_matching_gt` で `<U>` を捨てていたのを `parse_generic_params()` で実際に parse するよう変更、impl-level + method-only を combined_generic_params にまとめて parameter / return-type の generic_context に渡す。**type-checker** method_call.rs の non-generic-struct 分岐に method.generic_params を見て arg types から subst を組み立てるロジックを追加 (新ヘルパ `collect_substitution`)、return type に Generic(P) があれば置換。**compiler** `instantiate_generic_method` を `instantiate_generic_method_with_args` に置換: receiver type_args は impl-level params (positional) を埋め、残りを arg types から推論 (新ヘルパ `bind_method_only_param`)。`(target, method, full_type_args)` cache key で dedup。`value_scalar` の MethodCall アームと `peek_method_return_type` も method-only path に対応。`resolve_method_target` が args を受け取るように。e2e 2 件追加 (Box::pick&lt;U&gt; 単発 / 同 method を i64/u64/bool で 3 通り instantiate)。フル e2e 152 件 + consistency 23 件 + frontend / interpreter test 全 green。

   **2026-05-01: compound-returning method を val/var rhs に (Phase W)** — Phase R / R3 / U で残っていた「compound 戻り値の method を `val` で受けられない」制約を解消。`lower_let` に Expr::MethodCall + compound return type 検出パスを追加 (Tuple/Enum/Struct 検出を既存の Call branch と同じ場所に配置)。新ヘルパ `resolve_method_target(obj, method) -> Option<(FuncId, Binding)>` を抽出し、receiver の binding (struct / enum) を見て (target_sym, method) で `method_func_ids` を引く。ヒットしなければ generic_methods + `instantiate_generic_method` でモノモル化。compound return であれば適切な binding shape を allocate (`allocate_struct_fields` / `allocate_tuple_elements` / `allocate_enum_storage`)、leaf scalar dest 列を flatten、receiver の leaf scalars + method args を組み立てて CallStruct / CallTuple / CallEnum で multi-result call を emit。`lower_method_call` 自体の rejection は維持 (任意の expression position はまだ未対応)。e2e 3 件追加 (Pair&lt;T&gt;::swap struct return / Holder::maybe enum Option&lt;i64&gt; return / Counter::pair tuple return)。フル e2e 150 件 + consistency 23 件全 green。

   **2026-05-01: consistency stdout 比較 (Phase V)** — `compiler/tests/consistency.rs` に 3-way stdout 比較ヘルパを追加。`interpreter_stdout` / `jit_stdout` / `compiler_stdout` がそれぞれ interpreter binary (in-process) / `INTERPRETER_JIT=1` 付き interpreter binary / AOT-compiled exe を spawn して stdout バイト列を取得。`assert_stdout_consistent` が 3 者を比較し、divergence があれば全文ダンプして失敗。`assert_consistent` (exit code 比較) は維持。print formatting drift (interpreter generic print の divergence のような) を CI で検出可能に。テスト 7 件追加 (scalar / str / struct / enum / generic struct / generic enum / loop)。フル consistency 23 件 + e2e 147 件全 green。

   **2026-05-01: 関数戻り値 compound 値の直接 print (Phase U)** — `println(make_point())` / `println(p.doubled())` を許可。`lower_print` の compound-literal ショートカット (Phase P) に Expr::Call と Expr::MethodCall アームを追加: callee / method の戻り型 (Type::Struct / Tuple / Enum) を見て scratch binding を allocate (allocate_struct_fields / allocate_tuple_elements / allocate_enum_storage)、leaf scalar dest 列を `flatten_struct_locals` / `flatten_tuple_element_locals` / `flatten_enum_dests` で平坦化、既存の CallStruct / CallTuple / CallEnum で multi-result call を emit、結果は scratch binding 経由で `emit_print_struct` / `emit_print_tuple` / `emit_print_enum` に routed。MethodCall の場合は receiver scalar 列を first args に prepend (Phase R の `lower_method_call` と同じ展開)。MethodCall の本体 `lower_method_call` 自身は引き続き compound 戻り値を reject (val 経由必須) — 一般化は別 phase。e2e 4 件追加 (struct call / tuple call / enum call / struct-returning method)。フル e2e 147 件 + consistency 16 件全 green。

   **2026-05-01: 文字列値 (Phase T)** — compiler が `str` 型を value として扱えるよう拡張。IR に `Type::Str` (pointer-sized opaque handle) と `InstKind::ConstStr { message }` を追加。`lower_scalar` に `TypeDecl::String → Type::Str`、boundary lower (param / return / struct field / tuple element) も自動で受理。codegen は `ir_to_cranelift_ty` で `IrType::Str → I64`、`Print { ty: Str }` で `toy_print_str` / `toy_println_str` に dispatch、`ConstStr` で `symbol_value` を `.rodata` の文字列 blob に出して値を materialize。`declare_print_imports` と上流の `print_needed` 収集パスも `ConstStr` を含むよう拡張 (PrintStr と同じ blob を共有)。`Expr::String(sym)` が value 位置で `ConstStr` を emit、`value_scalar` も `Expr::String → Type::Str` を返す。これで `val s = "hello"` / `fn greet(s: str)` / `fn pick(b: bool) -> str { ... }` / `struct Greeting { msg: str }` がすべて動作。e2e 4 件追加 (val binding / fn arg / fn return / struct field)。フル e2e 143 件 + consistency 16 件全 green。**残課題**: 文字列連結 (`s.concat(...)`) / 比較 / `.len()` などの runtime ヘルパ未対応 (interpreter のみ)。

   **2026-05-01: 配列リテラル + 定数 index (Phase S)** — compiler に Expr::ArrayLiteral と Expr::SliceAccess (SingleElement) / Expr::SliceAssign を実装。`Binding::Array { element_ty: Type, elements: Vec<LocalId> }` を新設し、要素ごとの per-index local で固定長配列を表現。`val arr = [a, b, c]` は要素数だけ local を allocate して順次 store。`arr[const_idx]` の read は `try_constant_index` (literal Int64/UInt64 + top-level const のフォールド) で index を解決し直接 LoadLocal を emit、`arr[const_idx] = v` は同じく StoreLocal。`emit_print_array` は `[1, 2, 3]` 形式 (interpreter の `Object::Array` 出力と一致)。`value_scalar` の SliceAccess アームで element_ty を peek し、val annotation 不要に。要素は scalar (i64/u64/f64/bool) のみ受理、runtime index と range slicing は明示エラーで reject。e2e 3 件追加 (read / print / element write)。フル e2e 139 件 + consistency 16 件全 green。**残課題**: `stack_addr` + cranelift `stack_load`/`stack_store` ベースの runtime indexing、range slicing、要素に compound 型。

   **2026-05-01: method-level generics (Phase R3)** — `impl<T> Cell<T> { fn get(self: Self) -> T }` のような generic method を lazy monomorphisation で対応。Phase L (generic 関数) と同型: generic_methods (template) + method_instances ((target, method, type_args) → FuncId) + pending_method_work queue を新設し、`lower_program` で関数 queue と methods queue を交互に drain。call site (lower_method_call) は (target, method) が method_func_ids にヒットしなければ generic_methods を試す。`instantiate_generic_method` が receiver の struct_def.type_args から impl の generic_params を positional で bind、`Self` は受信側の `Type::Struct(recv_struct_id)` に解決、新しい FuncId を declare して queue に push。新ヘルパ `lower_method_param_type` (mut self) と `peek_method_return_type` (&self) で param/return 型の substitution を実装。`value_scalar` の MethodCall アームも generic methods 経由の戻り型を peek できるように拡張。e2e 2 件追加 (Cell&lt;i64&gt; 単発 / Cell&lt;i64&gt; + Cell&lt;u64&gt; 共存)。フル e2e 136 件 + consistency 16 件全 green。**残課題**: method-level only の generic param (`fn m<U>` で U が impl の T と無関係)、method-self generic から compound 型を return するパス。

   **2026-05-01: method dispatch in compiler (Phase R)** — `impl` ブロックの inherent / trait method 呼び出しを compiler に実装。`collect_method_decls` で program 中の全 `Stmt::ImplBlock` を pre-scan し `MethodRegistry: HashMap<(target_sym, method_sym), Rc<MethodFunction>>` を構築。各 method を `module.declare_function_anon` (新規追加、`function_index` を汚さない関数登録 helper) で mangled name `toy_<Type>__<method>` で IR に declare、`(target, method) -> FuncId` を別 map に保持。`self: Self` は `TypeDecl::Self_` として parse されているので impl の target struct symbol に substitute (param と return type 両方)。method body の lowering は `lower_method_body` ヘルパが MethodFunction → 合成 Function を作り既存の `lower_body` を再利用 (param に Self 置換のみ加える)。call site は新 `Expr::MethodCall(obj, method, args)` の lower で receiver identifier を Binding::Struct/Enum に解決し、(struct_def の base_name, method_sym) から FuncId を引いて receiver の leaf scalar 列を call args 先頭に prepend (`flatten_struct_locals` / `load_enum_locals` 利用)。`value_scalar` の MethodCall アームも追加し、val annotation 不要で戻り値型を推論。FunctionLower に method_registry / method_func_ids を持たせて呼び出し時にアクセス。`<T: Trait>` 経由の generic method 呼び出しは Phase L (関数モノモル化) との組み合わせで自動的に動作: instantiate 時に T が具体型に解決されるので receiver の binding に正しい struct_id が入り、Phase R の通常 dispatch 経路で resolve される (vtable 不要)。e2e 3 件追加 (inherent method / trait impl method / `<T: Greet>` 経由の announce). フル e2e 134 件 + consistency 16 件全 green。**制約**: method-level generics (`fn m<U>()` 内の generic)、dynamic `dyn Trait` は未対応 (frontend も同じく未対応)。

   **2026-05-01: interpreter print に generic 型引数を含める** — compiler は `Y<i64> { b: 2 }` / `Option<i64>::Some(5)` のように generic 型引数を出力するが、interpreter は `Y { b: 2 }` / `Option::Some(5)` と落としていた。Object::Struct と Object::EnumVariant に `type_args: Vec<TypeDecl>` を追加。`EvaluationContext` に `EnumRegistryEntry` (generic_params + variants の payload_types) と `StructRegistryEntry` (generic_params + 各 field の type_decl) を新設し、lib.rs での EnumDecl/StructDecl 登録時に payload_types / field type を保存。`derive_struct_type_args` / `derive_enum_type_args` ヘルパが構築時に `Generic(P)` 位置にあるフィールド/payload と runtime value を突き合わせて T を推論 (`collect_generic_bindings` で再帰)。Object::Struct / Object::EnumVariant の display は `format_type_header` 経由で `Name<T1, T2>` 形式を出力。`apply_annotation_type_args` を `handle_val_declaration` / `handle_var_declaration` に挟み、`val n: Option<i64> = Option::None` のような unit variant に annotation 由来の type_args を注入。e2e テスト 3 件 (struct value 推論 / enum value 推論 / unit variant annotation 注入) 追加。`PartialEq` / `Hash` は type_args を無視 (値同値性は data 由来)。フル interpreter / frontend / compiler テストすべて green。

   **2026-05-01: ネスト tuple + tuple-of-struct (Phase Q2)** — `TupleElementBinding` を `{index, shape: TupleElementShape}` に refactor (`shape` に Scalar/Struct/Tuple の 3 variant)。`allocate_tuple_elements` を再帰化 (compound 要素は `allocate_struct_fields` / 自分自身を呼ぶ)。`copy_tuple_elements` / `lower_into_tuple_slot` / `emit_print_tuple` も shape-aware に refactor。新たに `flatten_tuple_element_locals` (free function) を追加し、関数 boundary 関連の 5 箇所 (Return, identifier-arg expansion, CallTuple dest 列等) で leaf scalar 順に展開。`infer_tuple_element_type` ヘルパ (`&mut self`、新規 tuple shape を `intern_tuple` で intern 可) を追加し、`val t = ((1, 2), 3)` のように nested 形を annotation 無しで受理。`resolve_tuple_chain_elements` と `resolve_field_chain` 拡張により `t.0.1` / `t.0.x` / `outer.inner.0` のような mixed chain アクセスが動作。`lower_field_access` / `lower_tuple_access` の Tuple/Struct/Scalar 各 shape 分岐、`value_scalar` の TupleAccess アームも refactor。e2e テスト 4 件追加 (nested tuple read+print / tuple-of-struct read+print)。フル e2e 131 件 + consistency 16 件 + frontend / interpreter 全 green。これで struct field と tuple element の compound 型制約はすべて解消。

   **2026-05-01: struct field に tuple 対応 (Phase Q1)** — `FieldShape` に第3 variant `Tuple { tuple_id, elements: Vec<TupleElementBinding> }` を追加 (既存の `Binding::Tuple` と同じ element 列を持つ)。`substitute_field_type` に `TypeDecl::Tuple` アームを追加 (要素 scalar のみ受理、`intern_tuple` で `Type::Tuple(id)` を返す)。frontend `struct_literal.rs` の field-type validation にも `TypeDecl::Tuple` アームを追加。`allocate_struct_fields` が Tuple field type に対し `allocate_tuple_elements` を呼んで per-element local を展開、`flatten_struct_locals` が Tuple 要素を順次 push (関数境界の leaf scalar 順序を維持)、`store_struct_literal_fields` が Tuple field rhs として `Expr::TupleLiteral` を受理して各要素を store、`emit_print_struct` が Tuple field を `emit_print_tuple` 経由で出力。新たに `FieldChainResult::Tuple { elements }` を追加し、`resolve_field_chain` が tuple field に降りた際に Tuple result を返す。`lower_field_access` は Tuple field を pending tuple value に stash (tail-position `outer.inner` 用)。`lower_tuple_access` を拡張して `Expr::FieldAccess` 形の obj (`outer.inner.0`) も resolve_field_chain 経由で要素列を取り出す。`value_scalar` の FieldAccess / TupleAccess アームも Tuple 経路に対応。`resolve_field_local` は struct field に tuple がある場合は明示エラー (`obj.inner.N` で個別 element 代入する旨を案内)。e2e テスト 2 件追加 (struct field tuple read + print / struct-of-tuple を fn param/return に渡す)。フル e2e 127 件 + consistency 16 件 + frontend / interpreter 全 green。**残作業 (Phase Q2)**: ネストした tuple 要素 (`((a, b), c)`) と tuple 要素に struct (`(Point, i64)`) は別途 `TupleElementBinding` に shape 化が必要。

   **2026-05-01: print に struct/tuple/enum リテラル直接対応 (Phase P)** — `lower_print` を拡張: 既存の identifier-binding ショートカット直後に compound-literal 4 ケース (Expr::StructLiteral / Expr::TupleLiteral / Expr::QualifiedIdentifier の unit enum / Expr::AssociatedFunctionCall の tuple enum variant) を追加。それぞれ `resolve_struct_instance` / `resolve_enum_instance` / `resolve_enum_instance_with_args` で型 ID 解決 → scratch local 群を allocate → 値を構築 → 既存の `emit_print_struct` / `emit_print_tuple` / `emit_print_enum` 経由で出力。これで `println(Point { x: 3, y: 4 })` / `println((10, 20, 30))` / `println(Color::Red)` / `println(Shape::Circle(5))` のような書き方が可能に。`val` で受ける必要なし。e2e テスト 4 件追加 (struct リテラル / tuple リテラル / unit enum リテラル / tuple enum リテラル)。フル e2e 125 件 + consistency 16 件全 green。**残る制約**: 関数戻り値 compound や generic struct/enum リテラル (型引数推論手段なし) は依然 `val` で受ける必要あり。

   **2026-05-01: enum payload に tuple (Phase O)** — Phase G→N の payload 拡張系列の最終ピース。`PayloadSlot` に第4 variant `Tuple { tuple_id, elements: Vec<TupleElementBinding> }` を追加（既存の `Binding::Tuple` と同じ element 列を持つ）、`is_supported_enum_payload` に `Type::Tuple(_)` を追加。`allocate_payload_slot` が tuple slot を element 数ぶんの per-element local として展開。`load_enum_locals_into` / `flatten_enum_dests_into` / `copy_enum_storage` / `write_variant_into_storage` / `emit_print_enum_variant_body` / `dispatch_enum_variant_pattern` の各 PayloadSlot match に Tuple arm を追加。新ヘルパ `copy_tuple_elements` (element-wise load + store) と `lower_into_tuple_slot` (tuple literal or 同型 tuple identifier rhs を slot element 列に書き込む) を追加。`emit_print_enum_variant_body` の Tuple は既存 `emit_print_tuple` をそのまま再利用 (`Pair::Both((3, 4))` の出力)。`substitute_payload_type` と `lower_type_arg` に `TypeDecl::Tuple(elements)` アームを追加し、各要素を再帰 lower したうえで `intern_tuple` で `Type::Tuple(id)` を生成 — generic な `Option<(i64, i64)>` も解決可能に。Name サブパターン bind 時は per-element local を fresh allocate して `copy_tuple_elements` で deep-copy、`Binding::Tuple` として登録するので arm body 内で `t.0` / `t.1` access が可能。要素は scalar (i64/u64/f64/bool) のみ受理し、ネストした compound (struct / 別 tuple / enum) は reject。e2e テスト 3 件追加（`Pair::Both((i64, i64))` 構築 + match、tuple payload の println、ジェネリック `Option<(i64, i64)>`）。フル e2e 121 件 + consistency 16 件全 green。

   **2026-05-01: enum payload に struct (Phase N)** — `PayloadSlot` に第3 variant `Struct { struct_id, fields }` を追加（既存の `Binding::Struct` と同じ field tree を持つ）、`is_supported_enum_payload` に `Type::Struct(_)` を追加。`allocate_payload_slot` が struct slot を `allocate_struct_fields` 経由で展開、`load_enum_locals_into` / `flatten_enum_dests_into` / `copy_enum_storage` / `emit_print_enum_variant_body` / `dispatch_enum_variant_pattern` の各 PayloadSlot match に Struct arm を追加。新ヘルパ `copy_struct_fields` (再帰的に leaf scalar 単位で load + store) と `lower_into_struct_slot` (StructLiteral or struct identifier rhs を slot field tree に書き込む) を追加。`substitute_payload_type` を struct templates も受け取るシグネチャに拡張し (`instantiate_enum` も同様)、enum payload で struct を直接参照可能に。`lower_type_arg` も struct instantiation 対応で `Option<Point>` 形式の annotation を解決。Name サブパターン bind 時は struct slot 用に `allocate_struct_fields` + `copy_struct_fields` で fresh struct binding を作成。e2e テスト 3 件追加（`Shape::Wrap(Point)` 構築 + match、struct payload の println、ジェネリック `Option<Point>`）。

   **2026-05-01: enum payload に f64 (Phase M)** — Phase G の payload 型制約を緩和: `is_supported_enum_payload` に `Type::F64` を追加、`substitute_payload_type` 経由で `f64` も通る。storage / load / copy / print いずれも特別な処理は不要 (cranelift は f64 を natively 扱い、`add_local(Type::F64)` / `LoadLocal` / `StoreLocal` がそのまま動く、`Print` の f64 ヘルパは既存)。e2e テスト 3 件追加（`Shape::Circle(f64)` の構築 + match、f64 payload の println 出力、ジェネリック `Option<f64>` 経由）。

   **2026-05-01: ジェネリック関数モノモル化 (Phase L)** — 関数版の monomorphization。`lower_program` の pass 1 で `func.generic_params.is_empty() == false` を template として `GenericFuncs: HashMap<DefaultSymbol, Rc<Function>>` に格納（reject せず）、`function_index` には登録しない。`lower_call` で関数解決時に `module.function_index` ミス → `generic_funcs` ヒットの場合、call sites の引数式から型引数を推論し（`infer_generic_args_from_param`: scalar は `value_scalar`、struct/enum identifier は binding の `struct_id` / `enum_id`）、`instantiate_generic_function` で fresh `FuncId` + mangled export name (`toy_name__type_args`) で declare、`pending_generic_work` queue に body lowering ジョブを push。`lower_program` の末尾で queue を drain し、各 instance を template AST + `type_subst: HashMap<DefaultSymbol, Type>` で lower（subst-aware の `lower_type_with_subst` でパラメータ / return 型を解決）。Generic 関数からの generic 関数呼び出しもチェーン展開可。`FunctionLower` に `generic_funcs` / `generic_instances` / `pending_generic_work` / `type_subst` を追加。e2e 4 件追加（`id<T>` を 2 種で instantiate / `pair_sum<T>` 2 引数 / `unwrap_or<T>(Option<T>, T)` / generic-from-generic チェーン）。フル e2e 112 件 + consistency 16 件 + workspace 全 green。

   **2026-05-01: ジェネリック struct モノモル化 (Phase K)** — Phase F (ジェネリック enum) と同型の refactor。IR の `Type::Struct(DefaultSymbol)` を `Type::Struct(StructId)` に、`Module.struct_defs` を `Vec<StructDef>` に (各 entry が `base_name` + `type_args` + `fields` を保持)、`Module.struct_index: HashMap<(DefaultSymbol, Vec<Type>), StructId>` で dedup。lower.rs に `StructTemplate { generic_params, fields }` を追加し `collect_struct_defs` を template 収集に変更、`instantiate_struct` ヘルパで substitution + intern、`substitute_field_type` で TypeDecl::Generic / nested 型引数 (`Cell<Cell<i64>>`) を再帰 lower。`Binding::Struct.struct_id` / `FieldShape::Struct.struct_id` に rename し、`allocate_struct_fields` / `store_struct_literal_fields` / `emit_print_struct` / `lower_struct_literal_tail` を `StructId` 受け取りに変更。`resolve_struct_instance` / `extract_struct_type_args` ヘルパで val/var アノテーションから type_args を抽出。e2e 4 件追加（`Cell<u64>` 単発、`Cell<u64>` + `Cell<i64>` 共存、`Cell<u64>` 戻り値、`Pair<A, B>` 2 型パラメータ）。フル e2e 106 件 + consistency 16 件 + frontend / interpreter green。

   **2026-05-01: enum 全体の再代入 + overflow 挙動の統一 (Phase H + I)** — `lower_assign` が `Identifier(sym)` の lhs を Binding::Enum に対しても受理するよう拡張: rhs を `lower_into_enum_storage` 経由で既存 storage に書き込む（cranelift の def_var が再 binding を扱う、追加 local 不要）。interpreter `evaluation/operators.rs` の整数算術を `wrapping_*` に切替（`l + r` → `l.wrapping_add(r)` 等）。これで `compiler` / `JIT` / `interpreter` の overflow 挙動が一致（全て wrap セマンティクス）。consistency tests に `u64_wrapping_overflow_match` / `u64_wrapping_underflow_match` を追加して 3-way 一致を保証。e2e テスト 1 件追加（`var p` の enum 再代入）。

   **2026-05-01: ネストした enum payload + サブパターン (Phase G)** — `EnumStorage` 構造体を導入して enum binding の storage を recursive に: `payloads[variant_idx]` が `Vec<PayloadSlot>`、各 slot は `Scalar { local, ty }` または `Enum(Box<EnumStorage>)`。`Binding::Enum`、`MatchScrutinee::Enum`、`pending_enum_value` は `(LocalId, Vec<Vec<(LocalId, Type)>>)` から `EnumStorage` に切替。`allocate_enum_storage` / `allocate_payload_slot` / `load_enum_locals` / `flatten_enum_dests` / `copy_enum_storage` / `write_variant_into_storage` / `emit_print_enum` / `emit_print_enum_variant_body` をすべて recursive に。`dispatch_enum_variant_pattern` ヘルパが nested EnumVariant サブパターンを再帰 dispatch（インナーは新しい EnumStorage を `allocate_enum_storage` + `copy_enum_storage` で作って Binding::Enum として bind）。`instantiate_enum` の payload 検証を `Type::Enum` 対応に拡張、`substitute_payload_type` を recursive にして `Option<Option<i64>>` のような注釈をルーティング。`extract_enum_type_args` も再帰 lower で nested 型引数を扱う。e2e テスト 4 件追加（nested Some/Some / nested inner None / nested outer None / nested println recurse）。

   **2026-05-01: ジェネリック enum モノモル化 (Phase F)** — IR の `Type::Enum(symbol)` を `Type::Enum(EnumId)` に refactor、`Module.enum_defs` を `Vec<EnumDef>` に変更（各 entry が `base_name` + `type_args` + `variants` を保持）、`Module.enum_index: HashMap<(DefaultSymbol, Vec<Type>), EnumId>` で `(base_name, type_args)` → `EnumId` を dedup。lower.rs に `EnumTemplate { generic_params, variants }`（payload は AST の `TypeDecl` のまま）と `instantiate_enum` ヘルパ。`lower_param_or_return_type` が `TypeDecl::Enum(name, args)` / `TypeDecl::Struct(name, args)` を受け取って instantiate。`lower_let` がアノテーション (`val x: Option<i64> = ...`) を受け取り `resolve_enum_instance` / `resolve_enum_instance_with_args` で型引数を抽出（インファレンス: アノテーション → 関数戻り型 → 引数型）、tuple variant の引数型からの推論にも対応。すべての enum 構築 / match / boundary path を EnumId 経由に書き換え（`Binding::Enum.enum_id`, `MatchScrutinee::Enum.enum_id`）。frontend type-checker は `Struct(name, args) <-> Enum(name, args)` の equivalence も追加（パーサが `Name<...>` 注釈を Struct 形式で出すため）。e2e 5 件追加（明示注釈 `Option<i64>` / `Option<u64>` / 引数型から推論 / 関数戻り値 / 同一 template の異なるインスタンス共存）。

   **2026-05-01: enum 戻り値を実用パスとして開通 (Phase E)** — frontend `type_checker/visitor.rs` の関数 return-type 比較に `(Enum(a, []), Identifier(b))` / `(Identifier(b), Enum(a, []))` の equivalence を追加（既存の Struct/Identifier ケースと対称、`is_equivalent` の挙動と一致）。これで `fn make() -> Shape { ... }` のような非ジェネリック enum 戻り型がパースされたまま reject されなくなる。compiler 側は `lower_body` を更新: 関数の return type が `Type::Enum` の場合は target locals を pre-allocate し、body 全体を `lower_expr` の代わりに `lower_into_enum_target` で処理する。これで body の tail が `Enum::Variant` 直接構築でも、if-chain / match の各分岐が enum を返すパターンでも、既存の enum binding identifier でも全て同じ実装パスを通る (`pending_enum_value` channel + `emit_implicit_return` の Type::Enum arm が値を流す)。e2e テスト 4 件追加（tail-position binding return / if-chain return / match return / 単一 constructor return）。

   **2026-05-01: enum 構築を式位置で / Phase D + lower_match 推論バグ修正** — `lower_let` に composite enum-RHS パスを追加: `detect_enum_result` で if-chain / match / block の全 tail が同じ enum を返すか静的判定し、合致すれば一度だけ tag + payload locals を allocate、`lower_into_enum_target` を経由して各分岐が共有 target に書き込む。`lower_into_enum_target` は QualifiedIdentifier (unit) / AssociatedFunctionCall (tuple) / 既存 enum binding identifier / nested IfElifElse / Match / Block を再帰的にハンドル、`write_enum_into_target` (tag + payload store) と `copy_enum_into_target` (binding 全 local の forward) を共通化。`lower_if_chain_into_enum` と `lower_match_into_enum` は scalar 版と同じ block 構造だが result_local の代わりに enum target に書く（cranelift の `def_var` walk が SSA merge を担当）。**Phase A2 から潜在していたバグも修正**: `lower_match` の result_ty 推論が `value_scalar` を使っていたため、全 arm body が `Name` サブパターンへの参照のみだと（`Pick::A(n) => n` 等）binding が見えず Unit に degrade して "function falls through" エラーになっていた。`arm_body_type` ヘルパを追加し、scrutinee の payload 情報から pattern 由来の Name を一時的に bindings に投入して inference、終了時に rollback。e2e テスト 4 件追加（if-chain で enum 構築 / match arms で enum 構築 / nested if で enum 構築 / 一方の分岐は既存 binding を forward）。

   **2026-05-01: enum を `print` / `println` で受理 (Phase C)** — `lower_print` の Binding::Enum 拒否を `emit_print_enum` 呼び出しに置き換え。`emit_print_enum` は runtime で tag を読み出し、`n - 1` 個の brif chain で variant 分岐（最後の variant は無条件 fallthrough）、各 per-variant body block から merge にジャンプ。`emit_print_enum_variant_body` ヘルパが `EnumName::VariantName` を `PrintRaw` で出力し、tuple variant なら `(`, payload を `, ` 区切りで `Print` 列、`)` で閉じる。trailing newline は最終 fragment（unit variant なら header 自体、tuple variant なら閉じ括弧）に乗せる。interpreter `Object::to_display_string` と完全一致 (`Color::Red`、`Shape::Circle(5)`、`Shape::Rect(3, 7)`、`Maybe::Yes(true)`)。e2e テスト 7 件追加（unit variant、1-payload、multi-payload、bool payload、ループ内 runtime dispatch、改行なし print + concat、関数引数経由）。

   **2026-05-01: enum 関数境界 (Phase B)** — IR に `InstKind::CallEnum { target, args, dests }` を追加（CallStruct / CallTuple と同じ多値 return パターン）、`Type::Enum(name)` を `flatten_struct_to_cranelift_tys` で `[I64 (tag), variant0_payloads..., variant1_payloads..., ...]` に flatten（caller / callee 双方が同じ canonical 順を使うので boundary 一致）。lower.rs に `allocate_enum_storage` ヘルパを切り出して param entry / `bind_enum` / enum-returning call の dest 側で再利用、`load_enum_locals` / `flatten_enum_dests` で boundary の load / dest 列を生成。`lower_param_or_return_type` を `enum_defs` も受け取るシグネチャに拡張、`Type::Enum` を param / return として受理。`lower_body` が enum param で Binding::Enum を allocate（cranelift block params が同順で local に bind される）、`lower_call_args` で enum identifier を tag + payload 列に展開、`lower_let` で enum-returning call を `CallEnum` に dispatch。`pending_enum_value` channel + `Identifier(enum_binding)` の tail-position stash + `emit_implicit_return` / `Stmt::Return` の `Type::Enum` 経路を追加（lowering / codegen は完了、frontend type-checker が `-> Shape` を reject するため実用パス未開通）。e2e テスト 4 件追加（enum を function 引数で渡す / unit variant 経路 / 2 段経由 / branch ごとに構築して渡す）。

   **2026-05-01: enum + match Phase A2** — Phase A1 の `lower_match` を全面書き換え。`MatchScrutinee::{Enum, Scalar}` を導入し、scrutinee 分類で enum binding / scalar binding / scalar 式の 3 経路を統一処理。トップレベルパターンに `Literal(_)` を追加（scalar scrutinee 専用、equality branch を発行）、variant サブパターンに `Literal(_)` を追加（payload に追加の equality check、bind 前に走るので失敗時に stray binding が body に入らない）、`MatchArm.guard` を実装（pattern 一致 + bindings 投入後に評価、false で次 arm へ）。bindings は arm 開始時に snapshot し、arm 終了時に rollback して隣の arm に漏れない。e2e テスト 9 件追加（scalar u64 / bool / i64 リテラル match、scalar wildcard、variant の literal subpattern hit / miss、variant guard、scalar guard、guard false の fallthrough）。

   **2026-05-01: enum + match Phase A1** — 非ジェネリックな `enum E { Unit, Tuple(args), ... }` と `match` 式をコンパイラで実装。IR に `Type::Enum(DefaultSymbol)` / `Module.enum_defs: HashMap<DefaultSymbol, EnumDef>` (各 variant の name と payload type 列) を追加。lower.rs に `collect_enum_defs` を追加（ジェネリック enum と `f64` / Unit / 構造体 / tuple / enum payload を reject）、`Binding::Enum { enum_name, tag_local: LocalId, payload_locals: Vec<Vec<(LocalId, Type)>> }` を新設。enum 構築は `val s = Enum::Unit`（QualifiedIdentifier）と `val s = Enum::Tuple(args)`（AssociatedFunctionCall）の immediate rhs のみ受理。`bind_enum` ヘルパが tag local + 全 variant 分の payload locals を allocate、選んだ variant の payload を args から store。`lower_match` は scrutinee を identifier 経由で `Binding::Enum` に解決し、各 arm を tag 比較 → body block の brif chain として lower（wildcard は無条件 jump）。Name サブパターンは payload を fresh scalar local に bind、`_` は discard。merge は if-chain と同じ result_local パターン。e2e テスト 7 件追加（unit variant、1-payload、multi-payload、wildcard、`_` サブパターン、bool payload、loop 内 match）。**Phase A2 (defer)**: ジェネリック enum、ネストパターン、リテラルパターン、guard、enum 式位置構築（`if` 戻り値）、関数境界、`f64` payload。

   **2026-05-01: print/println で struct / tuple を受理** — IR に `InstKind::PrintRaw { text: String, newline: bool }` を追加（codegen-synthesised な punctuation / field name 用、source-program symbol を持たない）。`lower_print` が引数を Identifier として解決し、struct binding なら `emit_print_struct`（field をアルファベット順にソート、`Name { field: value, ... }` 形式で interleaved な PrintRaw + Print 列を発行、ネスト struct は再帰）、tuple binding なら `emit_print_tuple`（`(a, b, ...)` / 1-tuple は `(a,)`）を呼び出し。codegen は `raw_print_strings: HashMap<Vec<u8>, DataId>` で literal bytes 単位に intern（`", "` / `": "` 等の重複断片を共有）、`declare_raw_print_imports` で per-function に GlobalValue を引き込み、`toy_print_str` / `toy_println_str` を再利用。e2e テスト 8 件追加（struct 基本、field 順 alphabetical、改行なし print、ネスト struct、bool field、tuple、mixed-type tuple、1-tuple）。**制約**: struct リテラル直接や struct-returning call の戻り値は不可（いったん `val` で受ける）。interpreter `Object::to_display_string` と完全一致する出力。

   **2026-05-01: cast (`as`) / f64 / struct boundary crossing を追加** — IR に `Type::F64` / `Const::F64` / `InstKind::Cast { value, from, to }` / `InstKind::CallStruct { target, args, dests }` / `Module.struct_defs` を追加、`Terminator::Return` を `Vec<ValueId>` に変更（scalar / void / struct return を vec 長で表現）。lower で `Expr::Cast`、`Expr::Float64`、struct 引数 / 戻り値、`val x = struct_returning_call()` を扱う。tail-position の struct literal は `pending_struct_value` に貯めて implicit return が消費する設計（IR の SSA グラフに struct 値が流れない）。codegen で struct sig を per-field cranelift param / multi-return に展開、`Type::F64` 演算は cranelift の `fadd/fsub/fmul/fdiv/fcmp` + `fneg`、cast は `fcvt_from_sint/uint` / `fcvt_to_sint/uint_sat` で。runtime に `toy_print_f64` / `toy_println_f64`（`%g` / `%.1f` 切替）追加。e2e テスト 10 件追加（i64↔u64 cast、float-int round trip、float-int truncate、f64 算術 / unary neg / 関数呼び出し、struct return / param / round trip / 明示 return）。

   **現在の制約（2026-05-03 時点、live state）** — 詳細は `compiler/README.md`。以下の機能は未対応で、検出時は明確なエラーで reject される:

   - **型**: `i64` / `u64` / `f64` / `bool` / `Unit` / `str` (Phase T) / `ptr` (#121 Phase A) / `Allocator` (#121 Phase B-min、u64 sentinel) / narrow int 全 6 width (`u8/u16/u32/i8/i16/i32`、NUM-W-AOT) を value として扱える。allocator は `__builtin_default_allocator` / `__builtin_current_allocator` + `with allocator = ...` scope が動作 — `__builtin_arena_allocator` / `__builtin_fixed_buffer_allocator` のみ未対応 (#121 Phase B-rest)。heap / pointer builtins (`__builtin_heap_alloc/realloc/free/ptr_read/ptr_write`) は対応済み
   - **文字列**: 値として `val s = "foo"` / 関数 param / return / struct field 等で扱える (Phase T)。AOT の `.rodata` per-literal layout は `[bytes][NUL][u64 len LE]` (STR-PTR-LEN)。str runtime value は **u64 len フィールドへのポインタ** (STR-LEN-O1)。`s.as_ptr()` (`core/std/str.t::AsPtr`) で byte ポインタ、`s.len()` (`core/std/str.t::Length`) で **O(1)** バイト長 (`load.i64(s, 0)`)。低層 builtin は `__builtin_str_to_ptr(s)` / `__builtin_str_len(s)` / `__builtin_mem_copy(src, dest, n)`
   - **キャスト**: `as` で全 13 type の cross-cast マトリクス対応 (i64/u64/f64/bool + 6 narrow int)、generic 2-phase で sext/uext + ireduce + fcvt_*_sat
   - **f64 制約**: `%` (mod) は cranelift に native fmod が無いため reject
   - **tuple**: 局所バインディング・要素アクセス・要素書き込み・分解、関数引数 / 戻り値として tuple 値を渡せる、ネストタプル + tuple-of-struct + struct-of-tuple サポート (Phase Q1+Q2)、enum payload に tuple も可 (Phase O)
   - **コレクション**: array (Phase Y/Y2/Y3) 対応 — runtime index、constant-bound range slicing、scalar/struct/tuple 要素、narrow scalar の per-width packing (NUM-W-AOT-pack Phase 1)。dict は user-space `Dict<K, V>` 経由で利用 (`core/std/dict.t`、DICT-AOT-NEW Phase B/C/D)
   - **enum / match**: 非ジェネリック / ジェネリック enum + unit / tuple variant + `match` + 全 backend boundary対応。payload 型は scalar / 別 enum / struct / tuple すべて可、ネストパターン + 再帰 print 動作。深い網羅性解析 (96残 前半) で nested EnumVariant の coverage を検証
   - **trait**: `trait` 宣言と `impl <Trait> for <Type>` 対応 (Phase R)、trait method dispatch 動作、`<T: Trait>` bound 経由の generic 関数も対応
   - **allocator (#121 Phase A + B-min 対応済み)**: 残 (#121 Phase B-rest): `__builtin_arena_allocator` / `__builtin_fixed_buffer_allocator` の native runtime 実装 (現状 sentinel 0 以外の handle を生成する手段なし)、`with` body 内の早期 exit (return / break / continue) で stack を確実に pop する panic-style cleanup、`AllocatorBinding::Generic/Local/Ambient` を heap builtin 命令に配線して active stack 経由ディスパッチ (現状 heap_alloc は常に libc 直呼び)
   - **DbC**: `requires` / `ensures` 節は実行時チェック、`--release` で全 contract skip
   - **const**: top-level `const NAME: Type = expr` 対応 (リテラル / 既存 const 参照 / 算術 fold のみ)
   - **generics**: 関数 (Phase L)、struct (Phase Q1/Q2)、impl method (Phase R3)、method-only `<U>` (Phase X)、associated function (DICT-AOT-NEW Phase B) 対応。per-monomorph subst で val annotation 内の generic param も解決 (DICT-AOT-NEW Phase C)
   - **method receiver**: `self: Self` / `&self` / `&mut self` (MUT-SELF Stage 1) 対応。`&mut self` は Self-out-parameter convention で受信側 leaf locals に writeback
   - **struct の制約**:
     - 関数引数 / 戻り値として struct 値を渡せる (per-field 展開)
     - struct-returning call は式位置で使えず、必ず `val x = ...` で受ける
     - struct binding 全体の再代入 (`q = p`) は不可 (field 単位の代入のみ)
     - ネストした struct field と chain access (`a.b.c`) はサポート、ただし leaf scalar への代入のみ (`p.inner = Inner { ... }` 不可)
     - struct binding を `print` / `println` に渡せる (field アルファベット順、ネスト struct 対応)
     - generic struct 含めて全部対応 — `Dict<i64, u64> = Dict::new()` のような annotation 駆動の monomorphization が AOT で動作
   - **print / println**: `i64` / `u64` / `f64` / `bool` / 文字列リテラル / struct binding / tuple binding を受理。struct / tuple は **identifier（`val` / `var` 由来の binding）のみ** で、struct リテラル直接や struct-returning call の戻り値を直接 print することはできない（いったん `val` で受ける）。dict は不可
   - **panic / assert**: メッセージは文字列リテラル限定（const binding や concat 等は不可）
   - **その他**: 文字列ビルトインメソッド (`.len()` / `.concat()` 等)、associated function (`Foo::new()`)、メソッド呼び出し (`obj.method()`)、関数ポインタはいずれも未対応
   - **既知の挙動差**: compiler は `panic` / `print` / `println` を stdout に出力する。interpreter / JIT は `panic` を stderr に出す（libc `puts` 経由のシンプルな実装に揃えているため。出力方法を選べる仕組みは未着手）
   - **挙動の統一済み (旧称: 既知の挙動差)**: u64 / i64 の overflow は全 3 経路で wrap セマンティクス（interpreter は `wrapping_*` を使用、compiler / JIT は cranelift の wrapping arithmetic）。consistency tests で overflow / underflow ケースも 3-way 一致を検証

   **2026-05-01: print/println と struct を compiler で対応** — `compiler/runtime/toylang_rt.c` に `toy_print_*` / `toy_println_*`（i64 / u64 / bool / str 各 8 関数）を新設、driver が `cc` で同時にコンパイル＋リンク（macOS aarch64 の variadic ABI 問題を回避）。IR に `InstKind::Print { value, value_ty, newline }` と `InstKind::PrintStr { message, newline }` を追加、lower.rs で `BuiltinCall(Print/Println, args)` を引数の static type で振り分け（文字列リテラルは PrintStr 経路）。codegen で 8 つのランタイムヘルパーを `Linkage::Import` で extern 宣言、unique 文字列リテラルを `.rodata` の DataDescription に展開。struct: `lower.rs` の `Binding` を `Scalar` / `Struct { fields: Vec<FieldBinding> }` enum に拡張、struct 定義は `collect_struct_defs` で program 走査時に収集（フィールドは scalar のみ）。`Expr::StructLiteral` / `Expr::FieldAccess` / `Expr::Assign(FieldAccess, _)` を lowering で扱い、struct 値は IR レイヤを通さず field ごとに LocalId へ展開（codegen は変更なし）。e2e テスト 9 件追加（println string / numeric / bool / 改行なし print / loop 内 print / struct literal / field write / field 算術 / loop 蓄積）。**制約**: struct は関数引数/戻り値として渡せず、ネストフィールドアクセス未対応。**既知挙動差**: panic / print / println は stdout に出力。

   **2026-05-01: panic / assert を compiler で対応 + 3 経路一致テスト追加** — `Terminator::Panic { message: DefaultSymbol }` を IR に追加、`lower.rs` で `BuiltinCall(Panic, args)` と `BuiltinCall(Assert, args)` を検出して lower（assert は `Branch` + 失敗 block の `Panic`）。`codegen.rs` で各 unique panic message に `cranelift_module::DataDescription` で `.rodata` エントリ確保（"panic: <msg>\0"）、`puts` / `exit` を `Linkage::Import` で extern 宣言、Panic terminator は `puts(addr); exit(1); trap` で lower。stdout 経由（interpreter は stderr）の差分は MVP の既知の挙動差として README 記載。e2e テスト 4 件追加（panic 出力、assert 通過、assert 失敗、`if c { panic } else { v }` の式位置 panic）。`compiler/tests/consistency.rs` を新規追加: interpreter（lib API）と compiler（subprocess）で 10 件のプログラム（リテラル / 算術 / signed / fib / for-sum / while-break / elif / 短絡 / nested calls / bool 戻り値）を 3 経路一致のうち 2 経路で同 exit code を保証（JIT 一致は `interpreter/tests/jit_integration.rs` で既存検証）。

   **2026-05-01: 中間 IR レイヤを導入** — `compiler/src/ir.rs` に `Module` / `Function` / `Block` / `Instruction` / `Terminator` / `Type` / `ValueId` / `LocalId` / `BlockId` / `FuncId` を定義（`toy` プレフィックス無し）、`Display` で `--emit=ir` 用の textual format を提供。`compiler/src/lower.rs` で AST → IR の lowering pass を実装、`compiler/src/codegen.rs` を IR → Cranelift IR + `.o` 出力に再構成。値モデルは「型付き local slot + 関数ローカル SSA 値」（`val` / `var` / 引数は LocalId 経由で `LoadLocal` / `StoreLocal`、SSA 構築は Cranelift の `FunctionBuilder` に委譲）。CLI に `--emit=clif` 追加（Cranelift IR の textual dump）、既存の `--emit=ir` は新 IR を表示するように切替。e2e テスト 10 件 green（既存 9 件 + `--emit=clif` 検証 1 件）。`AllocatorBinding` の配線は次の段階で IR の `InstKind` に追加予定。

   **2026-05-01: Phase B の MVP 着手完了**。`compiler/` クレートを新設、`compiler input.t -o output` で `.o` を出して `cc` でリンク → 実行ファイルを生成する経路が動作。`--emit=ir|obj|clif|exe`（default exe）対応、CLI / lib API 両用。サポート: `i64` / `u64` / `bool` / `Unit` のみ、リテラル / 算術 / 比較 / 短絡論理 / unary / val/var / assignment / if-elif-else / while / for-range / break / continue / return / 同一プログラム内の関数呼出。未対応で次のフェーズに送り（rejected with clear error）: 文字列・struct・tuple・配列・dict・enum・trait・allocator・contracts・generics・panic/assert・print/println・heap builtins・i64↔u64 以外の cast。残り計画は元の Phase A〜E に従って継続。現状は AST → tree-walking interpreter（+ オプトイン Cranelift JIT）のみで、IR レイヤと実行可能ファイル出力経路は存在しない。`compiler/` クレートを新設し、frontend（parser / type_checker）を共有しつつ独自の codegen パイプラインを構築する。

   **Phase A: IR の新設**
   - 中間表現 `toy_ir`（仮称）の定義: SSA ベース、関数 / 基本ブロック / 命令の最小構成
   - 値表現は scalar / struct / tuple / enum を一級で扱う型付き SSA（cranelift IR にそのまま下ろせる粒度）
   - alloc site ごとに `AllocatorBinding::{Static(id), Generic(type_param), Ambient, Local(var_id)}` を付与（`ALLOCATOR_PLAN.md` Phase 3 の設計に従う）
   - AST → IR の lowering パス（型チェック後に走る、frontend と compiler の境界に置く）
   - `with` ブロックの allocator 式が compile-time 定数かを判定し、内部の `Ambient` を `Static` に置換する定数伝搬パス
   - JIT 側も将来的に同 IR を共有できるよう、IR は backend 非依存の表現に保つ（短期的には JIT は AST 直 codegen のまま、共有は後続フェーズで検討）

   **Phase B: バックエンドと実行ファイル生成**
   - バックエンド候補: **Cranelift Object**（既存 JIT との API 共有が容易・推奨）/ LLVM（最適化と互換性）/ 独自（学習目的）
   - 第一候補は `cranelift-object` で `.o` を出力 → system linker で実行ファイル
   - Windows / macOS / Linux のリンカ駆動とトリプル差異を吸収する thin layer
   - `compiler` CLI: `compiler input.t -o output` で実行ファイルを生成、`--emit=ir` / `--emit=obj` / `--emit=asm` で中間生成物も観察可能に

   **Phase C: 呼び出し規約とランタイム**
   - 案A（隠しパラメータ）: 全関数のシグネチャに `&dyn Allocator` を暗黙追加。`with` は呼び出し時に引数を差し替える
   - 案C（単相化）: `#[specialize_allocator]` 属性 / コンパイル時定数 allocator 経路は allocator 型ごとに複製
   - ランタイムを C ABI の `.o`（または静的ライブラリ）として提供: `HeapManager` / `GlobalAllocator` / `ArenaAllocator` / `FixedBufferAllocator` / `panic` helper / `process::exit` / I/O builtins (`print` / `println`) / 文字列メソッド
   - 文字列リテラルとシンボルテーブルを `.rodata` に埋め込む経路（JIT の `JIT_STRING_INTERNER` を参考に）
   - `main` 関数の数値戻り値をプロセス終了コードに（interpreter / JIT と同じセマンティクス）

   **Phase D: 機能網羅とテスト**
   - サポート対象: i64 / u64 / f64 / bool / str / ptr / 固定配列 / tuple / struct / enum + match / generic（単相化）/ Allocator system / DbC（`requires` / `ensures`）/ panic / assert
   - エンドツーエンドテスト: interpreter / JIT / コンパイラの 3 経路で同一プログラムが同一の exit code + stdout を返すことを検証する `e2e_consistency_tests`
   - 既存の `interpreter/example/*.t` を全てコンパイルして実行できることをゴールに
   - `INTERPRETER_CONTRACTS=off` 相当の release 切替を `--release` フラグで提供

   **Phase E: 最適化**
   - 定数伝搬で `Static` 結合された allocator の vtable 呼び出しを devirtualize
   - インライン化による alloc 呼び出しの完全消去（arena 等）
   - hot path で vtable オーバーヘッドゼロになることを benchmark で確認
   - generic 単相化と code size のトレードオフを measure

   実装規模の目安: Phase A〜D を MVP として通すだけでも数セッション〜週単位。`#121` の Phase 4 native codegen は本タスクの一部として吸収される。

## 検討中の機能

* FFI/拡張ライブラリ
* 文字列操作
* ラムダ式・クロージャ
* モジュール拡張（バージョニング、リモートパッケージ）
* 言語組み込みテスト機能
* 言語内からのAST取得・操作
* LSP (Language Server Protocol) 対応 — エディタ統合 (補完、go-to-definition、hover、診断、フォーマット)。frontend の AST/型チェッカ・SourceLocation を再利用して `tower-lsp` などで実装

## 実装済み機能サマリー

### コア言語機能
- 基本言語機能: if/else/elif、for、while、break/continue、return
- 変数: val（不変）/var（可変）、コンテキストベース型推論
- 数値型: u64 / i64 / f64（f64 リテラルは `1.5f64` / `42f64` のように `f64` サフィックス必須、タプルアクセスとの曖昧性回避）。`as` による i64/u64 ↔ f64 変換、剰余 `%` と複合代入 `+= -= *= /= %=` 対応
- 固定配列: 型推論対応、インデックス型推論、境界チェック、要素に struct / tuple / 別配列も可
- 配列スライス: `arr[start..end]`、`arr[..]`、負インデックス`arr[-1]`対応
- 辞書（Dict）型: `dict{key: value}`リテラル、Object型キーサポート
- 構造体: 宣言、implブロック、フィールドアクセス（read/write 両対応）、メソッド (`self: Self` / `&self` / **`&mut self`** — Stage 1 reference receiver、AOT で Self-out-parameter writeback)、非ジェネリック struct でも `Struct::new()` の associated function、`__getitem__`/`__setitem__`、ネストフィールド (`a.b.c`) chain access
- タプル: 局所バインディング + 関数引数 / 戻り値、`val (a, b) = expr` 分解 (ネスト対応)、`t.0` access、ネストタプル + tuple-of-struct + struct-of-tuple
- Trait: `trait Name { fn m(self: Self) -> T }` 宣言、`impl <Trait> for <Struct> { ... }` 実装、`<T: SomeTrait>` bound、conformance チェック（型不一致・欠落メソッド検出）。**プリミティブ型に対する extension trait** (`impl <Trait> for i64/f64/...`) も interpreter / JIT / AOT 全 backend で動作。stdlib の `i64.abs()` / `f64.abs()` / `f64.sqrt()` も `core/std/{i64,f64}.t` の extension trait impl 経由。chained primitive method call (`x.abs().abs()`) も 3 backend 全対応 (`#194`)
- `extern fn` 宣言: `extern fn name(params) -> ret` で signature だけ宣言、body は backend (interpreter registry / JIT helper or native / AOT libm import) が提供。math intrinsic はすべてこの仕組み経由。generic params (`extern fn name<T>(x: T) -> T`) は parser で受理、interpreter で動作 (`#195`)
- 文字列: ConstString/String二重システム、`str.len()`、`.concat()`、`.trim()`、`.to_upper()`、`.to_lower()`、`.split()`、`.substring()`、`.contains()`。STR-PTR-LEN (`93892b2`): AOT の `.rodata` per-literal layout は `[bytes][NUL][u64 len LE]`、`__builtin_str_to_ptr(s)` / `__builtin_str_len(s)` で byte ポインタとバイト長を取れる。`core/std/str.t` の `AsPtr` / `Length` trait 経由で `s.as_ptr()` / `s.len()` も可
- コメント: `#`（行）、`/* */`（ブロック）
- Allocator システム: `with allocator = expr { ... }`、`ambient` キーワード、`<A: Allocator>` bound、自動 ambient 挿入、Global / Arena / **FixedBuffer** allocator (interpreter / JIT で 3 種すべて実装済み)。AOT は Phase A (heap / pointer builtins via libc) + Phase B-min (`__builtin_default_allocator` / `__builtin_current_allocator` + `with allocator = ...` scope) を対応 — arena / fixed_buffer の native backend は #121 Phase B-rest で未対応
- Enum + match: unit + tuple variant、`Enum::Variant` / `Enum::Variant(args)`、ジェネリック enum (`Option<T>`)、ネスト enum payload (`Option<Option<i64>>`)、payload に `f64` / struct / tuple / **str** (`#205` AOT も対応)、リテラル / ネスト / タプルパターン、guard (`if cond`)、網羅性チェック、到達性チェック。enum receiver method dispatch も interpreter / AOT で動作 (`#96` `#203`)。JIT は enum 値モデル未対応で silent fallback (`#204` で precise diagnostic)
- DbC: `requires` / `ensures` 節の実行時チェック、`ensures` 内の `result` バインド、`INTERPRETER_CONTRACTS` (interpreter) / `--release` (compiler) で gating
- `panic("msg")` / `assert(cond, msg)` ビルトイン (3 backend 全対応、release でも常時 active 設計)
- `__builtin_sizeof(value)`: primitive / struct / enum (1-byte tag + payload) / tuple / array をサポート

### 型システム
- 自動型変換・型推論（数値リテラルのサフィックス省略可）
- ジェネリック関数: `fn identity<T>(x: T) -> T`（パース→型推論→実行→3 backend モノモル化）
- ジェネリック構造体: `struct Container<T>`、constraint-based型推論
- ネストジェネリック: `Container<Container<T>>`（C++11スタイル`>>`分割）
- Self キーワード: implブロック内での構造体参照、プリミティブ impl では対応する `TypeDecl` (i64 / f64 / ...) に解決
- Trait bound: `<A: Allocator>` および `<T: UserTrait>` を関数・struct・impl に付与、呼び出し側で検証、bound 連鎖
- method-only generic params: `impl Box { fn pick<U>(self, a: U, b: U) -> U }`
- method receiver: `self: Self` (by-value) / `&self` / `&mut self` の 3 形式 (Stage 1 reference type)。trait conformance で receiver kind 完全一致を要求。AOT は `&mut self` を Self-out-parameter convention で writeback
- references (REF-Stage-2 (a)+(d)+(e)+(f)): `&T` / `&mut T` 引数型、explicit `&value` / `&mut value` borrow 式、`is_arg_compatible` で `T → &T` auto-borrow + `&mut T → &T` downgrade (auto-borrow `T → &mut T` は意図的に不許可)、`&mut <name>` は `var`-declared bare identifier のみ受理、syntactic escape rule (戻り型 / val-var binding / struct field に Ref 不可)。runtime / IR は erasure (true mutation 伝播は (b)/(c)/(g) で deferred)
- pattern match の deep exhaustiveness: `Option<Option<T>>` 等の nested EnumVariant も payload position 単位で coverage 検証 (96残 前半)

### モジュール・その他
- Go-styleモジュールシステム: package/import/qualified name resolution、3-segment 以上は alias 経由
- **コア・モジュール auto-load**: `<repo>/core/` を起動時に再帰的に integrate (`<exe>/core/` / `<exe>/../share/toylang/core/` / `<exe>/../../core/` の順に探索、CLI flag `--core-modules <DIR>` と env var `TOYLANG_CORE_MODULES` で override)。`math::sin(x)` 等が import 行なしで呼べる
- **per-module function namespacing**: IR / 型チェッカー / interpreter 実行時の関数テーブル全 3 層を `(Option<DefaultSymbol> qualifier, DefaultSymbol name)` キー化 (`#193` `#193b`)。同名 `pub fn` を複数モジュールが持っても安全に共存。bare 呼び出しは user-authored を優先、qualified 呼び出しは module qualifier 直接 lookup
- stdlib: `core/std/math.t` (math 関数) / `core/std/i64.t` (Abs trait) / `core/std/f64.t` (Abs/Sqrt impl) / `core/std/option.t` (`enum Option<T>` + is_some/is_none/unwrap_or/expect) / `core/std/result.t` (`enum Result<T, E>` + is_ok/is_err/unwrap_or/expect) / `core/std/hash.t` (`Hash` trait + 全 primitive 型 impl) / `core/std/dict.t` (`Dict<K, V>` user-space hash table、`new`/`insert(&mut self)`/`get`/`get_or`/`contains_key`/`remove(&mut self)`/`size`、3 backend 一致動作 — DICT-AOT-NEW Phase D 完全対応) / `core/std/str.t` (`AsPtr` + `Length` extension traits — `s.as_ptr()` で byte ポインタ、`s.len()` で **O(1)** byte 長) / `core/std/collections/vec.t` (`Vec<T>` user-space dynamic array、`new`/`push(&mut self)`/`pop(&mut self)`/`get`/`set(&mut self)`/`size`/`capacity`/`is_empty` + `FromStr::from_str(s) -> Vec<u8>` の memcpy ベース str → Vec<u8> 変換)
- 統合インデックスシステム: 配列・辞書・構造体で統一`x[key]`構文

### テスト状況
- 合計 1158 テスト, 31 skipped（100% 成功率、2026-05-04 時点 — DICT-CROSS-MODULE-OPTION / NUM-W-AOT-pack / #121 Phase A+B-min/B-rest / DICT-AOT-NEW Phase B+C+D / `&mut self` Stage 1 / 96残 前半 / STR-PTR-LEN / STR-LEN-O1 / VEC-collection / STDLIB-alloc-trait / REF-Stage-2 (a)(d)(e)(f) で複数 regression test 追加。compiler/e2e は 191、consistency は 30 強）
- 内訳: interpreter unit + integration、frontend unit、compiler e2e (191) + consistency (23) — 後者は interpreter / JIT / AOT 3 経路一致を保証
- パフォーマンス: `compiler/build.rs` で `toylang_rt.c` を pre-build、AOT 1 テストあたりの compile 時間は ~50ms。並列 wall-clock の dominate factor は macOS の Mach-O コード署名検証 (~150-300ms/binary、`compiler/README.md` 参照)

### パーサーの既知制限事項
- bare `self` 構文非対応（`self: Self` が必要）
- `else if` 未サポート（`elif`を使用）
- `val` はキーワードのためパラメータ名に使用不可
- `extern fn` の generic params は parser で受理されるが JIT/AOT は per-instance シンボル名を持たないため interpreter のみで動作 (`#195` / `#195b`)
- `package` 宣言 / `import` path のセグメントに primitive type キーワード (`i64` / `f64` / ...) は使えない (`core/std/i64.t` が `package` 宣言を省略しているのはこのため)
- 3-part qualified call (`std::math::abs(x)`) は parser が last 名のみ採用 — `import std.math` または auto-load 経由で `math::abs(x)` の形にする必要あり (`#185残`)
