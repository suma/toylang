# Debug: String array parsing issue investigation

fn main() -> u64 {
    # Test 2: Two string array (problem case)
    val arr2 = ["hello", "world"]
    val len1 = arr2[0u64].len()
    val len2 = arr2[1u64].len()
    
    len1 + len2
}